apiVersion: v1
kind: Service
metadata:
  name: {{ include "montr.fullname" . }}-api
  labels:
    {{- include "montr.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    {{- include "montr.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.api.port }}
      targetPort: http
