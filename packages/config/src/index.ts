/**
 * @montr/config — Zod config schema + multi-source loader with hardened,
 * safety-first defaults (auto-fix OFF, DAST OFF, budget hard-halt ON,
 * telemetry OFF, egress default-deny).
 */
export * from "./schema.js";
export * from "./loader.js";
