# Montr Secure System Overview

Scope: High-level architecture, mental model, directory index, and domain glossary for Montr Secure.
Rendering context: Isomorphic
Project tier: 4
Last updated: auto

Overview
Montr Secure is an on-premises, bring-your-own-LLM-key security orchestration platform that unifies static analysis, software composition analysis, secret detection, and dynamic exploit validation into an automated, six-layer analysis pipeline. The platform correlates raw multi-engine findings against structural application models, confirms genuine exploitability through static data-flow proofs and gated live DAST, synthesizes validated patches with proof-of-fix tests, and outputs compliance-ready reports with gated pull request automation. The codebase is organized as a Turborepo monorepo comprising a Fastify REST API, a Next.js operator console, a BullMQ pipeline worker, a montr CLI for local/CI-triggered scans, and seventeen decoupled TypeScript packages.

System Mental Model
The system operates as an end-to-end security pipeline structured across six distinct processing layers orchestrated by a resumable finite state machine.
Layer 0 intake and scoping builds a comprehensive structural application map of routes, data models, and taint surfaces while calculating token and cost estimates.
Layer 1 discovery executes multi-engine static analyzers, secret detectors, and dependency reachability scanners concurrently to capture raw candidate findings.
Layer 2 correlation scores candidates across reachability, exposure, and business impact to merge duplicate root causes into ranked probable findings without discarding evidence.
Layer 3 exploit confirmation proves vulnerability exploitability via deterministic interprocedural taint tracing and approver-gated live dynamic security testing against staging environments.
Layer 4 fix generation produces unified diff patches, proof-of-fix regression tests, and safety classifications that partition fixes into auto-eligible or human-required.
Layer 5 gate and reporting generates executive summaries, compliance exports across SARIF, SOC2, ISO27001, and OWASP, and opens automated pull requests for eligible remediations.

Key Architectural Decisions
Strict perimeter containment ensures that source code never leaves the client infrastructure except when transmitted directly to the client configured LLM provider endpoint.
Row-scoped multitenancy guarantees complete isolation across clients in Postgres, with high-security fields encrypted at rest using AES-256-GCM.
Resumable state machines allow long-running analysis workflows to pause at estimate and fix gates, survive worker crashes, and resume from checkpoints without re-running completed layers.
Budget hard-halt policies continuously meter token expenditure against user ceilings, stopping pipeline execution and producing partial reports before budget overruns occur.
Append-only hash-chained audit logging cryptographically seals all system actions, gate approvals, and operator decisions to provide tamper-evident compliance trails.

Cross-Cutting Concerns
Authentication and access control employ Argon2 password hashing, JSON Web Tokens, CSRF tokens, and role-based permissions dividing capabilities across Operator, Approver, and Viewer roles.
Error handling relies on custom domain error classes defined in packages/contracts/src/errors.ts mapped to standardized HTTP status codes and structured log envelopes.
Telemetry and logging utilize a content-aware scrubber in packages/telemetry/src/scrubber.ts to strip credentials, tokens, and raw code snippets before writing to stdout or OpenTelemetry collectors.
Styling across the operator web console uses Tailwind CSS v4 design tokens with a dark-first color scheme and semantic severity badges.

Documentation Directory Map
docs/overview.md — Master entry point, architecture summary, directory map, and glossary.
docs/maintenance.md — Step-by-step update guide and decision tree for maintaining documentation after code changes.
docs/architecture/folder-structure.md — Structural layout of packages, apps, tests, and configuration files.
docs/architecture/rendering-strategy.md — Rendering architecture across the Next.js console and Fastify backend.
docs/architecture/data-flow.md — Complete lifecycle of data, pipeline transitions, and serialization boundaries.
docs/ui/component-library.md — Reusable UI primitives and domain widgets in the operator web console.
docs/ui/layout-system.md — Root and nested layout hierarchies, route wrappers, and navigation chrome.
docs/ui/theming.md — Color system, dark theme tokens, severity scales, and typography definitions.
docs/api/route-handlers.md — Fastify HTTP route handlers, path parameters, role requirements, and response contracts.
docs/api/external-services.md — Third-party integrations including LLM providers, VCS platforms, and vulnerability databases.
docs/api/database.md — Prisma relational schema, multitenancy design, encrypted fields, and audit log chaining.
docs/state/client-state.md — TanStack React Query cache management, role context, and local component state.
docs/state/server-state.md — Postgres persistence, Redis BullMQ queues, resume checkpoints, and cache policies.
docs/auth/auth-flow.md — Session lifecycle, token issuance, password hashing, and authentication guards.
docs/auth/authorization.md — Role-based permission matrix, gate approval constraints, and DAST authorization rules.
docs/infra/environment.md — Complete catalog of environment variables, defaults, and exposure levels.
docs/infra/deployment.md — Docker Compose configurations, Kubernetes Helm charts, and air-gapped installation procedures.
docs/infra/testing.md — Vitest test suites, coverage floors, golden corpus evaluation, and smoke tests.
docs/modules/orchestration.md — Resumable pipeline state machine, worker concurrency, and kill switch mechanisms.
docs/modules/appmap.md — Layer 0 AST extraction, route discovery, ORM mapping, and taint modeling.
docs/modules/discovery.md — Layer 1 static analysis engines, secret detection, SCA reachability, and custom rules.
docs/modules/correlation.md — Layer 2 candidate scoring, root-cause deduplication, and grounding algorithms.
docs/modules/confirmation.md — Layer 3 static data-flow solver and gated live DAST execution engine.
docs/modules/fix-generation.md — Layer 4 patch synthesis, proof test generation, and safety risk classifier.
docs/modules/reporting-vcs.md — Layer 5 report generation, compliance exporters, and automated PR management.
docs/modules/llm-gateway.md — Multi-provider BYO-key LLM gateway, model matrix, and real-time cost meter.
docs/modules/semantic-index.md — AST chunking, embedding generation, and pgvector-backed retrieval built alongside the App Map.
docs/modules/web-console.md — Next.js 14 operator console interface, real-time monitors, and management views.

Domain Glossary
App Map: A structured graph of an application containing detected routes, ORM models, entrypoints, and potential taint flows.
Candidate Finding: A raw, noisy issue surfaced by Layer 1 static tools prior to reachability correlation.
Probable Finding: A correlated finding with computed reachability, exposure, and impact scores mapped to an application root cause.
Confirmed Finding: A high-confidence vulnerability backed by either an interprocedural static proof or a successful live DAST exploit.
Unconfirmed Finding: A candidate demoted due to unreachability or unverified exploitability, retained in report appendixes.
Auto-Eligible Fix: A low-risk remediation (such as header hardening or dependency bump) permitted for automated pull request creation.
Human-Required Fix: A sensitive remediation (touching authentication, cryptography, or access control) requiring manual engineer review.
Kill Switch: An emergency control that aborts active pipeline scans and live HTTP DAST probing across all worker processes.

Recent Changes

[2026-08-22] B10 (integration wave — wires all six standalone blue-team capabilities plus B5, landed concurrently this same wave, into the real Layer 5 report): packages/contracts/src/report.ts gained `BlueTeamReportSchema` (additive `Report.blueTeam` field, `.default({})` so every pre-existing Report fixture built without it still parses) with six sub-sections — `mitreAttack` (B2), `detectionEngineering.{rules,coverage}` (B3/B4 + B6), `attackPaths` (B8), `threatModel.{present,summary,markdown,raw}` (B7), `hardening.{advisoryOnly:true,recommendations}` (B9), and `purpleTeam.{entries,totalScenarios,detectedCount,undetectedCount}` (B5, mirroring `summarizePurpleTeamRun`'s `PurpleTeamRunSummary` shape field-for-field so a caller passes B5's own `.entries` straight through). `packages/report/src/report-builder.ts`'s new `buildBlueTeamReport` assembles all six against the just-built core `Report`: it calls B2's `buildMitreAttackSection` directly (pure); calls B3/B4's `generateDetectionRules` per confirmed finding (pure) and feeds those SAME generated rules into B6's `buildDetectionCoverage` (also pure, from `@montr/appmap`, newly added as a `@montr/report` dependency) so a coverage verdict's `detectionRuleId` always references a rule the report also lists; calls B8's `buildAttackPaths` (`@montr/correlation`, also newly added as a dependency) when an App Map is supplied; calls B7's `buildThreatModelReport`/`renderThreatModelReportMarkdown` when the App Map carries a `threatModel`; and takes B9's hardening recommendations and B5's purple-team entries as caller-precomputed inputs on the new `BuildReportInput.appMap`/`hardeningRecommendations`/`purpleTeamEntries` fields (mirroring how `fixes`/`costRollup` are already precomputed by earlier layers — `generateHardeningRecommendations` needs a real `FileProvider` over the repo checkout, genuine I/O `buildReport` stays free of). Every section degrades to an honest empty/absent state (never a guess) when its optional input is omitted — verified by a dedicated test asserting all six stay empty/absent with only `confirmed` supplied. `packages/report/src/exports/controls.ts` gained `DETECTION_MONITORING_CONTROL_IDS`/`detectionMonitoringControls` naming the real SOC2 CC7.2/CC7.3 and ISO/IEC 27001:2022 A.8.15/A.8.16 controls this catalog already carried for `insufficient_logging`; `exports/evidence.ts`'s `evidenceRecord` now reads `Report.blueTeam.detectionEngineering` and, for ANY finding category with a VERIFIED coverage verdict (`DetectionCoverage.detected === true` — never merely "a rule exists" or the tri-state `"unknown"`), adds those controls to `EvidenceRecord.controls` as genuine evidence (deduped), plus new `detectionRuleIds`/`detectionCoverageVerified` fields (also CSV columns). `apps/web/src/mocks/data.ts`'s hand-built `Report` fixture gained an explicit empty `blueTeam` block to keep typechecking (mock predates this wave, so it honestly reports the empty state rather than fabricating content) — the only change outside `@montr/contracts`/`@montr/report`. New tests/report.blue-team.test.ts (11 tests): a realistic fixture (3 confirmed findings incl. one live-DAST SQLi, an App Map with a real SSRF->pivot attack-path chain, mixed telemetry surfaces producing all three DetectionCoverage tri-state verdicts, a real threat model, precomputed hardening + purple-team inputs) asserting every blue-team section is populated with real, finding-specific content, plus the detection-coverage-as-evidence behavior in both SOC2 and ISO27001 evidence packages. B5 had already landed by the time this task started (packages/confirm/src/purple-loop.ts, tests/confirm.purple-loop.test.ts, 469 lines) — no empty-slot fallback was needed. Full suite: 1848/1850 passing (2 pre-existing skips; one unrelated `tests/fix.generation.test.ts` timeout under full-suite parallel load, confirmed to pass in isolation, not a regression from this change). Updated docs/modules/reporting-vcs.md, docs/plan/26-08-22-tasks-ai-depth.md.

[2026-08-22] B5 (purple-team verification loop, the core purple-team differentiator): new packages/confirm/src/purple-loop.ts runs a RedTeamScenario through the EXISTING gated engine (runScenario, packages/confirm/src/scenarios.ts — the identical assertScenarioAuthorized + ScopeGuard gate every other caller uses, zero new execution path) and structurally checks whether a B3-generated Sigma DetectionRule (packages/report/src/detection-rules/sigma.ts) for the same confirmed finding would actually have fired against the resulting transcript. parseSigmaRule is a real, faithful SUBSET parser covering only the fixed shapes buildSigmaRule ever emits (cs-uri-stem|startswith / cs-method / cs-uri-query|contains / cs-body|contains selections joined by "and"/"1 of", or the file-fallback TargetFilename|contains selection), not a generic Sigma engine; evaluateSigmaRule evaluates that parsed spec against a request's actual method/URL/body (dynamic-segment-tolerant path prefix matching, percent-decoded query-marker substring matching) and returns a concrete, non-generic reason whether it fires or not. evaluateDetectionRuleAgainstScenario runs this across every transcript exchange, surfacing a genuine structural gap signal: RedTeamStep (packages/contracts/src/phase4.ts) carries no request-body field and runScenario never sends one, so a rule whose condition depends on cs-body|contains can never fire against a scenario-run transcript today — the evaluator states this explicitly rather than silently returning false. verifyScenarioDetection ties the gated run + evaluation together, finds-or-creates the finding's DetectionCoverage row, and persists the verdict via the real B1 DetectionCoverageRepository.updateVerification (packages/state-store/src/blue-team.ts, read-only consumption, no repository changes). summarizePurpleTeamRun is a pure, standalone per-scenario detected/undetected report-summary builder — NOT wired into report-builder.ts (B10's job in a later wave, same "built but unwired" precedent as B2/B3/B4/B6/B7/B8/B9). 23 new tests (tests/confirm.purple-loop.test.ts), including a real generateDetectionRules-produced Sigma rule evaluated against real scenario transcripts for both a fired:true and a fired:false case, and an explicit safety-gating regression suite proving the new code path is refused identically to every existing scenarios.ts caller (off-allowlist, production, DAST-disabled, unauthorized, kill switch — transport never invoked in any case). Updated docs/modules/confirmation.md.

[2026-08-22] B9 (blue-team hardening recommendations, architecturally separate from Layer 4 fixes): new packages/contracts/src/hardening.ts adds HardeningCategorySchema (security_headers/csp/cookie_policy/rate_limits/waf_rules/network_policy/framework_configuration) and HardeningRecommendationSchema — deliberately carrying no RiskClass, diff, or patch field of any kind, so nothing downstream can mistake a recommendation for an auto-appliable fix. A new standalone package, packages/hardening (not packages/report/src/exports/, which three other concurrent B-wave tasks were landing files in this same pass), implements the actual generation: generateHardeningRecommendations (packages/hardening/src/generate.ts) runs seven category detectors, each grounded in a real detected gap rather than a boilerplate checklist — security-headers.ts checks the App Map's detected frameworks (Express/Fastify/Next.js) against a real dependency-presence check (reusing packages/discovery's resolveInstalledPackages/collectImportedPackages, read-only) for helmet/@fastify/helmet or a next.config headers() export; csp.ts extracts an existing CSP header's value via a backreference-based regex (correctly handling embedded single-quoted CSP source keywords like 'unsafe-inline' that a naive character-class regex would truncate at) and, when none exists, grounds script-src/style-src in real inline-script (dangerouslySetInnerHTML, a literal <script>) and inline-style (style={{...}}) detection in JSX/TSX — documented honestly as blind to raw .html/.ejs/.pug templates, since packages/discovery's isTextFile deliberately excludes markup extensions; cookie-policy.ts flags a session-looking cookie (name matching session/sid/auth/token/jwt) missing Secure/HttpOnly/SameSite via its own independent regex (NOT imported from packages/fix/src/strategies.ts's mechanical-fix cookie regex, by design); rate-limits.ts recommends framework-idiomatic rate-limit config scoped to each App Map public route when no express-rate-limit/@fastify/rate-limit/rate-limiter-flexible dependency is detected; waf-rules.ts maps a confirmed finding's category to real AWS WAF managed rule group names, Cloudflare managed ruleset names, and OWASP ModSecurity CRS rule-file references (honestly hedged as "closest applicable" for categories like SSRF/XXE/open-redirect with no clean 1:1 CRS mapping); network-policy.ts reuses E16's real detectMissingNetworkPolicy/summarizeManifests (packages/discovery) to give a concrete Kubernetes NetworkPolicy YAML snippet blocking egress to the 169.254.169.254 cloud metadata IP for SSRF findings when K8s manifests exist, falling back to honestly-framed cloud security-group/IMDSv2 guidance when they don't; framework-configuration.ts covers Next.js poweredByHeader, Express's x-powered-by (skipped when helmet already covers it), and a missing trust-proxy setting when IP-based rate-limiting/logging logic is detected. Every detector is precision-tested to emit NOTHING when the gap is already closed (a real regression risk for a report full of recommendations for already-correct config), not just to emit correctly when a gap exists. packages/hardening/src/boundary.test.ts is a real architectural-boundary test (not just documentation): it asserts, by grepping this package's own source with comments stripped, that no file ever imports packages/fix and that no risk-classification vocabulary (classifyFixRisk, AUTO_ELIGIBLE_CATEGORIES, etc.) or diff/patch-shaped field appears anywhere in real code. renderHardeningRecommendationsMarkdown (packages/hardening/src/render.ts) renders the standalone Markdown section but is deliberately NOT wired into packages/report/src/report-builder.ts — that is B10's job in a later wave. docs/modules/fix-generation.md gained a boundary-clarification note (no functional change to that layer). New packages/contracts/src/hardening.test.ts (7 tests) and 48 tests across packages/hardening/src/**. Updated docs/modules/fix-generation.md, docs/architecture/folder-structure.md.

[2026-08-22] B6 (detection-coverage gap analysis, blue-team build): extended the App Map with a new optional AppMap.telemetrySurfaces field (packages/contracts/src/appmap.ts, in-memory-only, same limitation as threatModel/referencedModels) capturing the TARGET application's pre-existing observability posture — distinct from B5's later purple-team loop, which runs an actual scenario against LIVE telemetry. New packages/appmap/src/telemetry-surfaces.ts runs as another additional Layer 0 step (same convention as E6/B7), deterministic and requiring no LLM: detectRepoTelemetry matches known structured-logging (winston/pino/bunyan/log4js/loglevel/tslog for Node, structlog/logging for Python — the stdlib module is detected via a real import-statement scan since it has no manifest entry, slf4j/logback for JVM) and APM/observability (Datadog, New Relic, OpenTelemetry, Sentry, CloudWatch) package names against each stack's real dependency manifest; detectRouteTelemetry is a real ts-morph AST scan (TypeScript/JavaScript routes only) of each route handler's own body, distinguishing a structured logger call from a bare console.* call (real logging, but no structured fields) from silence — a route whose handler node cannot be resolved is omitted, never recorded as silent (mirrors Route.referencedModels's absent-vs-empty discipline). New packages/appmap/src/coverage-analysis.ts's evaluateCoverageForFinding derives the tri-state DetectionCoverage.detected verdict per confirmed finding (via routeForFinding's file+line proximity match, that route's telemetry, and any existing DetectionRule from StateStore.detectionRules) with a real, specific reasoning string per verdict — "unknown" is earned only for genuinely ambiguous cases (console-only logging; an unanalyzed/unresolved route), never a blanket default. persistDetectionCoverageForScan calls the real StateStore.detectionCoverage/detectionRules repositories (B1) end to end but is deliberately NOT wired into apps/worker/src/runners.ts's Layer 3 runner — a live, concurrently-edited integration point this same wave (B3/B5 both need it too); production wiring is a documented follow-up, mirroring E5/E9/A31's "built, tested, real repository calls, no production caller yet" precedent. New packages/fixtures/sample-repos/telemetry-samples/mixed-logging fixture repo (real winston + @sentry/node dependencies, one structured-logging route, one console-only route, one silent route) plus tests/appmap.telemetry-surfaces.test.ts (16 tests: real fixture-repo dependency/APM detection, real per-route AST detection distinguishing all three signals, all three coverage tri-state verdicts each earned from a distinct real scenario, and a real repository-contract integration test for persistDetectionCoverageForScan). Updated docs/modules/appmap.md.

[2026-08-22] B7 (threat-model report, the E6 threat-model's REVIEWABLE DELIVERABLE): extended E6's `ThreatModel` shape additively with real STRIDE classification (packages/contracts/src/threat-model.ts): a new `StrideCategorySchema` enum, an exhaustive `CATEGORY_TO_STRIDE: Record<Category, readonly StrideCategory[]>` table (mirroring B2's mitre.ts / controls.ts's `Record<Category, ...>` pattern), and `strideForCategory(category, plausibility)` which suppresses to `[]` whenever `plausibility` is `"none"` — the same precision discipline `zeroSurfaceCategories` already applies, so a category the App Map's own shape rules out never gets a spurious STRIDE tag. `AttackSurfaceEntry.stride` and `TrustBoundary.stride` are both new additive-optional (`.default([])`) fields — zero breakage for any existing E6 consumer. packages/appmap/src/threat-model.ts (E6) gained the actual per-boundary classification: `buildTrustBoundaries`'s new `strideForTrustBoundary` grounds each boundary's STRIDE tags in that boundary's OWN routes — `public`/`unknown`-auth boundaries always get Spoofing; they additionally get Tampering + Elevation of Privilege + Repudiation only when their routes actually write/delete an ORM model (reusing A18's route->model evidence, the same signal `weakAuthModelFanOut` already uses for `idor`/`broken_access_control`) and Information Disclosure only when their routes actually read one; a resolved-authentication boundary with no such fan-out gets `stride: []`. Denial of Service is deliberately never derived (no rate-limit/throughput signal exists in the App Map at Layer 0 — documented as a judgment call, not a silently-papered-over gap). New packages/report/src/exports/threat-model.ts is the standalone report-rendering layer (mirroring mitre-attack.ts's B2 precedent): `buildThreatModelReport` produces a plain-data model (trust boundaries, a STRIDE roll-up table, the plausible attack-surface categories sorted high-first, abuse cases, and a grounded plain-language summary for the security-lead "buyer" persona), rendered via `renderThreatModelReportJson` and `renderThreatModelReportMarkdown`; `scopeHints` is deliberately dropped from the rendered artifact since it is E6's internal prioritization signal, not reader-facing evidence. Exported from packages/report's index but, like mitre-attack.ts, deliberately NOT wired into exports/index.ts's format registry or report-builder.ts — B10 wires it in alongside the rest of the blue-team report sections. New packages/appmap/src/threat-model.test.ts (7 tests) and packages/report/src/exports/threat-model.test.ts (8 tests), covering both the precision case (a properly-authenticated, no-fan-out boundary gets zero spurious STRIDE tags) and the positive case (a public write-capable route gets Tampering + Elevation of Privilege + Repudiation, grounded in the real route/model names). Updated docs/modules/appmap.md, docs/modules/reporting-vcs.md.

[2026-08-22] B2 (MITRE ATT&CK mapping, second step of the blue-team build): new packages/contracts/src/mitre.ts adds an exhaustive `CATEGORY_MITRE_TECHNIQUES: Record<Category, readonly string[]>` (mirroring packages/report/src/exports/controls.ts's SOC2/ISO27001 `Record<Category, ...>` pattern — a closed union so an uncovered category is a compile error), mapping every finding category to real MITRE technique ids: mostly Enterprise ATT&CK (e.g. sql_injection -> T1190 Exploit Public-Facing Application + T1213 Data from Information Repositories; hardcoded_secret -> T1552.001 Credentials In Files; ssrf -> T1190 + T1552.005 Cloud Instance Metadata API; xss -> T1059.007 JavaScript + T1539 Steal Web Session Cookie; insecure_configuration -> T1190 + T1611 Escape to Host), with prompt_injection mapped to MITRE ATLAS's AML.T0051 (LLM Prompt Injection) since Enterprise ATT&CK has no dedicated technique for it — never a fabricated id. `MITRE_TECHNIQUE_CATALOG` carries name/tactic/url per id; `mitreTechniqueIdsForCategory` (raw ids, the shape `DetectionRule.mitreTechniques` from B1 expects) and `mitreTechniquesForCategory` (full descriptors) are the two accessors a later wave's DetectionRule-construction code (B3+) calls. Wired into two consumers this pass: packages/report/src/exports/sarif.ts's SARIF export now tags both `driver.rules[].properties.tags`/`.mitre` and `results[].properties.mitre` with the mapped technique ids (`external/attack/<lowercased-id>`, mirroring the existing `external/cwe/<id>` tag convention); and a new standalone packages/report/src/exports/mitre-attack.ts (`buildMitreFindingMappings`/`buildMitreAttackSection`/`renderMitreAttackJson`) surfaces a per-finding technique mapping plus a technique -> finding coverage index, exported from packages/report's index but deliberately NOT wired into the exports/index.ts format registry or report-builder.ts — that final wiring is a later wave's job, alongside the rest of the blue-team capabilities landing this wave. New packages/contracts/src/mitre.test.ts (11 tests, including a completeness assertion over every CategorySchema member and a catalog-coverage assertion over every referenced id) plus MITRE-specific cases added to tests/report.exports.test.ts and a new tests/report.mitre-attack.test.ts (5 tests). Updated docs/modules/reporting-vcs.md.

[2026-08-22] B1 (foundational first step of the blue-team build): extended packages/contracts/src/ with the blue-team entities the rest of the wave builds on — new packages/contracts/src/blue-team.ts defines DetectionRule (a generated Sigma/OTel/SIEM detection rule for a confirmed finding; format is an extensible enum since B3 will generate multiple formats per finding; mitreTechniques: string[] of ATT&CK ids for B2's mapping; provenance reuses ProofTypeSchema to mirror ConfirmedFinding's static/live distinction), AttackPath (an ordered, >= 2-hop chain of finding references across confirmed findings, with a feasibilityScore, a severity, and a human-readable narrative), and DetectionCoverage (a tri-state detected: boolean | "unknown" telemetry-coverage verdict per finding, an optional detectionRuleId, and an optional verification slot B5's purple-team loop will populate). ThreatModelSchema (E6) was reused as-is, not redefined. Also closed E6's own documented persistence gap: AppMap.threatModel is now a genuinely persisted nullable Json column (packages/state-store/prisma/schema.prisma), not dropped on write — packages/state-store/src/mappers.ts's appMapScalarsToCreate/appMapFromRows now round-trip it. New migration 9_blue_team_entities adds DetectionRule/AttackPath/DetectionCoverage tables (all clientId + scanId row-scoped, real Client/Scan FKs, onDelete: Cascade from Scan) plus the AppMap.threatModel column; detected is a real three-value DetectionStatus Prisma enum, not a nullable Boolean. New packages/state-store/src/blue-team.ts provides DetectionRuleRepositoryImpl/AttackPathRepositoryImpl/DetectionCoverageRepositoryImpl (CRUD + listByFinding + updateVerification for the purple-team result), wired into StateStore as detectionRules/attackPaths/detectionCoverage. Judgment call: none of DetectionRule.content/AttackPath.narrative/DetectionCoverage.reasoning are field-encrypted — mirrors CustomRule.body's precedent (an explicitly-unencrypted detection-rule body) and the fact that ConfirmedFinding.proofArtifact, the most sensitive exploit-detail field in the schema, is already plaintext Json; see docs/api/database.md for the full reasoning. Data model + persistence only — B2 (ATT&CK mapping), B3 (rule authoring), B4 (attack-path graph algorithm), and B5 (purple-team verification loop) build the generation logic on top in a later wave. New packages/contracts/src/blue-team.test.ts (28 tests) and packages/state-store/src/blue-team.test.ts (16 tests, including explicit cross-tenant row-scoping coverage for all three repos). Updated docs/api/database.md, docs/modules/reporting-vcs.md.

[2026-08-22] Added detection-rule generation and its human-readable log-signature narrative per confirmed finding (build-plan items B3, B4): new packages/report/src/detection-rules (context.ts's buildRuleContext is the shared input every renderer reads from — for a live-DAST-proven finding it recovers the ACTUAL exploit signature by diffing the transcript's last exchange, packages/confirm/src/live.ts's most-refined probe, against its first/baseline exchange, no per-category guessing; for a static-proof-only finding it falls back to a small table of well-known category payload markers plus the route resolved from the App Map by file+line match, mirroring static.ts's own findRoute fallback since ConfirmedFinding carries no routeId; with no resolvable route at all — a config/component-class category — it degrades further to a file_event-category rule keyed on the finding's location.file) renders that ONE context into three format-equivalent outputs: sigma.ts (real Sigma YAML — logsource/detection/condition/level/tags, webserver or file_event category), otel.ts (an OTTL boolean condition, the OpenTelemetry Collector filter/transform processor's real expression language, over semconv attribute names), and siem.ts (Splunk SPL). narrative.ts builds the B4 DetectionLogSignature — concrete fields, the exact log pattern, and an exhaustive Record<Category, ...>-enforced, finding-specific false-alarm-sources sentence (e.g. an admin bulk-export tool as the SQLi rule's noise source) rather than generic boilerplate. mitreTechniques defaults to B2's real mitreTechniqueIdsForCategory (packages/contracts/src/mitre.ts, landed earlier this same wave) rather than []. Contracts gained DetectionRuleSchema.logSignature (packages/contracts/src/blue-team.ts, additive/optional) persisted via a new nullable DetectionRule.logSignature Json column (migration 10_detection_rule_log_signature) and packages/state-store/src/blue-team.ts's existing repository mapper. generateDetectionRules is pure (no I/O); persistDetectionRules is the thin StateStore.detectionRules.create wrapper (B1's repository), row-scoped by clientId. STANDALONE and NOT wired into report-builder.ts (A10's "built but unwired, honestly documented" precedent) — B10 wires a detection-engineering report section in a later wave. 15 new tests (packages/report/src/detection-rules/generate.test.ts). Updated docs/api/database.md, docs/modules/reporting-vcs.md.

[2026-08-22] Added the attack-path graph, chaining ConfirmedFinding[] into kill chains (build-plan item B8): new packages/correlation/src/attack-paths implements three concrete, checkable chain conditions (packages/correlation/src/attack-paths/conditions.ts) — an RCE-class finding (command injection or insecure deserialization) chains to every other confirmed finding on the scan ("game over", force-bumping the chain's severity to critical); an IDOR/broken-access-control finding whose route statically reads (A18 fan-out) an ORM model with a credential-shaped field (packages/correlation/src/attack-paths/credentials.ts) chains to a different non-public route; an SSRF finding chains to a different non-public route as the closest structural proxy this schema has for "internal network space", strengthened when a real http_client taint sink corroborates it. Findings resolve to their Route via a local file+nearest-line match (packages/correlation/src/attack-paths/route-match.ts, mirroring the same convention already duplicated across packages/confirm/src/{static,live,investigation-pipeline}.ts, since ConfirmedFinding carries no routeId of its own). packages/correlation/src/attack-paths/graph.ts's DFS enumerates only MAXIMAL simple paths (a long chain's own shorter prefixes are never separately emitted) and ranks by packages/correlation/src/attack-paths/feasibility.ts's feasibility score — the product of each hop's confirmation confidence (live-DAST-proven outranks static-proof-only) and each connecting condition's structural strength. packages/correlation/src/attack-paths/persist.ts's discoverAndPersistAttackPaths writes the ranked result via StateStore.attackPaths (B1). Landed in packages/correlation (not packages/report) since its core dependency is the same App Map route/model grounding packages/correlation already owns; not yet wired into correlate.ts or packages/report's report assembly (B10, a later wave). 53 new tests (packages/correlation/src/attack-paths/*.test.ts). Updated docs/modules/correlation.md.

[2026-08-22] Broadened Layer 1 target coverage with real, tested IaC scanning and a real CycloneDX SBOM export, plus a start on supply-chain risk (enhancement E16, first three of its six sub-areas): new packages/discovery/src/detectors/iac.ts (detectIac) adds Dockerfile/Kubernetes/Terraform structural detectors (packages/discovery/src/detectors/iac/{dockerfile,kubernetes,terraform}.ts) as a fourth concurrent Layer 1 fan-out member alongside SAST/secrets/SCA/AI-security, plus an optional Semgrep pass against the real p/dockerfile/p/kubernetes/p/terraform registry packs (reusing sast.ts's parseSemgrepJson/candidateFromSemgrep unmodified) that gracefully degrades rather than hard-failing like detectSast — see docs/modules/discovery.md for the full detector list and its Terraform block-scoping / Dockerfile line-continuation scope notes. A new insecure_configuration Category (packages/contracts/src/compliance.ts, CWE-16/A05:2021, migration 8_insecure_configuration_category mirroring migration 7's prompt_injection precedent exactly) covers pure IaC misconfiguration; hardcoded_secret/broken_access_control/weak_crypto are reused directly for the checks that already fit them, keeping the taxonomy ripple to the same six files the prior prompt_injection addition touched. New packages/discovery/src/sbom.ts's buildDependencyInventory resolves the FULL dependency tree (not just SCA's vulnerable subset) with real call-site reachability (A12) as plain data, consumed with zero adapter by new packages/report/src/exports/cyclonedx.ts's buildCycloneDxSbom/renderCycloneDxSbom (new "cyclonedx" ExportFormat, real PURLs + numeric CWEs + affects[] refs) — proven end to end in tests/sbom.cyclonedx-e2e.test.ts; not yet wired to a production route/CLI (documented, same posture as E5/E9's unwired-but-tested library shipping). New packages/discovery/src/detectors/supply-chain.ts adds real offline Levenshtein-distance typosquat detection against a curated popular-package list, install-script risk scanning (the project's own package.json always, a resolved dependency's node_modules/<pkg>/package.json only when node_modules is actually present on disk — a true absence for a plain git checkout, not a swallowed error, since node_modules stays excluded from the general file walk by design), and malicious-package heuristics scoped honestly to what's checkable OFFLINE (non-ASCII/homoglyph package names, non-standard package-lock.json registry resolution) — "recently published + low downloads" is explicit documented future work since it needs live npm registry metadata this on-prem product does not fetch. OpenAPI/API-spec drift review, container image scanning, and cloud posture (E16's remaining three sub-areas) were not attempted — deferred per the audit's own stated priority order, honestly left unchecked in docs/plan/26-08-22-tasks-ai-depth.md. New fixtures under packages/fixtures/sample-repos/{iac-samples,supply-chain-samples}/{vulnerable,clean}. Updated docs/modules/discovery.md.

Update Triggers
Update this file when top-level architectural patterns change, when new packages or apps are introduced, when global cross-cutting policies evolve, or when files are added or removed from the docs directory.

Related Docs
docs/maintenance.md — Maintenance rules and decision tree for updating documentation.
docs/architecture/folder-structure.md — Detailed folder mapping of all packages and applications.
