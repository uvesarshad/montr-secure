# Montr Secure System Overview

Scope: High-level architecture, mental model, directory index, and domain glossary for Montr Secure.
Rendering context: Isomorphic
Project tier: 4
Last updated: auto

Overview
Montr Secure is an on-premises, bring-your-own-LLM-key security orchestration platform that unifies static analysis, software composition analysis, secret detection, and dynamic exploit validation into an automated, six-layer analysis pipeline. The platform correlates raw multi-engine findings against structural application models, confirms genuine exploitability through static data-flow proofs and gated live DAST, synthesizes validated patches with proof-of-fix tests, and outputs compliance-ready reports with gated pull request automation. The codebase is organized as a Turborepo monorepo comprising a Fastify REST API, a Next.js operator console, a BullMQ pipeline worker, a montr CLI for local/CI-triggered scans, and sixteen decoupled TypeScript packages.

System Mental Model
The system operates as an end-to-end security pipeline structured across six distinct processing layers orchestrated by a resumable finite state machine.
Layer 0 intake and scoping builds a comprehensive structural application map of routes, data models, and taint surfaces while calculating token and cost estimates.
Layer 1 discovery executes multi-engine static analyzers, secret detectors, and dependency reachability scanners concurrently to capture raw candidate findings.
Layer 2 correlation scores candidates across reachability, exposure, and business impact to merge duplicate root causes into ranked probable findings without discarding evidence.
Layer 3 exploit confirmation proves vulnerability exploitability via deterministic interprocedural taint tracing and approver-gated live dynamic security testing against staging environments.
Layer 4 fix generation produces unified diff patches, proof-of-fix regression tests, and safety classifications that partition fixes into auto-eligible or human-required.
Layer 5 gate and reporting generates executive summaries, compliance exports across SARIF, SOC2, ISO27001, and OWASP, and opens automated pull requests for eligible remediations.

Key Architectural Decisions
Strict perimeter containment ensures that source code never leaves the client infrastructure except when transmitted directly to the client configured LLM provider endpoint.
Row-scoped multitenancy guarantees complete isolation across clients in Postgres, with high-security fields encrypted at rest using AES-256-GCM.
Resumable state machines allow long-running analysis workflows to pause at estimate and fix gates, survive worker crashes, and resume from checkpoints without re-running completed layers.
Budget hard-halt policies continuously meter token expenditure against user ceilings, stopping pipeline execution and producing partial reports before budget overruns occur.
Append-only hash-chained audit logging cryptographically seals all system actions, gate approvals, and operator decisions to provide tamper-evident compliance trails.

Cross-Cutting Concerns
Authentication and access control employ Argon2 password hashing, JSON Web Tokens, CSRF tokens, and role-based permissions dividing capabilities across Operator, Approver, and Viewer roles.
Error handling relies on custom domain error classes defined in packages/contracts/src/errors.ts mapped to standardized HTTP status codes and structured log envelopes.
Telemetry and logging utilize a content-aware scrubber in packages/telemetry/src/scrubber.ts to strip credentials, tokens, and raw code snippets before writing to stdout or OpenTelemetry collectors.
Styling across the operator web console uses Tailwind CSS v4 design tokens with a dark-first color scheme and semantic severity badges.

Documentation Directory Map
docs/overview.md — Master entry point, architecture summary, directory map, and glossary.
docs/maintenance.md — Step-by-step update guide and decision tree for maintaining documentation after code changes.
docs/architecture/folder-structure.md — Structural layout of packages, apps, tests, and configuration files.
docs/architecture/rendering-strategy.md — Rendering architecture across the Next.js console and Fastify backend.
docs/architecture/data-flow.md — Complete lifecycle of data, pipeline transitions, and serialization boundaries.
docs/ui/component-library.md — Reusable UI primitives and domain widgets in the operator web console.
docs/ui/layout-system.md — Root and nested layout hierarchies, route wrappers, and navigation chrome.
docs/ui/theming.md — Color system, dark theme tokens, severity scales, and typography definitions.
docs/api/route-handlers.md — Fastify HTTP route handlers, path parameters, role requirements, and response contracts.
docs/api/external-services.md — Third-party integrations including LLM providers, VCS platforms, and vulnerability databases.
docs/api/database.md — Prisma relational schema, multitenancy design, encrypted fields, and audit log chaining.
docs/state/client-state.md — TanStack React Query cache management, role context, and local component state.
docs/state/server-state.md — Postgres persistence, Redis BullMQ queues, resume checkpoints, and cache policies.
docs/auth/auth-flow.md — Session lifecycle, token issuance, password hashing, and authentication guards.
docs/auth/authorization.md — Role-based permission matrix, gate approval constraints, and DAST authorization rules.
docs/infra/environment.md — Complete catalog of environment variables, defaults, and exposure levels.
docs/infra/deployment.md — Docker Compose configurations, Kubernetes Helm charts, and air-gapped installation procedures.
docs/infra/testing.md — Vitest test suites, coverage floors, golden corpus evaluation, and smoke tests.
docs/modules/orchestration.md — Resumable pipeline state machine, worker concurrency, and kill switch mechanisms.
docs/modules/appmap.md — Layer 0 AST extraction, route discovery, ORM mapping, and taint modeling.
docs/modules/discovery.md — Layer 1 static analysis engines, secret detection, SCA reachability, and custom rules.
docs/modules/correlation.md — Layer 2 candidate scoring, root-cause deduplication, and grounding algorithms.
docs/modules/confirmation.md — Layer 3 static data-flow solver and gated live DAST execution engine.
docs/modules/fix-generation.md — Layer 4 patch synthesis, proof test generation, and safety risk classifier.
docs/modules/reporting-vcs.md — Layer 5 report generation, compliance exporters, and automated PR management.
docs/modules/llm-gateway.md — Multi-provider BYO-key LLM gateway, model matrix, and real-time cost meter.
docs/modules/web-console.md — Next.js 14 operator console interface, real-time monitors, and management views.

Domain Glossary
App Map: A structured graph of an application containing detected routes, ORM models, entrypoints, and potential taint flows.
Candidate Finding: A raw, noisy issue surfaced by Layer 1 static tools prior to reachability correlation.
Probable Finding: A correlated finding with computed reachability, exposure, and impact scores mapped to an application root cause.
Confirmed Finding: A high-confidence vulnerability backed by either an interprocedural static proof or a successful live DAST exploit.
Unconfirmed Finding: A candidate demoted due to unreachability or unverified exploitability, retained in report appendixes.
Auto-Eligible Fix: A low-risk remediation (such as header hardening or dependency bump) permitted for automated pull request creation.
Human-Required Fix: A sensitive remediation (touching authentication, cryptography, or access control) requiring manual engineer review.
Kill Switch: An emergency control that aborts active pipeline scans and live HTTP DAST probing across all worker processes.

Recent Changes
[2026-08-22] Auth-boundary detection and interprocedural call graphs, across all three language analyzers (audit findings A20, A21): TypeScript route auth (packages/appmap/src/languages/typescript/routes.ts) now verifies an export's HOC-wrap (withAuth(handler), and the factory form requireRole(...)(handler)) as a real AST call shape rather than identifier-text presence, and resolves a root/src/ middleware.ts's config.matcher as Next's own global-middleware analog to Express/Fastify app.use(authMiddleware); Python (python/routes.ts) resolves Django 5.1+'s global LoginRequiredMiddleware; Java (java/extract.ts's new applySpringSecurityAuth, wired from java/index.ts) resolves Spring Security's declarative authorizeHttpRequests filter-chain lambda into Ant-pattern route matches — none of these override an existing more-specific per-route signal. Separately, Python and JVM each gained a real bounded interprocedural call graph (new packages/appmap/src/languages/{python,java}/callgraph.ts, mirroring typescript/callgraph.ts's exact output shape and bounded-hop philosophy) populating AppMap.taintFlows, previously always empty for both languages; Python reaches full parity with TS (same-file + one-hop relative-import resolution), Java reaches same-class + same-file cross-class resolution only (cross-file/same-package resolution was deliberately not attempted — see appmap.md). packages/confirm/src/static.ts's findSink/findSource gained a same-file-first, taintFlows-fallback lookup so Layer 3 static confirmation actually consumes this interprocedural data (previously taintFlows was read only by Layer 2's grounding.ts, even for TypeScript). Updated docs/modules/appmap.md, docs/modules/confirmation.md.
[2026-08-22] Correlation's same-file sanitizer heuristic and impact-score priors were hardened against the golden corpus (audit findings A25, A26): packages/correlation/src/grounding.ts's single loose keyword regex (matched against raw candidate evidence text in both directions) was replaced with two precise tiers — a call-expression-anchored check naming real sanitizer/validator/encoding APIs (DOMPurify, validator.js, zod, parseInt/Number/encodeURIComponent, etc.), checked against source/sink descriptions and the candidate's evidence snippet (real matched source code for every injection-class detector), plus a curated safe-marker vocabulary some App Map extractors deliberately author into a description, now scoped to source/sink descriptions only, never the uncontrolled evidence snippet, closing a concrete inversion bug (a bare "bind" or "no sanitization applied" substring could previously flip the verdict either way); packages/correlation/src/taxonomy.ts's CATEGORY_IMPACT_BASE.vulnerable_dependency was manually recalibrated from 0.5 to 0.7 after the golden corpus's real critical CVE-2021-44228 (Log4Shell) finding was found scoring below several high-severity findings of other categories purely due to the low prior — the golden corpus (n=44 labelled findings) is too thin for a real statistical fit, so this was a documented manual correction, not a regression; new tests/correlation.scoring-calibration.test.ts locks in the corrected cross-category severity ranking as a permanent regression guard. Updated docs/modules/correlation.md.
[2026-08-22] The strong content-aware scrubber is now a runtime gate on the audit-write path, not just a test-time certifier (audit finding A24): packages/state-store/src/audit.ts's PrismaAuditLogClient.append — the single real persistence chokepoint every caller across apps/api and apps/worker funnels through — now unconditionally runs both free-form text fields, metadata AND summary (summary was previously entirely unscrubbed), through @montr/security's redactSensitive before hashing/persisting, then re-verifies the redacted output with that package's independent findLogViolations as defense in depth; a residual violation after redaction triggers a hard anomaly-marker fallback rather than throwing, so a safety-relevant write (e.g. a kill-switch event) is redacted-and-persisted, never dropped (fail-safe, not fail-closed — chosen because the audit trail's job is to be trustworthy AND complete). packages/contracts/src/audit.ts's AuditEventSchema doc comment now points at this enforcement point instead of asserting scrubbing only via a comment; apps/api/src/audit.ts's recordAudit pre-scrub (via @montr/telemetry's scrubValue) is left in place as a harmless, non-authoritative defense-in-depth layer. @montr/telemetry/src/scrubber.ts (the separate, already-wired hot-path stdout/OTel scrubber) is untouched — the two stay intentionally independent per that module's own doc comment. New packages/state-store/src/audit.test.ts proves the gate at the actual persistence boundary (not just the scrubber functions in isolation). Updated docs/api/database.md.
[2026-08-22] App Map route discovery now covers Express and Fastify, not just Next.js: new packages/appmap/src/languages/typescript/express.ts and fastify.ts add real ts-morph-based route extractors (previously both frameworks were detected from package.json but produced zero routes), wired into languages/typescript/index.ts's framework-gated dispatch and verified against this repo's own apps/api Fastify routes (audit finding A17 — the narrow route-extraction gap only; the broader A17 breadth is tracked as enhancement E16). Also added route-to-model linking: Route gained an optional referencedModels field (packages/contracts/src/appmap.ts) populated by new packages/appmap/src/languages/typescript/route-models.ts, which resolves a direct prisma.<model>.<method>(...) call in a route handler plus one hop through a locally declared helper function (audit finding A18); not yet persisted through packages/state-store (documented limitation, not in this change's scope). Updated docs/modules/appmap.md.
[2026-08-22] Closed the "built but unwired" pattern across five real subsystems that had zero production callers (audit finding A10): apps/worker/src/main.ts and apps/api/src/production-deps.ts now resolve the field-encryption key via packages/config/src/key-source.ts's resolveFieldEncryptionKey (env/file/Vault) instead of reading security.fieldEncryptionKeyRef directly, so security.keySource=vault actually fetches from HashiCorp Vault in production; all five real LLM prompt call sites (packages/appmap/src/llm.ts, packages/discovery/src/triage.ts, packages/correlation/src/correlate.ts, packages/confirm/src/static.ts's runLlmReview, packages/fix/src/generate.ts) now resolve their system prompt through gateway.resolvePrompt(name, hardcodedFallback, {clientId}) instead of a hardcoded constant, so a DB-versioned PromptVersion actually overrides it when active (packages/contracts/src/llm.ts's LLMGateway interface gained an optional resolvePrompt method so callers typed only against the base contract compile); the §15 false-positive feedback loop is now wired — new packages/state-store/src/repositories.ts FalsePositiveMarkRepositoryImpl reads prior finding.marked_false_positive audit events back out per client, and apps/worker/src/runners.ts's new loadFpTuning() passes them as fpTuning into both correlate() and confirmFindings(), so a repeat of the same (category, file, line) on a later scan is demoted/skipped instead of re-promoted; gateway streaming was assessed and deliberately left unconsumed by any layer (every current call is a small single-shot metadata request with no streaming UI) but is now proven working end-to-end via a new real-adapter integration test (packages/llm-gateway/src/streaming.integration.test.ts); new scripts/check-unwired-seams.mjs (pnpm run check:unwired-seams, wired into CI) grep-asserts each of these seams keeps a real non-test caller outside its defining package, so this class of regression fails the build going forward. Updated docs/modules/llm-gateway.md, docs/infra/environment.md, docs/state/server-state.md, docs/infra/testing.md.
[2026-08-22] Fix generation no longer asks the model to rewrite whole files (audit finding A14): packages/fix/src/generate.ts's prompt now asks for a small, line-anchored edit list of 1-based startLine/endLine/replacement entries (new packages/fix/src/edits.ts parses, validates, and applies it) instead of the full fixedSource file body, which silently truncated and failed JSON parsing on any file past roughly 1,500 lines with no error, metric, or retry; the output cap rose from 2048 to 8192 tokens as defense in depth alongside the format change; an unparseable model response or a structurally invalid edit list is now counted via getMetrics().recordError (codes fix_generation.llm_response_unparseable and fix_generation.llm_edits_invalid) and logged with the model's stop reason, instead of a silent null-and-degrade to the mechanical strategy or advisory path. Updated docs/modules/fix-generation.md.
[2026-08-22] Live DAST now covers eleven categories instead of four, and the NoSQL probe fires a real Mongo operator payload instead of a reused SQL string (audit finding A9): packages/confirm/src/live.ts's LIVE_CONFIRMABLE_CATEGORIES gained ssrf, idor, broken_access_control, path_traversal, command_injection, xxe, and insecure_deserialization, each with a baseline+payload probe pair and a fixed marker/status oracle; nosql_injection now sends a field[$ne]= bracket-notation operator payload; xxe/insecure_deserialization are POST-based (the Probe/LiveHttpRequest body was already plumbed through undici but never used) and are gated by the existing mutating-request blast-radius cap, which defaults to zero. Verified (audit finding A7) that a successful live-DAST probe already promotes a finding to confirmed:true with the transcript attached as its ProofArtifact — the demote-only clamp the audit found is specific to the static-confirmation LLM veto in packages/confirm/src/static.ts and does not apply to the deterministic live-DAST oracle; idor and broken_access_control have no static data-flow proof at all, so live DAST is their only route to confirmed. Updated docs/modules/confirmation.md.
[2026-08-22] SCA reachability is now call-granularity on TypeScript/JavaScript instead of package-import presence (audit finding A12): packages/discovery/src/detectors/sca.ts's new collectCalledPackages runs a real ts-morph AST scan to check whether a binding imported from a vulnerable package is actually invoked (called, constructed, or JSX-rendered) anywhere, not merely present in an import/require statement; the old collectImportedPackages regex scan remains only as a fallback when nothing parses as TS/JS. This does not reuse packages/appmap/src/languages/typescript/callgraph.ts's taint-flow resolver (scanTaintFlows), which explicitly excludes bare/package specifiers by design and so cannot answer this question; no new Layer 0→Layer 1 data dependency was introduced (Layer 1 already receives the App Map, unused by this feature). Scoped to TS/JS only — Python/Java are not yet resolved by this detector at all; updated docs/modules/discovery.md.
[2026-08-22] Recall is now a first-class, published metric alongside precision/FP-rate on the golden-corpus gate (audit finding A6): packages/qa/src/report.ts's formatCorpusScore/toJsonReport always render recall next to fpRate/precision and print an explicit sampleSizeCaveat (new export, floor 30 confirmed findings) while the confirmed sample stays small — today's real measurement is TP=11, so the caveat fires on every qa:corpus run; the aggregate recallMin gate already existed in packages/qa/src/baseline.ts evaluateBaseline and corpus/baseline.json (recallMin: 0.2 plus four perCategory floors), now proven by dedicated tests in tests/qa.baseline.test.ts and tests/qa.report.test.ts; DOD.md item 4 and corpus/baseline.json's new $statisticalCaveat state plainly that 0% FP-rate at n=11 is not yet statistically meaningful; updated docs/infra/testing.md.
[2026-08-22] Real-time cost metering now actually accumulates in production: packages/llm-gateway/src/gateway.ts's account() records every completed call's usage into the per-scan CostMeter resolved from budgetRegistry (the same instance orchestrator/controller.ts registers, hands to layers as ctx.costMeter, and reads in enforceBudget) instead of only the unused constructor-level costMeter option apps/worker/src/main.ts never set (audit finding A32, follow-on to A2); updated docs/modules/llm-gateway.md, docs/modules/orchestration.md.
[2026-08-22] Scan resumability is now reachable in production: POST /api/v1/scans/:id/resume route, apps/worker boot-time reconciliation (reconcile.ts) for scans stuck at status running, BullMQ Worker lock/stall tuning, and FindingRepo.bulkCreate skipDuplicates fix for redelivered layer jobs (A3); also closed four confirmed-404 web routes — GET /scans/:id/progress, GET /scans/:id/appmap, cross-scan GET /pull-requests, and a scan-scoped POST /scans/:id/dast/authorize wrapper around the real target-based DAST flow (A5); updated docs/api/route-handlers.md, docs/modules/orchestration.md, docs/state/server-state.md, docs/auth/authorization.md.
[2026-08-22] LLM gateway now enforces a pre-call budget guard (estimated cost checked against the live CostMeter/BudgetPolicy before dispatch, refusing a single over-budget call mid-layer instead of only between layers, audit finding A2); new packages/cost-meter/src/registry.ts BudgetRegistry wired through apps/worker/src/main.ts; updated docs/modules/llm-gateway.md, docs/modules/orchestration.md.
[2026-08-22] SAST is now a required detector and air-gapped installs can wire it to a local Semgrep ruleset directory (discovery.rulesetsDir / MONTR_DISCOVERY_RULESETS_DIR, audit finding A4); updated docs/modules/discovery.md, docs/infra/environment.md, docs/infra/deployment.md.
[2026-08-22] Cost meter fails closed on unknown model ids instead of pricing at $0 (A1); rate card in packages/contracts/src/llm.ts gained claude-opus-5 and claude-fable-5; docs/modules/llm-gateway.md updated.
[2026-08-22] Model matrix refreshed and the gateway now supports a fallback-model cascade (audit finding A11): packages/contracts/src/llm.ts's RECOMMENDED_MODEL_MATRIX.confirmation is now claude-opus-5 (current flagship; was claude-opus-4-8, which stays valid/priced) and .triage is now the undated claude-haiku-4-5 (was claude-haiku-4-5-20251001 — MODEL_COST_RATES intentionally keeps that dated entry, since normalizeModelId's -YYYYMMDD stripping already prices either form identically); new config.llm.fallbackModel (packages/config/src/schema.ts, env MONTR_LLM_FALLBACK_MODEL) lets packages/llm-gateway/src/gateway.ts's complete() retry the same request against one alternate model — exactly once, bounded, never infinite — via retry.ts's new withRetryAndFallback, only after the primary model's full retry policy is exhausted; each fallback attempt logs llm.model_fallback and increments the llm_gateway.model_fallback error metric. Updated docs/modules/llm-gateway.md, docs/infra/environment.md.
[2026-08-22] Built the in-repo-buildable half of the autonomous trigger surface (audit finding A15): new apps/cli (@montr/cli, bin montr) drives the real apps/api HTTP API to create/poll a full- or diff-mode scan from a local checkout and exits non-zero on a confirmed finding at/above a configurable --fail-on severity, for CI gating; new POST /api/v1/webhooks/scan-trigger (apps/api/src/routes/webhooks.ts) verifies a GitHub-compatible X-Hub-Signature-256 HMAC (apps/api/src/auth/webhook-signature.ts) over the raw body and then creates+starts a scan through the same orchestrator path POST /scans uses, disabled by default (503) until MONTR_WEBHOOK_SECRET and MONTR_WEBHOOK_OPERATOR_EMAIL are set; packages/report/src/vcs.ts gained postGitHubComment (additive, reuses the existing lazy @octokit/rest import) so the webhook flow can best-effort post a single PR acknowledgement comment. A full GitHub App (manifest registration, OAuth installation, marketplace listing) remains explicitly out of scope; a webhook-triggered scan posting its CONFIRMED findings back to the PR once complete is deferred (needs apps/worker's Layer 5 to persist/read the originating PR reference). Updated docs/architecture/folder-structure.md, docs/api/route-handlers.md, docs/api/external-services.md, docs/infra/environment.md, CLAUDE.md, AGENTS.md.

[2026-08-22] LLM gateway plumbing for tool use, extended thinking, prompt caching, and real structured outputs (audit findings A8, A13; scoped gateway plumbing, not the agentic loop itself — that is tracked as E1): all four adapters (packages/llm-gateway/src/adapters/{anthropic,bedrock,vertex,azure}.ts) now forward request.tools and parse a tool_use/functionCall/tool_calls response into the new additive LLMResponse.toolCalls; request.effort (new EffortSchema) drives Anthropic/Bedrock's output_config.effort + adaptive thinking (Haiku-tier models excluded — they 400 on it); Anthropic/Bedrock's system prompt now carries cache_control:{type:"ephemeral"}; new packages/llm-gateway/src/structured-output.ts resolves a real per-purpose JSON schema (derived from each of the five real call sites' own already-existing parsed-JSON shapes, with zero changes to those five packages) so Anthropic/Bedrock/Vertex/Azure all enforce real structured outputs instead of prose-JSON + try/catch; a new llm_gateway.response_parse_failure metric (packages/llm-gateway/src/gateway.ts) fires centrally at the gateway boundary on any JSON.parse failure, independent of each caller's own fallback; correlation's and confirmation's maxTokens raised (512→2048, 1024→4096) for output-reasoning headroom. Deferred: App Map prompt-caching (no call site passes one without a larger refactor), non-Anthropic effort mapping, and a tool-execution loop (E1). Found-while-working fix: anthropicRejectsSampling now also matches claude-opus-5 (previously only opus-4-7/4-8), which had silently gone stale when A11 made opus-5 the confirmation-tier default. Updated docs/modules/llm-gateway.md.

[2026-08-22] The model-variance harness can now actually detect variance (audit finding A23): its only prior caller, packages/qa/src/synthetic.ts's perfectConfirmedForRepo, ignored the model parameter and echoed ground truth, so every model scored an identical, tautological 100% and no accuracy cliff could ever surface. New packages/qa/src/real-confirmation-scanner.ts's realConfirmedForRepo runs the REAL @montr/confirm static-confirmation engine (packages/confirm's confirmFindings, unmodified) against the two @montr/fixtures sample repos (the only repos with a hand-built AppMap + ProbableFinding[] fixture today — not the full 16-repo golden corpus, which corpus/baseline.json's minReposScored:14 assumes; a new REAL_VARIANCE_BASELINE/realVarianceCorpus pair scopes the gate to what is actually measured instead), once per model in the fake gateway's listModels() (RECOMMENDED_MODEL_MATRIX's triage/default/confirmation tiers); gatewayForModel makes the confirmation-tier LLM veto (golden rule #4's fail-safe demotion) when the model is below the confirmation floor and confirm otherwise — the existing createFakeLlmGateway({ cannedByPurpose }) convention, just picked per model — so a genuine accuracy cliff now produces a real, differing recall per model row (verified today: the below-floor triage model scores 0% recall vs 66.7% for the default/confirmation-tier models) instead of an identical number. packages/qa/src/cli.ts's qa:variance now runs this REAL scanner by default (--variance-selfcheck opts back into the old tautological echo as a plumbing-only smoke test, mirroring qa:corpus vs qa:corpus:selfcheck), and .github/workflows/ci.yml's golden-corpus job runs it as a new step, fully offline (fake gateway, no live provider credentials or spend). Updated docs/infra/testing.md.

[2026-08-22] Added opt-in per-tenant BullMQ queue isolation, OFF by default (audit finding A27): packages/contracts/src/queue.ts's new resolveQueueName(layer, clientId, tenantIsolation) derives a per-client queue name (montr.layer0.<clientId>) instead of the shared QUEUE_NAMES[layer] when enabled; new config.queue (packages/config/src/schema.ts's QueueConfigSchema — perTenantIsolation false / tenantIds [] by default, env MONTR_QUEUE_PER_TENANT_ISOLATION / MONTR_QUEUE_TENANT_IDS) is read by packages/orchestrator/src/bullmq-scheduler.ts's new deriveTenantSchedulerOptions, called identically by apps/worker (consumer: BullMqJobScheduler now fans out one BullMQ Queue + Worker per (layer, tenantId) pair, each independently polling Redis so one tenant's backlog cannot delay another's newly-queued job) and apps/api's produce-only EnqueueOnlyScheduler (producer), so both processes always agree on queue names. This repo ships plain (non-Pro) BullMQ with no group-based fair-scheduling primitive, so N-queues-per-tenant was the deliberate choice over a lighter-weight mechanism; not recommended past a modest tenant count per worker process. Updated docs/state/server-state.md, docs/modules/orchestration.md, docs/infra/environment.md.

[2026-08-22] Real token counting and Batch API capability added to the LLM gateway (audit findings A19, A31): MontrLlmGateway.countTokens() calls the Anthropic adapter's real messages.countTokens endpoint (packages/llm-gateway/src/adapters/anthropic.ts, mapping.ts's new buildAnthropicCountTokensBody), falling back to the fast chars/4 heuristic for Bedrock/Vertex/Azure or on failure; the A2 pre-call budget guard deliberately keeps using the heuristic (estimateTokens) rather than countTokens, to avoid adding a network round-trip to every gated call — documented as a deliberate latency/accuracy tradeoff. packages/cost-meter/src/estimate.ts's per-unit constants are now documented in-code as uncalibrated heuristics (no real captured prompt-size data exists in this repo); a new packages/llm-gateway/src/estimate-variance.test.ts validates PRD §19's ±15% estimate-vs-actual bound against a representative synthetic scan built from each real call site's own payload shape, replacing DOD.md item 6's ≈$0 concession with a real (if not ±15%-precise — measured ~5x, safe-direction) comparison. Batch API: MontrLlmGateway gained submitBatch()/pollBatch()/getBatchResults() (new packages/llm-gateway/src/batch.ts types), backed only by AnthropicAdapter's real messages.batches.create/retrieve/results (other providers throw NotImplementedError); packages/cost-meter/src/pricing.ts's priceUsageUsd gained an opts.batch flag applying the 50% Batch API discount (BATCH_DISCOUNT_MULTIPLIER), and MeterEntry/CostMeter.record() carry it through. Not wired into any pipeline layer — the orchestrator's FSM assumes synchronous layer completion, incompatible with a job that may resolve up to 24h later without further resumability work, and this change's file scope excluded the layer packages (packages/fix, packages/confirm, etc.) that would need to change to consume it; built and fully tested (mirroring how A10 handled gateway streaming). Confirmed prompt-cache usage (A31 item 1) is correctly priced end-to-end via a new real-adapter integration test (packages/llm-gateway/src/cache-pricing.integration.test.ts) — no wiring gap found. The App Map second cache point (A31 item 2) remains deferred: a concrete minimal wiring was designed (fold a stable per-scan App-Map summary into confirm/src/static.ts's system prompt) but not implemented, since packages/confirm was outside this change's scoped file list. Updated docs/modules/llm-gateway.md.

Update Triggers
Update this file when top-level architectural patterns change, when new packages or apps are introduced, when global cross-cutting policies evolve, or when files are added or removed from the docs directory.

Related Docs
docs/maintenance.md — Maintenance rules and decision tree for updating documentation.
docs/architecture/folder-structure.md — Detailed folder mapping of all packages and applications.
