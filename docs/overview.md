# Montr Secure System Overview

Scope: High-level architecture, mental model, directory index, and domain glossary for Montr Secure.
Rendering context: Isomorphic
Project tier: 4
Last updated: auto

Overview
Montr Secure is an on-premises, bring-your-own-LLM-key security orchestration platform that unifies static analysis, software composition analysis, secret detection, and dynamic exploit validation into an automated, six-layer analysis pipeline. The platform correlates raw multi-engine findings against structural application models, confirms genuine exploitability through static data-flow proofs and gated live DAST, synthesizes validated patches with proof-of-fix tests, and outputs compliance-ready reports with gated pull request automation. The codebase is organized as a Turborepo monorepo comprising a Fastify REST API, a Next.js operator console, a BullMQ pipeline worker, a montr CLI for local/CI-triggered scans, and seventeen decoupled TypeScript packages.

System Mental Model
The system operates as an end-to-end security pipeline structured across six distinct processing layers orchestrated by a resumable finite state machine.
Layer 0 intake and scoping builds a comprehensive structural application map of routes, data models, and taint surfaces while calculating token and cost estimates.
Layer 1 discovery executes multi-engine static analyzers, secret detectors, and dependency reachability scanners concurrently to capture raw candidate findings.
Layer 2 correlation scores candidates across reachability, exposure, and business impact to merge duplicate root causes into ranked probable findings without discarding evidence.
Layer 3 exploit confirmation proves vulnerability exploitability via deterministic interprocedural taint tracing and approver-gated live dynamic security testing against staging environments.
Layer 4 fix generation produces unified diff patches, proof-of-fix regression tests, and safety classifications that partition fixes into auto-eligible or human-required.
Layer 5 gate and reporting generates executive summaries, compliance exports across SARIF, SOC2, ISO27001, and OWASP, and opens automated pull requests for eligible remediations.

Key Architectural Decisions
Strict perimeter containment ensures that source code never leaves the client infrastructure except when transmitted directly to the client configured LLM provider endpoint.
Row-scoped multitenancy guarantees complete isolation across clients in Postgres, with high-security fields encrypted at rest using AES-256-GCM.
Resumable state machines allow long-running analysis workflows to pause at estimate and fix gates, survive worker crashes, and resume from checkpoints without re-running completed layers.
Budget hard-halt policies continuously meter token expenditure against user ceilings, stopping pipeline execution and producing partial reports before budget overruns occur.
Append-only hash-chained audit logging cryptographically seals all system actions, gate approvals, and operator decisions to provide tamper-evident compliance trails.

Cross-Cutting Concerns
Authentication and access control employ Argon2 password hashing, JSON Web Tokens, CSRF tokens, and role-based permissions dividing capabilities across Operator, Approver, and Viewer roles.
Error handling relies on custom domain error classes defined in packages/contracts/src/errors.ts mapped to standardized HTTP status codes and structured log envelopes.
Telemetry and logging utilize a content-aware scrubber in packages/telemetry/src/scrubber.ts to strip credentials, tokens, and raw code snippets before writing to stdout or OpenTelemetry collectors.
Styling across the operator web console uses Tailwind CSS v4 design tokens with a dark-first color scheme and semantic severity badges.

Documentation Directory Map
docs/overview.md — Master entry point, architecture summary, directory map, and glossary.
docs/maintenance.md — Step-by-step update guide and decision tree for maintaining documentation after code changes.
docs/architecture/folder-structure.md — Structural layout of packages, apps, tests, and configuration files.
docs/architecture/rendering-strategy.md — Rendering architecture across the Next.js console and Fastify backend.
docs/architecture/data-flow.md — Complete lifecycle of data, pipeline transitions, and serialization boundaries.
docs/ui/component-library.md — Reusable UI primitives and domain widgets in the operator web console.
docs/ui/layout-system.md — Root and nested layout hierarchies, route wrappers, and navigation chrome.
docs/ui/theming.md — Color system, dark theme tokens, severity scales, and typography definitions.
docs/api/route-handlers.md — Fastify HTTP route handlers, path parameters, role requirements, and response contracts.
docs/api/external-services.md — Third-party integrations including LLM providers, VCS platforms, and vulnerability databases.
docs/api/database.md — Prisma relational schema, multitenancy design, encrypted fields, and audit log chaining.
docs/state/client-state.md — TanStack React Query cache management, role context, and local component state.
docs/state/server-state.md — Postgres persistence, Redis BullMQ queues, resume checkpoints, and cache policies.
docs/auth/auth-flow.md — Session lifecycle, token issuance, password hashing, and authentication guards.
docs/auth/authorization.md — Role-based permission matrix, gate approval constraints, and DAST authorization rules.
docs/infra/environment.md — Complete catalog of environment variables, defaults, and exposure levels.
docs/infra/deployment.md — Docker Compose configurations, Kubernetes Helm charts, and air-gapped installation procedures.
docs/infra/testing.md — Vitest test suites, coverage floors, golden corpus evaluation, and smoke tests.
docs/modules/orchestration.md — Resumable pipeline state machine, worker concurrency, and kill switch mechanisms.
docs/modules/appmap.md — Layer 0 AST extraction, route discovery, ORM mapping, and taint modeling.
docs/modules/discovery.md — Layer 1 static analysis engines, secret detection, SCA reachability, and custom rules.
docs/modules/correlation.md — Layer 2 candidate scoring, root-cause deduplication, and grounding algorithms.
docs/modules/confirmation.md — Layer 3 static data-flow solver and gated live DAST execution engine.
docs/modules/fix-generation.md — Layer 4 patch synthesis, proof test generation, and safety risk classifier.
docs/modules/reporting-vcs.md — Layer 5 report generation, compliance exporters, and automated PR management.
docs/modules/llm-gateway.md — Multi-provider BYO-key LLM gateway, model matrix, and real-time cost meter.
docs/modules/semantic-index.md — AST chunking, embedding generation, and pgvector-backed retrieval built alongside the App Map.
docs/modules/web-console.md — Next.js 14 operator console interface, real-time monitors, and management views.

Domain Glossary
App Map: A structured graph of an application containing detected routes, ORM models, entrypoints, and potential taint flows.
Candidate Finding: A raw, noisy issue surfaced by Layer 1 static tools prior to reachability correlation.
Probable Finding: A correlated finding with computed reachability, exposure, and impact scores mapped to an application root cause.
Confirmed Finding: A high-confidence vulnerability backed by either an interprocedural static proof or a successful live DAST exploit.
Unconfirmed Finding: A candidate demoted due to unreachability or unverified exploitability, retained in report appendixes.
Auto-Eligible Fix: A low-risk remediation (such as header hardening or dependency bump) permitted for automated pull request creation.
Human-Required Fix: A sensitive remediation (touching authentication, cryptography, or access control) requiring manual engineer review.
Kill Switch: An emergency control that aborts active pipeline scans and live HTTP DAST probing across all worker processes.

Recent Changes

[2026-09-12] A9 (two compounding semantic-index gaps closed — the embeddings adapter matrix and the "complete but zero consumers" wiring gap): `packages/llm-gateway/src/embeddings.ts` gained `OpenAiEmbeddingAdapter`, the same `openai` SDK `embeddings.create` wire shape `AzureEmbeddingAdapter` already speaks, pointed at `https://api.openai.com/v1` with a Bearer key (mirroring `adapters/openai-compatible.ts`'s A3 baseURL/Bearer-key convention) — `createEmbeddingAdapter`'s provider switch now returns it for `openai`; the other five OpenAI-compatible providers (google/xai/moonshot/zhipu/deepseek) remain honest `NotImplementedError` stubs (out of this change's explicit scope). `packages/semantic-index` is now a real pipeline consumer, not a documented exception: `packages/appmap/src/build.ts` gained a best-effort, structural `BuildAppMapDeps.semanticIndex` hook — called with the sandboxed checkout's still-live `workspace.dir` plus clientId/appMapId/repo/commitSha immediately after telemetry-surfaces detection and before persistence/cleanup, any failure caught and logged, never failing the scan; `packages/appmap/src/runner.ts`'s `createLayer0Runner` threads it through. A new, additive, OFF-by-default `SemanticIndexConfigSchema` (`packages/config/src/schema.ts`, `enabled`/`embeddingModel`, env `MONTR_SEMANTIC_INDEX_ENABLED`/`MONTR_SEMANTIC_INDEX_EMBEDDING_MODEL`) gates real construction in `apps/worker/src/main.ts`'s new `resolveSemanticIndexOptions` (requires `llm.provider` to be `azure` or `openai`; the `CodeChunkRepository` itself always constructs safely, a missing `pgvector` extension only fails at query time and is caught). `apps/worker/src/runners.ts` wires the resulting `buildSemanticIndex`/`querySemanticIndex` calls into all three intended call sites: Layer 0 (index build), Layer 2 correlation (`CorrelateInput.semanticSearch`, appends a purely informational "structurally similar code found elsewhere" sentence to a promoted finding's reachability hypothesis — deliberately never touches the golden-corpus-calibrated scoring formula), and Layer 3 confirmation, which gained a new seventh read-only investigation-loop tool, `semantic_search` (`packages/confirm/src/investigate-tools.ts`, dispatched via the same tool-definition/schema/dispatch convention as `grep`/`find_definition`), threaded through `ConfirmDeps.semanticSearch`. Every new capability is additive and gated — absent config/provider/index, each degrades to a logged skip or an honest "not available" tool result, never a crash. `scripts/check-unwired-seams.mjs` moved `createEmbeddingAdapter`/`buildSemanticIndex`/`querySemanticIndex` from `KNOWN_UNWIRED` (informational) into the enforced `SEAMS` list — the check now fails CI if any of the three ever loses its real caller. 44 new/updated tests across `packages/llm-gateway/src/embeddings.test.ts`, `packages/config/src/semantic-index-config.test.ts`, `tests/appmap.build.test.ts`, `tests/confirm.investigation.test.ts`, `tests/correlation.correlate.test.ts`; full scoped suite green, `tsc -b` clean on every touched package. Updated docs/modules/semantic-index.md, docs/modules/llm-gateway.md, docs/modules/confirmation.md, docs/modules/correlation.md, docs/modules/appmap.md, docs/infra/environment.md.

[2026-09-12] A10 (RedTeamStep gains a request-body field — closes a genuine structural gap in the purple-team detection loop): `RedTeamStepSchema` (packages/contracts/src/phase4.ts) gained an additive, optional `body: z.string().optional()` field; `runScenario` (packages/confirm/src/scenarios.ts) now forwards a step's `body` to `deps.transport.send` when set and records it on the transcript exchange as `request.bodySnippet` via `toExchange` — both changes are byte-identical no-ops for any step that defines no body. The three OWASP starter-catalogue scenarios whose confirming payload is a POST body (`owasp-a03-command-injection`, `owasp-a08-software-data-integrity`, `owasp-a10-ssrf` in packages/state-store/src/redteam-catalogue.ts) now carry their real confirming payload — a `$(id)` command-injection marker, a Jackson-style `"@type"` polymorphic-deserialization marker, and a loopback/metadata SSRF target URL — each a literal `CATEGORY_MARKERS` entry (packages/report/src/detection-rules/context.ts) so the B3-generated Sigma rule's `cs-body|contains` condition genuinely matches. `packages/confirm/src/purple-loop.ts`'s `evaluateSigmaRule`/`evaluateDetectionRuleAgainstScenario` needed no logic change — they already read `request.bodySnippet` — only the module header and per-request "no body" gap message were corrected, since the gap is now per-step rather than universal. `packages/qa/src/blue-team-corpus.ts`'s `BLUE_TEAM_GROUND_TRUTH` relabelled all three previously `expectedFired: false` cases to `true`. Real measured result (`node scripts/blue-team-corpus-scan.mjs` + `pnpm --filter @montr/qa qa:blue-team-corpus`): TP=9 FP=0 FN=0 TN=0 across all 9 labelled scenarios (up from TP=6/TN=3), detectionPrecision=100.0%, detectionRecall=100.0%, baseline gate PASS. Consequence documented rather than hidden: with zero labelled negative cases left, `detectionPrecisionMin` (corpus/blue-team-baseline.json) is now a dormant floor for this specific corpus — negative/near-miss coverage of the evaluator continues independently via tests/confirm.purple-loop.test.ts's hand-built cases. Two pre-existing test invariants that encoded the old bug as expected behavior were corrected (tests/qa.blue-team-corpus.test.ts, scripts/blue-team-corpus-scan.run.test.ts), and two new tests added (tests/confirm.scenarios.test.ts, tests/confirm.purple-loop.test.ts) proving the body is sent, recorded, and can fire a rule on its own. No Prisma/DB schema change — `RedTeamScenario.steps` was already an encrypted Json blob; `body` is a new field inside the existing TS shape stored in it. Updated docs/modules/confirmation.md, docs/infra/testing.md, corpus/blue-team-baseline.json, docs/plan/26-09-12-tasks-red-blue-agentic-posture.md (A10).

[2026-09-12] A17 (structured output audit — closes the audit's premise gap and fixes a real bug it surfaced): audited all seven responseFormat:"json" call sites against packages/llm-gateway/src/structured-output.ts's purpose-keyed schema map. Six were already covered by real, provider-side schema-constrained decoding automatically (A13 landed this with zero call-site changes) — no request.responseSchema was added at those sites since doing so would only duplicate an already-live schema with no functional effect, and each site's fail-safe JSON-parse fallback was deliberately left in place (genuinely different failure mode: truncation/off-spec responses, and several sites' flexible-shape branches are exercised by packages/fixtures/src/llm.ts's non-schema-conformant canned responses). The seventh, packages/confirm/src/adversarial.ts's E4 verifier panel, was a real bug: it shared metadata.purpose:"confirmation" with confirm/src/static.ts but asked its own lenses for a DIFFERENT JSON shape ({confirm,rationale} vs. {confirmed,argument}) — under real schema enforcement the mismatched purpose-keyed default's additionalProperties:false would silently strip confirm/rationale from every real response, so every adversarial verifier would read as an undefined vote and always reject, invisible to every existing test (all mock LLMGateway.complete() directly, bypassing real schema enforcement). Fixed by giving that one call site its own explicit request.responseSchema. No production caller invokes this path yet (see A3 in docs/plan), so no live customer impact today, but it would have silently neutered E4 the moment that gap closes. New packages/confirm/src/adversarial.test.ts (1 test); full packages/confirm suite (110 tests) and typecheck clean. Updated docs/modules/llm-gateway.md, docs/modules/confirmation.md, docs/plan/26-09-12-tasks-red-blue-agentic-posture.md (A17).

[2026-09-12] A12 (fix agent loop's second knob no longer silently defaults multi-file reading off): `FixAgentLoopConfigSchema` (packages/config/src/schema.ts) now defaults `maxToolCalls` to 3 instead of 0 (still capped at 20) — an operator who sets only MONTR_FIX_AGENT_LOOP_ENABLED=true now gets the sandboxed read_file tool by default, matching what the env var's name and A5's stated purpose already implied, instead of a retry loop with no multi-file reading. Chosen over the doc-only alternative because a small (3), cheap, already-capped non-zero default better matches operator expectations than a second undiscoverable knob. Belt-and-suspenders for the one remaining silent case — an operator who explicitly sets MONTR_FIX_AGENT_LOOP_MAX_TOOL_CALLS=0 — apps/worker/src/runners.ts's Layer 4 runner now logs a worker.fix_agent_loop.no_tool_calls warning whenever the loop is enabled with maxToolCalls at 0. Updated docs/infra/environment.md (both the MAX_TOOL_CALLS entry and a cross-reference on the ENABLED entry) and docs/modules/fix-generation.md. Updated docs/plan/26-09-12-tasks-red-blue-agentic-posture.md (A12).

[2026-09-12] A11/A4 (golden-corpus precision gate correction — closes the regression the 2026-09-09 entry below flagged as outstanding): re-running the real pipeline (node scripts/corpus-scan.mjs, real semgrep + gitleaks) and rescoring (pnpm --filter @montr/qa qa:corpus) now measures FP-rate 0.0% / precision 100.0% / recall 34.8% (TP=16 FP=0 FN=30), baseline gate PASS — the two sql_injection/command_injection false positives present since the 2026-08-19 A17 recalibration are gone. Root cause was NOT a detector or scoring defect: both flagged cases were genuine, independently-exploitable vulnerabilities corpus/javaseccode/ground-truth.manifest.json had simply never catalogued — a second, distinct SQL injection in SQLI.java (jdbc_ps_vuln, line 149, string-concatenates the untrusted username into the SQL text before ever calling PreparedStatement, so the `?` placeholder buys nothing) and a second, distinct command injection in CommandInject.java (codeInjectHost, line 45, concatenates the untrusted Host header into a `sh -c curl` ProcessBuilder call). Fixed by adding gt_javaseccode_sqli2/gt_javaseccode_cmdi2 to the manifest, following the exact precedent corpus/README.md's sampling-method section already documents for pygoat (gt_pygoat_cmdi2) — corpus/baseline.json's thresholds were never touched, and no detector or corpus fixture was special-cased. No packages/discovery or packages/correlation code changed. Updated docs/plan/26-09-12-tasks-red-blue-agentic-posture.md (A4).

[2026-09-12] A2/A7/A8/A13 (four "argument missing at the call site" plumbing bugs closed together in apps/worker/src/runners.ts, all converging on the same Layer 5 buildReport call — no new logic, all downstream logic already existed and was tested): A2 — Layer 5's runner now calls resolveAppMap(ctx) exactly like Layers 1-3 and threads the result into buildReport's appMap input, so packages/report/src/report-builder.ts's buildBlueTeamReport's detection-coverage, attack-path, and threat-model sections stop rendering permanently empty. A7 — Layer 3's runner calls packages/appmap/src/coverage-analysis.ts's persistDetectionCoverageForScan immediately after confirmFindings resolves the confirmed set (best-effort, never fails confirmation on a store hiccup), so B6's tri-state detection-coverage verdict is now actually persisted per confirmed finding. A8 — Layer 3's runner also wires packages/confirm/src/purple-loop.ts's verifyScenarioDetection: gated on the SAME approver-authorized live-DAST toggle (job.allowLive/stagingUrl) Layer 3's own live confirmation already uses (Layer 5 has no such field, so Layer 3 is the only correct home), scenario selection is category-based (mirrors packages/qa/src/blue-team-corpus.ts's hand-traced pattern via the exported REDTEAM_CATEGORY_TO_FINDING_CATEGORIES, against this client's real ENABLED RedTeamScenario catalogue rows), and it reuses packages/confirm/src/live.ts's own defaultTransport (newly exported) — no new egress path. The resulting purple-team entries travel to Layer 5 only through the orchestrator's in-process priorOutputs cache (packages/contracts/src/layers.ts's Layer3OutputSchema gained an additive, non-persisted purpleTeamEntries field, mirroring Layer2Output.demoted's identical precedent) and feed BuildReportInput.purpleTeamEntries. A13 — Layer 5's runner now calls packages/hardening/src/generate.ts's generateHardeningRecommendations using a real @montr/discovery fsFileProvider over the same repoRoot Layer 1/4 already resolve, threading the result into BuildReportInput.hardeningRecommendations; degrades to an honest empty list (never a guess) when no local checkout exists. packages/report/src/report-builder.ts's buildBlueTeamReport itself was NOT modified — this was entirely a caller-side wiring gap. 6 new apps/worker/src/runners.test.ts tests (including two real end-to-end purple-team runs: one proving the safety gate is honored when dast.enabled is false, one running an actual scenario against a real local HTTP server and persisting a genuine live verification onto the A7-created DetectionCoverage row) plus 2 pre-existing Layer 5 tests fixed (they built a scan fixture with no App Map, which A2 now requires); apps/worker/src/testkit.ts's in-memory StateStore gained real detectionCoverage/detectionRules/redTeamScenarios repository stand-ins. Full apps/worker + packages/confirm/appmap/report/contracts/orchestrator/hardening + root tests/_.blue-team_/purple-loop/scenarios suites green. Updated docs/modules/reporting-vcs.md, docs/modules/confirmation.md, docs/modules/appmap.md, docs/plan/26-09-12-tasks-red-blue-agentic-posture.md (A2/A7/A8/A13).

[2026-09-09] A5 production wiring + CI repair (closing out the July-line divergence task list): `@montr/config` gained `FixAgentLoopConfigSchema` under a new `fixGeneration.agentLoop` section (`enabled` default false, `maxIterations` default 3 capped at 10, `maxToolCalls` default 0 capped at 20 — the caps mean a mistyped extra zero fails config validation rather than causing runaway spend), surfaced through three env vars (`MONTR_FIX_AGENT_LOOP_ENABLED`, `MONTR_FIX_AGENT_LOOP_MAX_ITERATIONS`, `MONTR_FIX_AGENT_LOOP_MAX_TOOL_CALLS`, see docs/infra/environment.md) and threaded into `apps/worker/src/runners.ts`'s Layer 4 runner. A5's loop was previously unreachable in production; it is now operator-configurable while remaining OFF unless deliberately enabled. Alongside: eslint.config.js gained a `**/.next/**` ignore (the root `eslint .` entry point was reporting ~11.4k errors, 205 of 207 offending files being generated Next.js build output — the per-package turbo gate was always clean); two genuine unused-variable violations fixed at source in packages/contracts/src/blue-team.test.ts and tests/fix.container-replay.test.ts; a new non-blocking `windows` CI job (windows-latest: install, typecheck, unit tests, e2e-scan) so the A2/A6 Windows fixes cannot silently rot; CI's helm renders now pass `networkPolicy.llmEndpoint.cidr` (TEST-NET-3, RFC 5737) — the chart deliberately `required`s it and rejects 0.0.0.0/0 as a real default-deny egress guarantee, and CI had never supplied one, so every render failed; and packages/fixtures/sample-repos/iac-samples/vulnerable/.env is now tracked via a narrow `.gitignore` carve-out — the repo-wide `.env` ignore meant this FAKE fixture file never reached a fresh clone, so `iac.test.ts`'s missing-.dockerignore assertion (which only fires when a secret-bearing file is genuinely present) passed locally and failed in CI. NOTE: the golden-corpus gate was RED and NOT addressed here — CI measured FP-rate 12.5% / precision 87.5% against corpus/baseline.json's 5% / 90% thresholds (two false positives, in sql_injection and command_injection, appearing since the 2026-08-19 A17 recalibration). Recall improved (25.0% -> 31.8%), so this was a precision regression, not a wholesale one. The baseline was deliberately NOT loosened — its own header says tighten, never loosen. Since resolved — see the 2026-09-12 entry above.

[2026-09-09] A5 (Layer 4 fix generation gains a bounded agentic retry loop with sandboxed multi-file context — the last capability the abandoned July line had that current lacked, closed): packages/fix/src/generate.ts's `FixGenerationContext` gained an additive, OFF-by-default `agentLoop` option (`enabled`/`maxIterations`/`maxToolCalls`), mirroring the `containerProof` (E13) constructor-opt-in convention — absent or disabled, fix generation is the exact single-shot LLM Fix Proposal call, unchanged. Enabled, the new `proposeFixWithAgent` replaces that single call with a bounded retry loop: propose a line-anchored edit list, check it, feed back the SPECIFIC failure reason (not valid JSON; a structurally invalid edit list, using `edits.ts`'s own `parseLlmEdits` rejection reasons; the vulnerable pattern not found in the original; or still present after the edit), retry up to `maxIterations` real proposal attempts. With `maxToolCalls` above zero it additionally exposes a sandboxed `read_file` tool — backed by the same `FixGenerationContext.source` reader every other read in this module already uses, never raw filesystem access — so the model may inspect imported/sibling files first; tool round-trips are bounded separately and never consume a proposal attempt. The central design constraint: the loop's own retry/feedback cycle is driven ONLY by a new cheap, in-process oracle (build the unified diff, apply it with the `diff` library, test the target strategy's own vulnerability regex predicate) — never `patch.ts`'s real-`vitest`-subprocess `validatePatch` or `container-validate.ts`'s real-Docker `validatePatchWithContainerReplay`, both of which still run exactly once per final candidate in `generateOne`, completely unchanged from the single-shot path; looping either of those per retry would have turned a bounded few-iteration loop into a multi-second-per-iteration one. The `fix.generated` audit event's metadata gained an additive `iterations` field (a round-trip count, metadata only, never a code body). Depends on A4's gateway tool-call round-tripping (`packages/llm-gateway/src/mapping.ts`, `LLMMessageSchema.toolCalls`). Off by default, and now reachable WITHOUT a code change: `apps/worker/src/runners.ts` populates `agentLoop` from `@montr/config`'s `fixGeneration.agentLoop` — but only when an operator has explicitly enabled it, leaving the field absent otherwise so the default request is byte-for-byte the pre-existing single-shot call. 8 new tests, colocated at `packages/fix/src/generate.agent-loop.test.ts` and `generate.agent-loop.expensive-verification.test.ts` (the latter spies directly on `validatePatch` to prove it runs once per candidate, never once per retry iteration). Updated docs/modules/fix-generation.md.

[2026-09-09] A3 (LLM gateway supports only the Anthropic model family — closed): `ProviderSchema` (packages/contracts/src/llm.ts) grew from four providers (anthropic, bedrock, vertex, azure) to ten, adding openai, google, xai, moonshot, zhipu, deepseek — all reached through one new generic `OpenAiCompatibleAdapter` (packages/llm-gateway/src/adapters/openai-compatible.ts, the `openai` SDK with a per-provider baseURL + Bearer key), ported from an abandoned earlier project line and adapted to current conventions rather than copied verbatim. It deliberately REUSES current's `azure.ts` body-builder (now exported as `buildBody`, previously private) and tool-call parser rather than porting a second, simpler copy — preserving structured-output (A13), tool-calling (A8), and tool-round-tripping (A4) for all six new providers instead of forking capabilities. `packages/llm-gateway/src/keytier.ts` classifies openai/google/xai as `unknown` (fail-safe, same as a direct Anthropic key) and moonshot/zhipu/deepseek as `data_retaining` by default (consumer/CN-tier, blocked under `keyTierGuard: block` absent an operator-declared enterprise tier). `packages/security/src/egress-guard.ts`'s `PROVIDER_DEFAULT_HOSTS` gained one exact host per new provider. The Prisma `Provider` enum (packages/state-store/prisma/schema.prisma) gained the same six values via additive migration `11_llm_provider_expansion` so `LlmCredential.provider` can store them; `packages/llm-gateway/src/embeddings.ts`'s adapter factory treats all six as not-yet-implemented (honest gap, mirrors anthropic/bedrock/vertex today). For a BYO-key product this converts "BYO Anthropic key" into "BYO any key" — a commercial positioning change, not just a technical one. Updated docs/modules/llm-gateway.md, docs/api/database.md.

[2026-08-22] B12 (blue-team golden corpus + CI regression gate — the blue-team mirror of the existing golden-corpus gate): new packages/qa/src/blue-team-corpus.ts's `BLUE_TEAM_GROUND_TRUTH` hand-labels 9 of the 13 templates in `REDTEAM_SCENARIO_CATALOGUE` (packages/state-store/src/redteam-catalogue.ts) — the 9 with a single, coherent target route a confirmed finding's App-Map-resolved route would represent; the other 4 (owasp-a02-crypto-failures, owasp-a05-security-misconfiguration, owasp-a06-vulnerable-components, owasp-a09-logging-monitoring) each probe several structurally unrelated endpoints and are honestly excluded rather than force-labelled. Each label is a real, hand-traced verdict — given the scenario's real steps (verbatim from the catalogue) and the real Sigma rule B3 would generate for the target category (via `buildRuleContext`/`CATEGORY_MARKERS`), does the rule's `condition` (route-only when the category has no `CATEGORY_MARKERS` entry, else route + a payload marker) structurally match ANY step's concretized request — including two genuine structural misses (command_injection, insecure_deserialization, ssrf) where the confirming payload lives only in a POST body that `RedTeamStep`/`runScenario` never actually sends. `runBlueTeamCorpus` is the real invocation: real `instantiateScenario` + a paired synthetic `ConfirmedFinding`/`AppMap`, the real `generateDetectionRules` (B3), and the real `runPurpleTeamScenario` (B5, identically gated, fake in-process transport) — nothing hand-computed. Its first real run caught a genuine bug in the corpus's own scenario config (not the product): `dast.scope.maxMutatingRequests` defaults to 0 and `runScenario` halts the ENTIRE scenario on the first refused step, so every case with a mutating confirming step (POST/PUT) was silently starving — fixed by explicitly raising the cap in `blueTeamConfig()`, the same knob the config schema's own doc comment invites an operator to raise. Real measured result after the fix: TP=6 FP=0 FN=0 TN=3, detectionPrecision=100.0%, detectionRecall=100.0% (`scripts/blue-team-corpus-scan.mjs` / `.run.test.ts`, mirroring `corpus-scan.mjs`'s exact pattern). New `packages/qa/src/blue-team-baseline.ts` (`evaluateBlueTeamBaseline`, mirrors `baseline.ts`'s `evaluateBaseline` shape exactly), `blue-team-report.ts`, `blue-team-findings-io.ts`, `blue-team-cli.ts` (`qa:blue-team-corpus`/`qa:blue-team-corpus:selfcheck`, mirrors `cli.ts`), and the committed `corpus/blue-team-baseline.json` (detectionPrecisionMin/detectionRecallMin: 0.8, minScenariosScored: 9 — a safety margin below the real 100%/100% measurement). Wired into `.github/workflows/ci.yml`'s `golden-corpus` job as three additional steps after the A23 model-variance step. 30 new tests (tests/qa.blue-team-baseline.test.ts, tests/qa.blue-team-corpus.test.ts, tests/qa.blue-team-cli.test.ts) plus scripts/blue-team-corpus-scan.run.test.ts's own real-run assertions. Updated docs/infra/testing.md, docs/plan/26-08-22-tasks-ai-depth.md.

Update Triggers
Update this file when top-level architectural patterns change, when new packages or apps are introduced, when global cross-cutting policies evolve, or when files are added or removed from the docs directory.

Related Docs
docs/maintenance.md — Maintenance rules and decision tree for updating documentation.
docs/architecture/folder-structure.md — Detailed folder mapping of all packages and applications.
