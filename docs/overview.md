# Montr Secure System Overview

Scope: High-level architecture, mental model, directory index, and domain glossary for Montr Secure.
Rendering context: Isomorphic
Project tier: 4
Last updated: auto

Overview
Montr Secure is an on-premises, bring-your-own-LLM-key security orchestration platform that unifies static analysis, software composition analysis, secret detection, and dynamic exploit validation into an automated, six-layer analysis pipeline. The platform correlates raw multi-engine findings against structural application models, confirms genuine exploitability through static data-flow proofs and gated live DAST, synthesizes validated patches with proof-of-fix tests, and outputs compliance-ready reports with gated pull request automation. The codebase is organized as a Turborepo monorepo comprising a Fastify REST API, a Next.js operator console, a BullMQ pipeline worker, a montr CLI for local/CI-triggered scans, and seventeen decoupled TypeScript packages.

System Mental Model
The system operates as an end-to-end security pipeline structured across six distinct processing layers orchestrated by a resumable finite state machine.
Layer 0 intake and scoping builds a comprehensive structural application map of routes, data models, and taint surfaces while calculating token and cost estimates.
Layer 1 discovery executes multi-engine static analyzers, secret detectors, and dependency reachability scanners concurrently to capture raw candidate findings.
Layer 2 correlation scores candidates across reachability, exposure, and business impact to merge duplicate root causes into ranked probable findings without discarding evidence.
Layer 3 exploit confirmation proves vulnerability exploitability via deterministic interprocedural taint tracing and approver-gated live dynamic security testing against staging environments.
Layer 4 fix generation produces unified diff patches, proof-of-fix regression tests, and safety classifications that partition fixes into auto-eligible or human-required.
Layer 5 gate and reporting generates executive summaries, compliance exports across SARIF, SOC2, ISO27001, and OWASP, and opens automated pull requests for eligible remediations.

Key Architectural Decisions
Strict perimeter containment ensures that source code never leaves the client infrastructure except when transmitted directly to the client configured LLM provider endpoint.
Row-scoped multitenancy guarantees complete isolation across clients in Postgres, with high-security fields encrypted at rest using AES-256-GCM.
Resumable state machines allow long-running analysis workflows to pause at estimate and fix gates, survive worker crashes, and resume from checkpoints without re-running completed layers.
Budget hard-halt policies continuously meter token expenditure against user ceilings, stopping pipeline execution and producing partial reports before budget overruns occur.
Append-only hash-chained audit logging cryptographically seals all system actions, gate approvals, and operator decisions to provide tamper-evident compliance trails.

Cross-Cutting Concerns
Authentication and access control employ Argon2 password hashing, JSON Web Tokens, CSRF tokens, and role-based permissions dividing capabilities across Operator, Approver, and Viewer roles.
Error handling relies on custom domain error classes defined in packages/contracts/src/errors.ts mapped to standardized HTTP status codes and structured log envelopes.
Telemetry and logging utilize a content-aware scrubber in packages/telemetry/src/scrubber.ts to strip credentials, tokens, and raw code snippets before writing to stdout or OpenTelemetry collectors.
Styling across the operator web console uses Tailwind CSS v4 design tokens with a dark-first color scheme and semantic severity badges.

Documentation Directory Map
docs/overview.md — Master entry point, architecture summary, directory map, and glossary.
docs/maintenance.md — Step-by-step update guide and decision tree for maintaining documentation after code changes.
docs/architecture/folder-structure.md — Structural layout of packages, apps, tests, and configuration files.
docs/architecture/rendering-strategy.md — Rendering architecture across the Next.js console and Fastify backend.
docs/architecture/data-flow.md — Complete lifecycle of data, pipeline transitions, and serialization boundaries.
docs/ui/component-library.md — Reusable UI primitives and domain widgets in the operator web console.
docs/ui/layout-system.md — Root and nested layout hierarchies, route wrappers, and navigation chrome.
docs/ui/theming.md — Color system, dark theme tokens, severity scales, and typography definitions.
docs/api/route-handlers.md — Fastify HTTP route handlers, path parameters, role requirements, and response contracts.
docs/api/external-services.md — Third-party integrations including LLM providers, VCS platforms, and vulnerability databases.
docs/api/database.md — Prisma relational schema, multitenancy design, encrypted fields, and audit log chaining.
docs/state/client-state.md — TanStack React Query cache management, role context, and local component state.
docs/state/server-state.md — Postgres persistence, Redis BullMQ queues, resume checkpoints, and cache policies.
docs/auth/auth-flow.md — Session lifecycle, token issuance, password hashing, and authentication guards.
docs/auth/authorization.md — Role-based permission matrix, gate approval constraints, and DAST authorization rules.
docs/infra/environment.md — Complete catalog of environment variables, defaults, and exposure levels.
docs/infra/deployment.md — Docker Compose configurations, Kubernetes Helm charts, and air-gapped installation procedures.
docs/infra/testing.md — Vitest test suites, coverage floors, golden corpus evaluation, and smoke tests.
docs/modules/orchestration.md — Resumable pipeline state machine, worker concurrency, and kill switch mechanisms.
docs/modules/appmap.md — Layer 0 AST extraction, route discovery, ORM mapping, and taint modeling.
docs/modules/discovery.md — Layer 1 static analysis engines, secret detection, SCA reachability, and custom rules.
docs/modules/correlation.md — Layer 2 candidate scoring, root-cause deduplication, and grounding algorithms.
docs/modules/confirmation.md — Layer 3 static data-flow solver and gated live DAST execution engine.
docs/modules/fix-generation.md — Layer 4 patch synthesis, proof test generation, and safety risk classifier.
docs/modules/reporting-vcs.md — Layer 5 report generation, compliance exporters, and automated PR management.
docs/modules/llm-gateway.md — Multi-provider BYO-key LLM gateway, model matrix, and real-time cost meter.
docs/modules/semantic-index.md — AST chunking, embedding generation, and pgvector-backed retrieval built alongside the App Map.
docs/modules/web-console.md — Next.js 14 operator console interface, real-time monitors, and management views.

Domain Glossary
App Map: A structured graph of an application containing detected routes, ORM models, entrypoints, and potential taint flows.
Candidate Finding: A raw, noisy issue surfaced by Layer 1 static tools prior to reachability correlation.
Probable Finding: A correlated finding with computed reachability, exposure, and impact scores mapped to an application root cause.
Confirmed Finding: A high-confidence vulnerability backed by either an interprocedural static proof or a successful live DAST exploit.
Unconfirmed Finding: A candidate demoted due to unreachability or unverified exploitability, retained in report appendixes.
Auto-Eligible Fix: A low-risk remediation (such as header hardening or dependency bump) permitted for automated pull request creation.
Human-Required Fix: A sensitive remediation (touching authentication, cryptography, or access control) requiring manual engineer review.
Kill Switch: An emergency control that aborts active pipeline scans and live HTTP DAST probing across all worker processes.

Recent Changes
[2026-08-22] Added an AI-application security ruleset that scans TARGET applications (the customer's codebase, not this product) for LLM-integration vulnerabilities (enhancement E11): new prompt_injection category in the finding taxonomy (packages/contracts/src/compliance.ts's CategorySchema/CATEGORY_TAXONOMY, mapped to CWE-1427/A03:2021), with the matching Prisma Category enum value and migration 7_prompt_injection_category (packages/state-store/prisma), plus every exhaustive Record<Category, ...> map completed for the new member (packages/correlation/src/taxonomy.ts's CATEGORY_IMPACT_BASE and INJECTION_CATEGORIES, packages/confirm/src/taxonomy.ts's DATAFLOW_SINK_KINDS and BASE_SEVERITY, packages/report/src/exports/controls.ts's SOC2/ISO27001 control maps). New packages/discovery/src/detectors/ai-security.ts's detectAiSecurity is a lightweight offline ts-morph AST detector (mirroring packages/discovery/src/detectors/sca.ts's collectCalledPackages pattern, not bundled Semgrep YAML — this codebase's only existing Semgrep rule-file convention is client-authored via custom-rules.ts) covering five sub-categories: prompt injection surface (new prompt_injection category), unsafe tool/function exposure (broken_access_control), unescaped LLM output rendered to users (xss), secrets leaking into prompts (sensitive_data_exposure, distinct from the general gitleaks/regex secret detector — this flags a credential flowing INTO an LLM call's arguments, which leaves the target's infra to a third-party provider), and missing output validation on a sensitive sink (tagged with the sink's own category — sql_injection/command_injection/path_traversal — via metadata.aiSource rather than a new blanket category). Wired into packages/discovery/src/index.ts's Layer 1 concurrent fan-out alongside SAST/secrets/SCA. New packages/fixtures/sample-repos/ai-app-vulnerable and ai-app-clean fixture repos plus packages/discovery/src/detectors/ai-security.test.ts (14 tests: one vulnerable/clean pair per sub-category, plus a no-LLM-call-sites degrade-to-empty case and an fsFileProvider integration test against the new fixture repos). Updated docs/modules/discovery.md.
[2026-08-22] Added cross-scan memory, generalizing A10's §15 false-positive tuning loop to a broader class of durable, per-repo learned facts (enhancement E8): new packages/state-store/prisma/schema.prisma LearnedFact model (clientId + repo scoped, LearnedFactType enum of custom_sanitizer/framework_idiom/operator_decision, free-form Json content, Json provenance) with a matching migration and packages/state-store/src/learned-facts.ts's LearnedFactRepositoryImpl (record + listByRepo); confirmed_false_positive is deliberately NOT a persisted LearnedFactType — that class already lives in A10's audit-log-backed FalsePositiveMarkRepository and is merged in at read time instead of duplicated. Write path: a new operator-facing POST /api/v1/learned-facts route (apps/api/src/routes/learned-facts.ts, operator/approver role, audited as learned_fact.recorded) plus a GET listing endpoint; apps/api/src/store.ts's ApiStore/StateStoreLike gained a mirrored LearnedFactStore. Read/injection path: apps/worker/src/runners.ts's new loadLearnedFactsContext loads this client+repo's facts (capped at LEARNED_FACT_CONTEXT_LIMIT=10, merged with A10's FP-mark signals) and withLearnedFactsContext wraps the gateway instance handed to Layer 1 discovery, Layer 2 correlation, and Layer 3 confirmation, appending the bounded context to a matching request's system prompt — additive only, never replacing the resolvePrompt-resolved template or suppressing a finding on its own; a repo with no prior facts produces a byte-identical prompt to before this change (regression-tested). The wrapper lives in apps/worker rather than editing packages/correlation/confirm/discovery/triage.ts directly, since those were out of this change's file scope — the alternative in-package wiring point is documented in code comments and docs/modules/llm-gateway.md. Updated docs/api/database.md, docs/api/route-handlers.md, docs/modules/llm-gateway.md, docs/modules/discovery.md.
[2026-08-22] Layer 3 gained a third, opt-in confirmation path — an agentic investigation loop gated by executable evidence and adversarial majority (enhancements E1, E2, E4): new packages/confirm/src/investigate-tools.ts defines six READ-ONLY tools (read_file, grep, find_definition, query_call_graph, list_routes, get_orm_model) plus a terminal submit_conclusion tool; new packages/confirm/src/investigate.ts's runInvestigation drives a multi-turn tool-calling loop against them (E1), bounded by a hard structural ceiling of 8 turns (independent of, and additive to, the existing A2 pre-call budget guard) that always resolves to "inconclusive" on exhaustion, never "confirmed_candidate" (fail-safe). A "confirmed_candidate" verdict is only ever a proposal: new packages/confirm/src/evidence.ts's gatherExecutableEvidence (E2) requires a real, pre-existing repo test the investigator named to actually FAIL when executed (a bounded, no-shell-interpolation vitest subprocess run of that one file) before the candidate can proceed — an unproven proposal (no test named, the test passes, or it can't be launched) is fail-safe-equivalent to "no evidence" and the finding stays in the Unconfirmed appendix. Evidence that clears E2 is then run through new packages/confirm/src/adversarial.ts's runAdversarialVerification (E4): up to four independent, sequential verifier calls with distinct lenses (exploitability, reachability, business_impact, and an explicitly adversarial refutation pass shown the other three verdicts), requiring a strict majority (3 of 4 by default) to promote; an errored or unparseable verifier call always counts as a reject vote. New packages/confirm/src/investigation-pipeline.ts's attemptInvestigationConfirmation is the sole integration point wiring E1→E2→E4 together, called from packages/confirm/src/confirm.ts ONLY when a finding cleared neither the existing deterministic static-proof path nor live DAST; a confirmed finding from this path gets a "static"-kind ProofArtifact (no new proof-artifact kind was added to packages/contracts — out of this change's file scope) whose argument embeds the investigator's rationale, the evidence summary, and the full verifier tally. OFF by default (ConfirmDeps.investigation.enabled) — deliberately not defaulted on, since unlike the existing single-call static veto this is a materially larger cost/latency profile per unconfirmed finding; wiring a production default and a real repo checkout (ConfirmInput.repoRoot, not yet populated by apps/worker/src/runners.ts) are documented, narrow follow-ups outside this change's file scope (packages/confirm/src/**, packages/appmap/src/** and packages/llm-gateway/src/** read-only). This is what gives idor and broken_access_control — previously 0% recall, with no static data-flow proof at all — a real confirmation path beyond live DAST alone. Every existing safety property is unchanged: the deterministic static-proof path (A21) and the live-DAST-confirms path (A7/A9) keep confirming exactly as before; the new path can only ADD a confirmation on top of full proof, never subtract from or bypass either existing path. New tests/confirm.investigation.test.ts (13 tests, all passing alongside the full existing suite). Updated docs/modules/confirmation.md.
[2026-08-22] Added threat-model derivation as a Layer 0.5 SUB-STEP of Layer 0, not a new pipeline layer (enhancement E6): new packages/appmap/src/threat-model.ts's buildDeterministicThreatModel always runs (no LLM required), deriving trust boundaries from route authState groupings and a per-category attack-surface plausibility rating grounded in the App Map's own taint-sink kinds, data-store kinds, and A18 route-to-model fan-out — for example an app with no deserialize-kind taint sink anywhere is rated zero-surface for insecure_deserialization/xxe, matching the audit's own worked example; deriveThreatModel additionally lets an LLM append abuse-case scenarios and priority-category hints on top of that baseline when a gateway is wired, validating every proposed route path/category against the real App Map/taxonomy first and never letting the model remove or downgrade a deterministic entry. The result is attached to the new optional AppMap.threatModel (packages/contracts/src/threat-model.ts, in-memory-only, same limitation as A18's Route.referencedModels — not yet persisted through packages/state-store), called from packages/appmap/src/build.ts right after auth-boundary labeling. New packages/discovery/src/threat-model-scope.ts's applyThreatModelScopeHints consumes threatModel.scopeHints to annotate + stable-sort Layer 1 candidates by priority — purely additive, never dropping a candidate or a category. A closed six-member LayerId union (packages/contracts/src/queue.ts) made a real new pipeline layer disproportionate for this step, so packages/orchestrator/src/controller.ts and packages/contracts/src/queue.ts are unchanged; packages/confirm/src/types.ts's ConfirmInput already carries the full AppMap, so appMap.threatModel is already reachable there with zero interface change, but Layer 3 consuming it is deliberately deferred (packages/confirm/src/** untouched). Also added the "threat_model" LLMPurpose (packages/contracts/src/llm.ts) and its structured-output JSON schema (packages/llm-gateway/src/structured-output.ts). Updated docs/modules/appmap.md, docs/modules/discovery.md.
[2026-08-22] Auth-boundary detection and interprocedural call graphs, across all three language analyzers (audit findings A20, A21): TypeScript route auth (packages/appmap/src/languages/typescript/routes.ts) now verifies an export's HOC-wrap (withAuth(handler), and the factory form requireRole(...)(handler)) as a real AST call shape rather than identifier-text presence, and resolves a root/src/ middleware.ts's config.matcher as Next's own global-middleware analog to Express/Fastify app.use(authMiddleware); Python (python/routes.ts) resolves Django 5.1+'s global LoginRequiredMiddleware; Java (java/extract.ts's new applySpringSecurityAuth, wired from java/index.ts) resolves Spring Security's declarative authorizeHttpRequests filter-chain lambda into Ant-pattern route matches — none of these override an existing more-specific per-route signal. Separately, Python and JVM each gained a real bounded interprocedural call graph (new packages/appmap/src/languages/{python,java}/callgraph.ts, mirroring typescript/callgraph.ts's exact output shape and bounded-hop philosophy) populating AppMap.taintFlows, previously always empty for both languages; Python reaches full parity with TS (same-file + one-hop relative-import resolution), Java reaches same-class + same-file cross-class resolution only (cross-file/same-package resolution was deliberately not attempted — see appmap.md). packages/confirm/src/static.ts's findSink/findSource gained a same-file-first, taintFlows-fallback lookup so Layer 3 static confirmation actually consumes this interprocedural data (previously taintFlows was read only by Layer 2's grounding.ts, even for TypeScript). Updated docs/modules/appmap.md, docs/modules/confirmation.md.
[2026-08-22] Correlation's same-file sanitizer heuristic and impact-score priors were hardened against the golden corpus (audit findings A25, A26): packages/correlation/src/grounding.ts's single loose keyword regex (matched against raw candidate evidence text in both directions) was replaced with two precise tiers — a call-expression-anchored check naming real sanitizer/validator/encoding APIs (DOMPurify, validator.js, zod, parseInt/Number/encodeURIComponent, etc.), checked against source/sink descriptions and the candidate's evidence snippet (real matched source code for every injection-class detector), plus a curated safe-marker vocabulary some App Map extractors deliberately author into a description, now scoped to source/sink descriptions only, never the uncontrolled evidence snippet, closing a concrete inversion bug (a bare "bind" or "no sanitization applied" substring could previously flip the verdict either way); packages/correlation/src/taxonomy.ts's CATEGORY_IMPACT_BASE.vulnerable_dependency was manually recalibrated from 0.5 to 0.7 after the golden corpus's real critical CVE-2021-44228 (Log4Shell) finding was found scoring below several high-severity findings of other categories purely due to the low prior — the golden corpus (n=44 labelled findings) is too thin for a real statistical fit, so this was a documented manual correction, not a regression; new tests/correlation.scoring-calibration.test.ts locks in the corrected cross-category severity ranking as a permanent regression guard. Updated docs/modules/correlation.md.
[2026-08-22] The strong content-aware scrubber is now a runtime gate on the audit-write path, not just a test-time certifier (audit finding A24): packages/state-store/src/audit.ts's PrismaAuditLogClient.append — the single real persistence chokepoint every caller across apps/api and apps/worker funnels through — now unconditionally runs both free-form text fields, metadata AND summary (summary was previously entirely unscrubbed), through @montr/security's redactSensitive before hashing/persisting, then re-verifies the redacted output with that package's independent findLogViolations as defense in depth; a residual violation after redaction triggers a hard anomaly-marker fallback rather than throwing, so a safety-relevant write (e.g. a kill-switch event) is redacted-and-persisted, never dropped (fail-safe, not fail-closed — chosen because the audit trail's job is to be trustworthy AND complete). packages/contracts/src/audit.ts's AuditEventSchema doc comment now points at this enforcement point instead of asserting scrubbing only via a comment; apps/api/src/audit.ts's recordAudit pre-scrub (via @montr/telemetry's scrubValue) is left in place as a harmless, non-authoritative defense-in-depth layer. @montr/telemetry/src/scrubber.ts (the separate, already-wired hot-path stdout/OTel scrubber) is untouched — the two stay intentionally independent per that module's own doc comment. New packages/state-store/src/audit.test.ts proves the gate at the actual persistence boundary (not just the scrubber functions in isolation). Updated docs/api/database.md.
[2026-08-22] App Map route discovery now covers Express and Fastify, not just Next.js: new packages/appmap/src/languages/typescript/express.ts and fastify.ts add real ts-morph-based route extractors (previously both frameworks were detected from package.json but produced zero routes), wired into languages/typescript/index.ts's framework-gated dispatch and verified against this repo's own apps/api Fastify routes (audit finding A17 — the narrow route-extraction gap only; the broader A17 breadth is tracked as enhancement E16). Also added route-to-model linking: Route gained an optional referencedModels field (packages/contracts/src/appmap.ts) populated by new packages/appmap/src/languages/typescript/route-models.ts, which resolves a direct prisma.<model>.<method>(...) call in a route handler plus one hop through a locally declared helper function (audit finding A18); not yet persisted through packages/state-store (documented limitation, not in this change's scope). Updated docs/modules/appmap.md.
[2026-08-22] Closed the "built but unwired" pattern across five real subsystems that had zero production callers (audit finding A10): apps/worker/src/main.ts and apps/api/src/production-deps.ts now resolve the field-encryption key via packages/config/src/key-source.ts's resolveFieldEncryptionKey (env/file/Vault) instead of reading security.fieldEncryptionKeyRef directly, so security.keySource=vault actually fetches from HashiCorp Vault in production; all five real LLM prompt call sites (packages/appmap/src/llm.ts, packages/discovery/src/triage.ts, packages/correlation/src/correlate.ts, packages/confirm/src/static.ts's runLlmReview, packages/fix/src/generate.ts) now resolve their system prompt through gateway.resolvePrompt(name, hardcodedFallback, {clientId}) instead of a hardcoded constant, so a DB-versioned PromptVersion actually overrides it when active (packages/contracts/src/llm.ts's LLMGateway interface gained an optional resolvePrompt method so callers typed only against the base contract compile); the §15 false-positive feedback loop is now wired — new packages/state-store/src/repositories.ts FalsePositiveMarkRepositoryImpl reads prior finding.marked_false_positive audit events back out per client, and apps/worker/src/runners.ts's new loadFpTuning() passes them as fpTuning into both correlate() and confirmFindings(), so a repeat of the same (category, file, line) on a later scan is demoted/skipped instead of re-promoted; gateway streaming was assessed and deliberately left unconsumed by any layer (every current call is a small single-shot metadata request with no streaming UI) but is now proven working end-to-end via a new real-adapter integration test (packages/llm-gateway/src/streaming.integration.test.ts); new scripts/check-unwired-seams.mjs (pnpm run check:unwired-seams, wired into CI) grep-asserts each of these seams keeps a real non-test caller outside its defining package, so this class of regression fails the build going forward. Updated docs/modules/llm-gateway.md, docs/infra/environment.md, docs/state/server-state.md, docs/infra/testing.md.
[2026-08-22] Fix generation no longer asks the model to rewrite whole files (audit finding A14): packages/fix/src/generate.ts's prompt now asks for a small, line-anchored edit list of 1-based startLine/endLine/replacement entries (new packages/fix/src/edits.ts parses, validates, and applies it) instead of the full fixedSource file body, which silently truncated and failed JSON parsing on any file past roughly 1,500 lines with no error, metric, or retry; the output cap rose from 2048 to 8192 tokens as defense in depth alongside the format change; an unparseable model response or a structurally invalid edit list is now counted via getMetrics().recordError (codes fix_generation.llm_response_unparseable and fix_generation.llm_edits_invalid) and logged with the model's stop reason, instead of a silent null-and-degrade to the mechanical strategy or advisory path. Updated docs/modules/fix-generation.md.
[2026-08-22] Live DAST now covers eleven categories instead of four, and the NoSQL probe fires a real Mongo operator payload instead of a reused SQL string (audit finding A9): packages/confirm/src/live.ts's LIVE_CONFIRMABLE_CATEGORIES gained ssrf, idor, broken_access_control, path_traversal, command_injection, xxe, and insecure_deserialization, each with a baseline+payload probe pair and a fixed marker/status oracle; nosql_injection now sends a field[$ne]= bracket-notation operator payload; xxe/insecure_deserialization are POST-based (the Probe/LiveHttpRequest body was already plumbed through undici but never used) and are gated by the existing mutating-request blast-radius cap, which defaults to zero. Verified (audit finding A7) that a successful live-DAST probe already promotes a finding to confirmed:true with the transcript attached as its ProofArtifact — the demote-only clamp the audit found is specific to the static-confirmation LLM veto in packages/confirm/src/static.ts and does not apply to the deterministic live-DAST oracle; idor and broken_access_control have no static data-flow proof at all, so live DAST is their only route to confirmed. Updated docs/modules/confirmation.md.
[2026-08-22] SCA reachability is now call-granularity on TypeScript/JavaScript instead of package-import presence (audit finding A12): packages/discovery/src/detectors/sca.ts's new collectCalledPackages runs a real ts-morph AST scan to check whether a binding imported from a vulnerable package is actually invoked (called, constructed, or JSX-rendered) anywhere, not merely present in an import/require statement; the old collectImportedPackages regex scan remains only as a fallback when nothing parses as TS/JS. This does not reuse packages/appmap/src/languages/typescript/callgraph.ts's taint-flow resolver (scanTaintFlows), which explicitly excludes bare/package specifiers by design and so cannot answer this question; no new Layer 0→Layer 1 data dependency was introduced (Layer 1 already receives the App Map, unused by this feature). Scoped to TS/JS only — Python/Java are not yet resolved by this detector at all; updated docs/modules/discovery.md.
[2026-08-22] Recall is now a first-class, published metric alongside precision/FP-rate on the golden-corpus gate (audit finding A6): packages/qa/src/report.ts's formatCorpusScore/toJsonReport always render recall next to fpRate/precision and print an explicit sampleSizeCaveat (new export, floor 30 confirmed findings) while the confirmed sample stays small — today's real measurement is TP=11, so the caveat fires on every qa:corpus run; the aggregate recallMin gate already existed in packages/qa/src/baseline.ts evaluateBaseline and corpus/baseline.json (recallMin: 0.2 plus four perCategory floors), now proven by dedicated tests in tests/qa.baseline.test.ts and tests/qa.report.test.ts; DOD.md item 4 and corpus/baseline.json's new $statisticalCaveat state plainly that 0% FP-rate at n=11 is not yet statistically meaningful; updated docs/infra/testing.md.
[2026-08-22] Real-time cost metering now actually accumulates in production: packages/llm-gateway/src/gateway.ts's account() records every completed call's usage into the per-scan CostMeter resolved from budgetRegistry (the same instance orchestrator/controller.ts registers, hands to layers as ctx.costMeter, and reads in enforceBudget) instead of only the unused constructor-level costMeter option apps/worker/src/main.ts never set (audit finding A32, follow-on to A2); updated docs/modules/llm-gateway.md, docs/modules/orchestration.md.
[2026-08-22] Scan resumability is now reachable in production: POST /api/v1/scans/:id/resume route, apps/worker boot-time reconciliation (reconcile.ts) for scans stuck at status running, BullMQ Worker lock/stall tuning, and FindingRepo.bulkCreate skipDuplicates fix for redelivered layer jobs (A3); also closed four confirmed-404 web routes — GET /scans/:id/progress, GET /scans/:id/appmap, cross-scan GET /pull-requests, and a scan-scoped POST /scans/:id/dast/authorize wrapper around the real target-based DAST flow (A5); updated docs/api/route-handlers.md, docs/modules/orchestration.md, docs/state/server-state.md, docs/auth/authorization.md.
[2026-08-22] LLM gateway now enforces a pre-call budget guard (estimated cost checked against the live CostMeter/BudgetPolicy before dispatch, refusing a single over-budget call mid-layer instead of only between layers, audit finding A2); new packages/cost-meter/src/registry.ts BudgetRegistry wired through apps/worker/src/main.ts; updated docs/modules/llm-gateway.md, docs/modules/orchestration.md.
[2026-08-22] SAST is now a required detector and air-gapped installs can wire it to a local Semgrep ruleset directory (discovery.rulesetsDir / MONTR_DISCOVERY_RULESETS_DIR, audit finding A4); updated docs/modules/discovery.md, docs/infra/environment.md, docs/infra/deployment.md.
[2026-08-22] Cost meter fails closed on unknown model ids instead of pricing at $0 (A1); rate card in packages/contracts/src/llm.ts gained claude-opus-5 and claude-fable-5; docs/modules/llm-gateway.md updated.
[2026-08-22] Model matrix refreshed and the gateway now supports a fallback-model cascade (audit finding A11): packages/contracts/src/llm.ts's RECOMMENDED_MODEL_MATRIX.confirmation is now claude-opus-5 (current flagship; was claude-opus-4-8, which stays valid/priced) and .triage is now the undated claude-haiku-4-5 (was claude-haiku-4-5-20251001 — MODEL_COST_RATES intentionally keeps that dated entry, since normalizeModelId's -YYYYMMDD stripping already prices either form identically); new config.llm.fallbackModel (packages/config/src/schema.ts, env MONTR_LLM_FALLBACK_MODEL) lets packages/llm-gateway/src/gateway.ts's complete() retry the same request against one alternate model — exactly once, bounded, never infinite — via retry.ts's new withRetryAndFallback, only after the primary model's full retry policy is exhausted; each fallback attempt logs llm.model_fallback and increments the llm_gateway.model_fallback error metric. Updated docs/modules/llm-gateway.md, docs/infra/environment.md.
[2026-08-22] Built the in-repo-buildable half of the autonomous trigger surface (audit finding A15): new apps/cli (@montr/cli, bin montr) drives the real apps/api HTTP API to create/poll a full- or diff-mode scan from a local checkout and exits non-zero on a confirmed finding at/above a configurable --fail-on severity, for CI gating; new POST /api/v1/webhooks/scan-trigger (apps/api/src/routes/webhooks.ts) verifies a GitHub-compatible X-Hub-Signature-256 HMAC (apps/api/src/auth/webhook-signature.ts) over the raw body and then creates+starts a scan through the same orchestrator path POST /scans uses, disabled by default (503) until MONTR_WEBHOOK_SECRET and MONTR_WEBHOOK_OPERATOR_EMAIL are set; packages/report/src/vcs.ts gained postGitHubComment (additive, reuses the existing lazy @octokit/rest import) so the webhook flow can best-effort post a single PR acknowledgement comment. A full GitHub App (manifest registration, OAuth installation, marketplace listing) remains explicitly out of scope; a webhook-triggered scan posting its CONFIRMED findings back to the PR once complete is deferred (needs apps/worker's Layer 5 to persist/read the originating PR reference). Updated docs/architecture/folder-structure.md, docs/api/route-handlers.md, docs/api/external-services.md, docs/infra/environment.md, CLAUDE.md, AGENTS.md.

[2026-08-22] LLM gateway plumbing for tool use, extended thinking, prompt caching, and real structured outputs (audit findings A8, A13; scoped gateway plumbing, not the agentic loop itself — that is tracked as E1): all four adapters (packages/llm-gateway/src/adapters/{anthropic,bedrock,vertex,azure}.ts) now forward request.tools and parse a tool_use/functionCall/tool_calls response into the new additive LLMResponse.toolCalls; request.effort (new EffortSchema) drives Anthropic/Bedrock's output_config.effort + adaptive thinking (Haiku-tier models excluded — they 400 on it); Anthropic/Bedrock's system prompt now carries cache_control:{type:"ephemeral"}; new packages/llm-gateway/src/structured-output.ts resolves a real per-purpose JSON schema (derived from each of the five real call sites' own already-existing parsed-JSON shapes, with zero changes to those five packages) so Anthropic/Bedrock/Vertex/Azure all enforce real structured outputs instead of prose-JSON + try/catch; a new llm_gateway.response_parse_failure metric (packages/llm-gateway/src/gateway.ts) fires centrally at the gateway boundary on any JSON.parse failure, independent of each caller's own fallback; correlation's and confirmation's maxTokens raised (512→2048, 1024→4096) for output-reasoning headroom. Deferred: App Map prompt-caching (no call site passes one without a larger refactor), non-Anthropic effort mapping, and a tool-execution loop (E1). Found-while-working fix: anthropicRejectsSampling now also matches claude-opus-5 (previously only opus-4-7/4-8), which had silently gone stale when A11 made opus-5 the confirmation-tier default. Updated docs/modules/llm-gateway.md.

[2026-08-22] The model-variance harness can now actually detect variance (audit finding A23): its only prior caller, packages/qa/src/synthetic.ts's perfectConfirmedForRepo, ignored the model parameter and echoed ground truth, so every model scored an identical, tautological 100% and no accuracy cliff could ever surface. New packages/qa/src/real-confirmation-scanner.ts's realConfirmedForRepo runs the REAL @montr/confirm static-confirmation engine (packages/confirm's confirmFindings, unmodified) against the two @montr/fixtures sample repos (the only repos with a hand-built AppMap + ProbableFinding[] fixture today — not the full 16-repo golden corpus, which corpus/baseline.json's minReposScored:14 assumes; a new REAL_VARIANCE_BASELINE/realVarianceCorpus pair scopes the gate to what is actually measured instead), once per model in the fake gateway's listModels() (RECOMMENDED_MODEL_MATRIX's triage/default/confirmation tiers); gatewayForModel makes the confirmation-tier LLM veto (golden rule #4's fail-safe demotion) when the model is below the confirmation floor and confirm otherwise — the existing createFakeLlmGateway({ cannedByPurpose }) convention, just picked per model — so a genuine accuracy cliff now produces a real, differing recall per model row (verified today: the below-floor triage model scores 0% recall vs 66.7% for the default/confirmation-tier models) instead of an identical number. packages/qa/src/cli.ts's qa:variance now runs this REAL scanner by default (--variance-selfcheck opts back into the old tautological echo as a plumbing-only smoke test, mirroring qa:corpus vs qa:corpus:selfcheck), and .github/workflows/ci.yml's golden-corpus job runs it as a new step, fully offline (fake gateway, no live provider credentials or spend). Updated docs/infra/testing.md.

[2026-08-22] Added opt-in per-tenant BullMQ queue isolation, OFF by default (audit finding A27): packages/contracts/src/queue.ts's new resolveQueueName(layer, clientId, tenantIsolation) derives a per-client queue name (montr.layer0.<clientId>) instead of the shared QUEUE_NAMES[layer] when enabled; new config.queue (packages/config/src/schema.ts's QueueConfigSchema — perTenantIsolation false / tenantIds [] by default, env MONTR_QUEUE_PER_TENANT_ISOLATION / MONTR_QUEUE_TENANT_IDS) is read by packages/orchestrator/src/bullmq-scheduler.ts's new deriveTenantSchedulerOptions, called identically by apps/worker (consumer: BullMqJobScheduler now fans out one BullMQ Queue + Worker per (layer, tenantId) pair, each independently polling Redis so one tenant's backlog cannot delay another's newly-queued job) and apps/api's produce-only EnqueueOnlyScheduler (producer), so both processes always agree on queue names. This repo ships plain (non-Pro) BullMQ with no group-based fair-scheduling primitive, so N-queues-per-tenant was the deliberate choice over a lighter-weight mechanism; not recommended past a modest tenant count per worker process. Updated docs/state/server-state.md, docs/modules/orchestration.md, docs/infra/environment.md.

[2026-08-22] Real token counting and Batch API capability added to the LLM gateway (audit findings A19, A31): MontrLlmGateway.countTokens() calls the Anthropic adapter's real messages.countTokens endpoint (packages/llm-gateway/src/adapters/anthropic.ts, mapping.ts's new buildAnthropicCountTokensBody), falling back to the fast chars/4 heuristic for Bedrock/Vertex/Azure or on failure; the A2 pre-call budget guard deliberately keeps using the heuristic (estimateTokens) rather than countTokens, to avoid adding a network round-trip to every gated call — documented as a deliberate latency/accuracy tradeoff. packages/cost-meter/src/estimate.ts's per-unit constants are now documented in-code as uncalibrated heuristics (no real captured prompt-size data exists in this repo); a new packages/llm-gateway/src/estimate-variance.test.ts validates PRD §19's ±15% estimate-vs-actual bound against a representative synthetic scan built from each real call site's own payload shape, replacing DOD.md item 6's ≈$0 concession with a real (if not ±15%-precise — measured ~5x, safe-direction) comparison. Batch API: MontrLlmGateway gained submitBatch()/pollBatch()/getBatchResults() (new packages/llm-gateway/src/batch.ts types), backed only by AnthropicAdapter's real messages.batches.create/retrieve/results (other providers throw NotImplementedError); packages/cost-meter/src/pricing.ts's priceUsageUsd gained an opts.batch flag applying the 50% Batch API discount (BATCH_DISCOUNT_MULTIPLIER), and MeterEntry/CostMeter.record() carry it through. Not wired into any pipeline layer — the orchestrator's FSM assumes synchronous layer completion, incompatible with a job that may resolve up to 24h later without further resumability work, and this change's file scope excluded the layer packages (packages/fix, packages/confirm, etc.) that would need to change to consume it; built and fully tested (mirroring how A10 handled gateway streaming). Confirmed prompt-cache usage (A31 item 1) is correctly priced end-to-end via a new real-adapter integration test (packages/llm-gateway/src/cache-pricing.integration.test.ts) — no wiring gap found. The App Map second cache point (A31 item 2) remains deferred: a concrete minimal wiring was designed (fold a stable per-scan App-Map summary into confirm/src/static.ts's system prompt) but not implemented, since packages/confirm was outside this change's scoped file list. Updated docs/modules/llm-gateway.md.

[2026-08-22] Dynamic model-tier escalation added to the LLM gateway (enhancement E9): new packages/llm-gateway/src/escalation.ts's evaluateConfidence judges a completed response low-confidence via, in priority order, a self-reported JSON confidence field (forward-compatible, nothing emits it yet), the model's stopReason === "refusal", or a JSON-mode response that fails JSON.parse — none of which require any prompt change in packages/discovery, packages/correlation, or packages/confirm. packages/llm-gateway/src/gateway.ts's complete() gained an OFF-by-default CreateGatewayOptions.escalation (mirrors the budgetRegistry/fallbackModel constructor-opt-in pattern, A2/A11): when enabled, a request that resolved via an explicit request.tier (never a pinned request.model) and comes back low-confidence is retried against the next tier up (models.ts's new nextTier over the existing triage→default→confirmation ladder), bounded by escalation.maxEscalations (default 2) and by nextTier itself refusing to go past confirmation; every escalated attempt still gets the full retry policy, model-fallback cascade, and pre-call budget guard, unchanged. LLMResponse gained an additive confidence field (packages/contracts/src/llm.ts) populated only when escalation is enabled. This is a DIFFERENT mechanism from A11's fallback cascade — fallback reacts to FAILURE and swaps MODEL, escalation reacts to a SUCCESSFUL low-confidence response and walks TIER. Deliberately left unconsumed by any layer: flipping it on for triage (packages/discovery/src/triage.ts's existing `{tier:"triage"}` request already qualifies with zero changes) requires an apps/worker/src/main.ts gateway-construction change outside this change's scoped file list — built and fully tested standalone (packages/llm-gateway/src/escalation.test.ts), mirroring how A10/A31 handled gateway streaming and the Batch API. Updated docs/modules/llm-gateway.md.

[2026-08-22] Live DAST's fixed single-payload probe is now backed by a bounded ADAPTIVE exploit agent (enhancement E3), layered alongside the existing 11-category fixed path rather than replacing it: packages/confirm/src/live.ts's confirmLive still runs craftProbes/oracle first for every live-eligible finding (fast, zero LLM cost, resolves the large majority cleanly); only when that fixed attempt's verdict is AMBIGUOUS (new isAmbiguous — a raw 5xx, a generic error/stack-trace leak, an SSRF filter-block signal, a redirect that fired but missed the exact target, or a reflected-but-transformed XSS fragment) does the new runAdaptiveLoop engage deps.llm (confirmation tier, low effort) to pick ONE variant id from a small, hardcoded, per-category PAYLOAD_VARIANTS catalog (alternate SQL comment styles/UNION probe/time-based blind; NoSQL $gt/$regex/$exists; decimal/IPv6-mapped/alt-hostname SSRF encodings; XSS attribute-breakout/<img>/<svg> vectors; open-redirect protocol-relative/backslash/userinfo-@ bypasses; encoded/double-encoded/null-byte path-traversal; pipe/backtick/$() command-injection; alternate XXE target file/parameter-entity; alternate deserialization type markers; IDOR/broken_access_control id and header-stripping variants) — a bounded, reviewable mutation space, never a free-form model-authored payload; responseSchema constrains the reply to the exact offered-id enum and the caller re-validates it before ever building a request. Every adaptive probe passes through the SAME ScopeGuard.assertProbeAllowed gate as the fixed path (allowlist, production block, kill switch, rate/blast-radius caps, egress guard) — guard.ts is untouched and unbypassed. The loop is capped at MAX_ADAPTIVE_ROUNDS = 3, independent of the gateway's own A2 pre-call budget guard (defense in depth); the kill switch is re-checked at the top of every round AND immediately after every LLM call returns (not just once at the start), so a mid-investigation activation halts probing before the next request leaves the process — proven by a dedicated test. oracle() was changed minimally (score against the most recent payload/adaptive_payload-role probe via reverse-find, not always the first) so a later adaptive round can supersede the fixed payload's verdict; a no-op for the fixed-only path. Gated strictly on deps.llm being supplied, so every pre-existing fixed-path test is byte-for-byte unaffected. Updated docs/modules/confirmation.md.

[2026-08-22] E10/E12/E15 close-out pass over three mostly-done enhancement items. E10 (gateway modernization): re-verified all done sub-items hold, and re-examined the ONE remaining gap ("streaming wired through to the console") now that E1's agentic investigation loop exists — concluded raw token streaming is still the wrong shape (would leak prompt content past golden rule #1), but the RIGHT coarse mechanism (packages/orchestrator's LayerRunContext.emitProgress → EventBus → apps/api's GET /scans/:id/progress, A5) is already real on the console side (apps/web/src/components/layer-progress.tsx already renders a live `phase` string per active layer with zero further changes needed) and has zero real producers anywhere in the pipeline today — left honestly deferred as it requires a call site in packages/confirm/apps/worker outside this change's scope; no apps/web changes made. E12 (autonomy surface, delta-only reporting): scheduled scanning itself was already fully real (apps/api/src/routes/schedules.ts CRUD + apps/worker/src/scheduling's real BullMQ repeatable-job dispatch, `scheduledBudgetPolicy`/human-gate preserved) but always produced a standalone full report with no comparison to the prior run; new `GET /schedules/:id/delta` (apps/api/src/routes/schedules.ts) finds the two most recent completed/partial scheduler-triggered scans for a schedule's repo (matched via `Scan.operator === "scan-scheduler"`, the marker apps/worker's scan-scheduler.ts already stamps — no new Scan field needed) and diffs their confirmed findings by (category, file, line) via the new pure `computeFindingsDelta`, returning newFindings/resolvedCount. Known limitation: without a `Scan.scheduleId` field (out of scope to add), two schedules for the same repo would be indistinguishable — documented, not silent. E15 (eval-driven prompt optimization): packages/llm-gateway/src/prompts.ts's `PromptVersionSource` gained an optional `listVersions` method plus a new `resolvePromptVersionTemplate` (resolve an EXACT version, not just whichever is active) and `MontrLlmGateway.resolvePromptVersion`; new packages/llm-gateway/src/prompt-version-registry.ts's `InMemoryPromptVersionRegistry` is a Map-backed `PromptVersionSource` (same name/version/isActive shape as state-store's real Postgres-backed repository) for offline eval/tests. New packages/qa/src/prompt-eval.ts's `evaluatePromptCandidate` connects this registry to the golden-corpus scorer: A/Bs a candidate prompt version against the active one on the REAL @montr/confirm engine (via a refactored, additive `confirmedForRepoWithGateway` extracted from A23's real-confirmation-scanner.ts), scored with the same `scoreScanResults`/`evaluateBaseline` the CI gate uses, gated by a relative regression floor (candidate must not score worse than active). packages/qa gained a new @montr/llm-gateway dependency. Updated docs/modules/llm-gateway.md, docs/infra/testing.md.

[2026-08-22] Added a semantic codebase index — AST-chunked, embedded, pgvector-backed retrieval built alongside the App Map (enhancement E5): new packages/semantic-index chunks a repo into function/method/class-sized units (reusing packages/appmap's ts-morph/web-tree-sitter parser loaders, additively re-exported from packages/appmap/src/index.ts for this purpose), embeds them via a new packages/llm-gateway/src/embeddings.ts EmbeddingProviderAdapter (implemented for azure only today — Anthropic has no embeddings endpoint, Bedrock/Vertex are documented follow-ups since their embedding APIs are a different wire protocol from those providers' existing chat adapters), and stores/retrieves vectors through a new packages/state-store/prisma/schema.prisma CodeChunk model (Unsupported("vector(1536)") pgvector column, migration 5_semantic_code_index, new packages/state-store/src/code-chunk.ts's PrismaCodeChunkRepository using $queryRaw/$executeRaw since Prisma Client has no typed accessor for the vector column) — deliberately kept out of the main StateStore aggregate since nothing consumes it yet. Two top-level entry points, buildSemanticIndex and querySemanticIndex (packages/semantic-index/src/build.ts), are complete and tested but NOT wired into packages/correlation or packages/confirm (both were off-limits — other concurrent work was touching them); scripts/check-unwired-seams.mjs records both the embeddings adapter and this package's entry points as known, documented exceptions. The pgvector Postgres extension is also NOT yet present in deploy/docker/docker-compose.yml's postgres:16-alpine image or deploy/helm's default postgres.image — documented as a required follow-up, not applied to deploy/ in this change. New docs/modules/semantic-index.md. Updated docs/api/database.md, docs/modules/appmap.md, docs/modules/llm-gateway.md, docs/architecture/folder-structure.md, docs/maintenance.md.

[2026-08-22] E10 closed out — the LLM gateway's real progress-streaming path finally has a producer: packages/confirm/src/types.ts's ConfirmDeps gained an additive, optional emitProgress?: (phase, pct, message?) => void field mirroring packages/orchestrator's LayerRunContext.emitProgress signature exactly; apps/worker/src/runners.ts's layer3 runner threads ctx.emitProgress straight into it (the identical pass-through pattern already used for signal/logger); packages/confirm/src/investigate.ts's E1 agentic investigation loop now calls it once per tool-call turn with phase "investigating", a pct estimated from turn/turnsCap, and a message derived from the ACTUAL tool call that turn made (e.g. "Reading app/api/orders/[id]/route.ts...", "Checking the call graph for ...", "Submitting investigation verdict (confirmed_candidate)..."), wrapped in a try/catch so a broken consumer can never break the investigation. Purely additive and OFF-shaped by default: omitting emitProgress (every prior caller/test) leaves the investigation loop's outcome byte-identical to before this change — proven by a new regression test in tests/confirm.investigation.test.ts alongside a test asserting the once-per-turn call contract. Reaches apps/web/src/components/layer-progress.tsx with zero console changes, since that component already renders a live phase/pct string per active layer (A5). All 7 E10 sub-items are now done. Updated docs/modules/confirmation.md, docs/modules/orchestration.md, docs/modules/llm-gateway.md.

[2026-08-22] Added an external benchmark harness against the real OWASP Benchmark for Java, closing the "no external verification" gap (enhancement E14, audit finding A29): new packages/qa/src/owasp-benchmark.ts parses OWASP Benchmark's OWN expectedresults-*.csv ground-truth format and scores a tool's output with OWASP Benchmark's OWN methodology (TP/FP/TN/FN per test case -> TPR/FPR -> the project's own headline "Benchmark score" = TPR-FPR), deliberately never touching corpus/'s internal scorer or corpus/baseline.json gate — an external, independently-curated dataset must be scored on its own terms, not folded into a number this product's own team already controls. New corpus/owasp-benchmark/ vendors a real, pinned-commit (ba2e3f9a29fa3bde6a5c073d679393c9b94f025f), honestly-labelled EXCERPT (24 of 2,740 test files, 0.9%) of the real upstream project — GPL-2.0, not Apache-2.0 as E14's task brief assumed, corrected in corpus/owasp-benchmark/README.md — covering the 6 of OWASP Benchmark's 11 categories with a real detector in this product's taxonomy (sqli/cmdi/pathtraver/xss/securecookie/crypto|hash|weakrand); the other 5 (ldapi/xpathi/trustbound) have no detector for any supported language and are honestly excluded, never silently scored as zero. New scripts/benchmark-owasp.mjs runs the REAL apps/worker L0-L3 pipeline against the vendored subset (mirrors scripts/corpus-scan.mjs's pattern exactly); scripts/benchmark-semgrep.mjs runs raw Semgrep directly against the same subset with this product's own primary OWASP ruleset (p/owasp-top-ten) for a real competitor head-to-head; scripts/benchmark-codeql.mjs checks for the CodeQL CLI (not installed in this environment) and documents the exact commands to run it elsewhere rather than fabricating a score. A REAL measured run surfaced a genuine, previously-undiscovered production bug rather than a benign scaffold gap: packages/discovery/src/rulesets/java/index.ts's semgrepRulesets: ["p/java", "p/spring"] silently zeroes ALL SAST candidates for every JVM app today, because the p/spring Semgrep Registry pack currently 404s and Semgrep aborts its ENTIRE run (not just that one pack) when any --config target fails to resolve — verified end-to-end (swapping in a p/java-only runner via the already-supported LayerRunnerOptions.semgrep injection point raised this subset's confirmed findings from 0 to 5); A4's required-detector fail-loud guard does not catch this failure mode since Semgrep still returns a well-formed (if empty) JSON body. Not fixed here — packages/discovery/src/** was out of this change's file scope — documented as a new, real, higher-priority finding in docs/infra/testing.md and corpus/owasp-benchmark/README.md for the next audit pass. The real, un-worked-around measured result on the 24-case subset: montr-secure TPR=0.0%/FPR=0.0%/precision=100.0%/score=0.0 vs raw Semgrep TPR=83.3%/FPR=33.3%/precision=88.2%/score=50.0 — reported exactly as measured, not projected. Updated docs/infra/testing.md.

[2026-08-22] Layer 4's proof-of-fix upgraded to a real ephemeral-container exploit replay, closing A22 (enhancement E13): new packages/fix/src/container-harness.ts's startEphemeralContainer builds/starts a genuinely throwaway Docker container for a target application checkout — a target with its own Dockerfile is built as-is, a target with a package.json "start"/"dev" script but no Dockerfile gets a minimal synthesized node:20-alpine Dockerfile, anything else is out of scope for this pass and fails closed with a clear error (no Python/JVM target containerization attempted); the container is attached only to a freshly-created bridge network with IP masquerade explicitly disabled (docker network create --opt com.docker.network.bridge.enable_ip_masquerade=false — verified empirically against a real Docker engine that this keeps loopback-bound published-port inbound reachable while the container's own outbound egress to the real internet or any other container/network on the host fails closed; a plain --internal network was tried first and rejected because it also breaks published-port inbound on this engine), and every docker invocation shares one timeout budget with guaranteed teardown on any failure path. New packages/fix/src/exploit-replay.ts reuses (read-only, no live.ts changes) a CONFIRMED finding's own live-DAST HttpExchange transcript — pickReplayExchange takes the decisive last entry, rewriteRequestForContainer re-targets it at the container, exploitEvidenceMatches judges success by a real comparison against the originally recorded evidence (shared body substring, or exact status match when no usable body was recorded). New packages/fix/src/container-validate.ts's validatePatchWithContainerReplay is a strict, additive upgrade over patch.ts's existing vitest-subprocess validatePatch (same PatchValidation shape plus an additive containerReplay evidence block): for a live-DAST-confirmed finding with a real target checkout supplied, it builds one container from the pre-patch source (must reproduce the recorded exploit) and a second from the post-patch source (must no longer reproduce it), deriving failsPrePatch/passesPostPatch from this real runtime outcome; for a static-proof-only or investigation-path confirmation, an empty transcript, a missing target checkout, or a genuine container-infrastructure failure, it falls back to the existing vitest mechanism unchanged (coverage for non-live-evidence findings is never broken) with the container attempt's error attached and visible when relevant. Wired OFF by default via generate.ts's new FixGenerationContext.containerProof (mirrors the ConfirmDeps.investigation.enabled / escalation constructor-opt-in convention); packages/contracts/src/fix.ts gained an additive ProofOfFixTestSchema.containerReplay field (reusing findings.ts's HttpExchangeSchema) so the FULL replay evidence persists on the Fix row itself, while the fix.generated audit event gets only a lean, transcript-free summary (golden rule #1). containerProof.targetRepoDir is not yet populated by apps/worker/src/runners.ts — the same documented not-yet-wired pattern as ConfirmInput.repoRoot for E1's investigation loop; fully built and proven against a REAL (non-mocked) Docker container in tests/fix.container-replay.test.ts. Updated docs/modules/fix-generation.md.

[2026-08-22] Broadened Layer 1 target coverage with real, tested IaC scanning and a real CycloneDX SBOM export, plus a start on supply-chain risk (enhancement E16, first three of its six sub-areas): new packages/discovery/src/detectors/iac.ts (detectIac) adds Dockerfile/Kubernetes/Terraform structural detectors (packages/discovery/src/detectors/iac/{dockerfile,kubernetes,terraform}.ts) as a fourth concurrent Layer 1 fan-out member alongside SAST/secrets/SCA/AI-security, plus an optional Semgrep pass against the real p/dockerfile/p/kubernetes/p/terraform registry packs (reusing sast.ts's parseSemgrepJson/candidateFromSemgrep unmodified) that gracefully degrades rather than hard-failing like detectSast — see docs/modules/discovery.md for the full detector list and its Terraform block-scoping / Dockerfile line-continuation scope notes. A new insecure_configuration Category (packages/contracts/src/compliance.ts, CWE-16/A05:2021, migration 8_insecure_configuration_category mirroring migration 7's prompt_injection precedent exactly) covers pure IaC misconfiguration; hardcoded_secret/broken_access_control/weak_crypto are reused directly for the checks that already fit them, keeping the taxonomy ripple to the same six files the prior prompt_injection addition touched. New packages/discovery/src/sbom.ts's buildDependencyInventory resolves the FULL dependency tree (not just SCA's vulnerable subset) with real call-site reachability (A12) as plain data, consumed with zero adapter by new packages/report/src/exports/cyclonedx.ts's buildCycloneDxSbom/renderCycloneDxSbom (new "cyclonedx" ExportFormat, real PURLs + numeric CWEs + affects[] refs) — proven end to end in tests/sbom.cyclonedx-e2e.test.ts; not yet wired to a production route/CLI (documented, same posture as E5/E9's unwired-but-tested library shipping). New packages/discovery/src/detectors/supply-chain.ts adds real offline Levenshtein-distance typosquat detection against a curated popular-package list, install-script risk scanning (the project's own package.json always, a resolved dependency's node_modules/<pkg>/package.json only when node_modules is actually present on disk — a true absence for a plain git checkout, not a swallowed error, since node_modules stays excluded from the general file walk by design), and malicious-package heuristics scoped honestly to what's checkable OFFLINE (non-ASCII/homoglyph package names, non-standard package-lock.json registry resolution) — "recently published + low downloads" is explicit documented future work since it needs live npm registry metadata this on-prem product does not fetch. OpenAPI/API-spec drift review, container image scanning, and cloud posture (E16's remaining three sub-areas) were not attempted — deferred per the audit's own stated priority order, honestly left unchecked in docs/plan/26-08-22-tasks-ai-depth.md. New fixtures under packages/fixtures/sample-repos/{iac-samples,supply-chain-samples}/{vulnerable,clean}. Updated docs/modules/discovery.md.

[2026-08-22] Fixed a P0 discovered while implementing E14 (audit finding A33): packages/discovery/src/rulesets/java/index.ts's semgrepRulesets used to be ["p/java", "p/spring"]; the p/spring Semgrep Registry pack is dead (verified 404 directly against Semgrep's own raw config-fetch endpoint, https://semgrep.dev/c/p/spring, and against the Registry's rulesets listing API, which has no Spring-named pack today), and real Semgrep aborts its ENTIRE --json invocation when even one --config target fails to resolve, not just the failing pack — so every JVM scan silently produced zero SAST candidates, with no error surfaced anywhere. Fix 1 (correctness): the Java ruleset now ships p/java alone — no current Registry pack is Spring-specific, so nothing was swapped in blind. Fix 2 (structural, applies to every language's ruleset list, not just Java's): packages/discovery/src/detectors/sast.ts's detectSast, in the hosted-registry-pack path (the air-gapped rulesetsDir path from A4 is untouched — a single local directory, not independently-resolved pack ids), now resolves each --config entry in its OWN Semgrep subprocess via new runRegistryRulesets and merges + dedupes the results, instead of one shared invocation across every pack; a pack that fails to resolve gets a ctx.warn + a sast.ruleset_pack_unresolved metric (getMetrics().recordError, the same A1/A11/A19 convention) and every other pack's real findings still ship, while the existing RequiredDetectorUnavailableError hard-failure (A4) now specifically means every configured pack failed, not just one. Verified end to end against the REAL Semgrep binary and the real corpus/jvm-vuln fixture: the pre-fix combined --config p/java --config p/spring invocation returns 0 results (exit 7); the corrected per-pack path against that same two-entry list now returns 4 real findings plus a p/spring-specific warning instead of a silent empty scan. New regression tests in tests/discovery.detectors.test.ts cover the per-pack degrade, cross-pack candidate dedup, and the all-packs-failed case still hard-failing (A4 safety property preserved). Updated docs/modules/discovery.md.

Update Triggers
Update this file when top-level architectural patterns change, when new packages or apps are introduced, when global cross-cutting policies evolve, or when files are added or removed from the docs directory.

Related Docs
docs/maintenance.md — Maintenance rules and decision tree for updating documentation.
docs/architecture/folder-structure.md — Detailed folder mapping of all packages and applications.
