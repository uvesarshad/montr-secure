> Source audit: [26-09-09-audit-july-line-divergence](./26-09-09-audit-july-line-divergence.md)
> Updated: 26-09-12 · 22/26 done

- [x] **(A1, P0)** Wire the `migrate` service into the documented bring-up — drop the profile gate, add `depends_on: migrate: {condition: service_completed_successfully}` to api and worker — so `docker compose up --build` does not start them against a schema-less database.
- [x] **(A8, P0)** Zero-pad all twelve Prisma migration directories (`00_init` … `11_llm_provider_expansion`) so lexicographic application order matches intended order. Without this, a fresh database applied migration 10 immediately after `0_init` and failed on a table created three migrations later.
- [ ] **(A8, P0)** For every database that already applied the OLD migration names — dev machines and any stood-up environment — run `prisma migrate resolve --applied <new_name>` once per renamed migration (the ten renamed `00_`–`09_` directories), or reset if the data is disposable. Renaming changes the name recorded in `_prisma_migrations`, so without this those ten will be treated as new and re-run. **Do this before deploying this commit anywhere with data worth keeping.** Fresh installs need nothing. TOOLING NOW EXISTS: `pnpm migrate:resolve-rename` inspects the target database, reports which of the three states it is in, dry-runs by default, and needs `--apply` to change anything — all three states plus both anomaly refusals verified against a real Postgres. Running it against your databases is still yours to do.
- [x] **(A9, P0)** Swap the bundled Postgres image to `pgvector/pgvector:pg16` in both `deploy/docker/docker-compose.yml` and `deploy/helm/montr-secure/values.yaml`, so `05_semantic_code_index`'s `CREATE EXTENSION vector` succeeds. Closes a follow-up the migration's own header flagged as required and that was never actioned.
- [x] **(A2, P1)** Spawn `process.execPath` against `node_modules/vitest/vitest.mjs` instead of the `.bin/vitest` shim in all five scripts (`e2e-scan`, `selfscan`, `corpus-scan`, `benchmark-owasp`, `blue-team-corpus-scan`), restoring Windows compatibility without breaking darwin/linux.
- [x] **(A3, P1)** Extend the gateway from four providers to ten (add `openai`, `google`, `xai`, `moonshot`, `zhipu`, `deepseek`) via one generic `OpenAiCompatibleAdapter`, reusing azure.ts's now-exported `buildBody` so every new provider inherits structured output, tool calling and A4 round-tripping. Includes the additive Prisma `Provider` enum migration and egress-guard hosts.
- [x] **(A4, P1)** Add multi-turn tool round-tripping to the gateway: `toolCalls` on `LLMMessageSchema`, and Anthropic `tool_use`/`tool_result`, Vertex `functionCall`/`functionResponse` and OpenAI `tool_calls`/`tool_call_id` encoding in all three mappers — without regressing structured output, effort/thinking, caching, token counting or the Batch API.
- [x] **(A5, P1)** Add the bounded agentic fix loop with sandboxed `read_file` multi-file reads, driven by a cheap in-process oracle so real vitest and container proof still run exactly once per final candidate rather than per iteration. Opt-in, off by default.
- [x] **(A10, P1)** Stop `scripts/integration-smoke.mjs` migrating by hand before starting api/worker — bring the stack up with a single `docker compose up -d` so the job exercises the documented path instead of compensating for it being broken.
- [x] **(A6, P2)** Add `.gitattributes` (`text=auto eol=lf`, LF-forced shell scripts, no-diff lockfile, binary asset markers including the vendored corpus webfonts) so the prettier CI gate is deterministic across contributor `core.autocrlf` settings.
- [x] **(A7, P2)** Correct `deploy/docker/README.md` — remove the stale "Wave-0 stub caveat" claiming api/worker/web are stubs, and reconcile the migration instructions with the A1 wiring change.

## Suggested enhancements

- [x] Ship multi-provider support as a positioning change, not just a feature — `README.md`, `CLAUDE.md` and `AGENTS.md` now state ten providers rather than four, with the two bootstrap files kept in sync per the project rule.
- [x] Treat A4 + A5 as a single "agentic remediation" epic, sequencing A4 first since A5 is unimplementable without it.
- [x] Add a CI job that runs the documented bring-up from a clean volume — closed by A10, which converts the existing integration-smoke job into exactly that test.
- [x] Retire the redundant backup branch — `backup/pre-rewrite-2026-09-09` deleted after confirming it was identical (same tip commit, zero divergence) to the pre-existing `archive-july-2026`.
- [x] Add a from-scratch deploy smoke test to the release path — now enforced automatically by the A10 CI job rather than left to a manual checklist.
- [x] Add a Windows CI leg, or drop the pretence. A2 and A6 are both Windows-only breakage that no current CI job would catch, so today's fixes will silently rot. Either test the platform or state plainly in the README that it is unsupported — the current in-between is the worst of the three options.
- [x] Decide whether to enable the A5 agent loop in production. It is built, tested and wired to nothing: `apps/worker/src/runners.ts` does not populate `agentLoop`, matching the existing `containerProof.targetRepoDir` precedent. Until someone turns it on, autonomous multi-file fixing — the point of the A4+A5 epic — stays dormant. Turning it on raises cost and latency per fix, so this is a deliberate call, not a default.
- [x] Fix the root lint entry point. `pnpm lint:root` (`eslint .`) reports ~11,400 errors, almost entirely under `apps/`, on files untouched by this work. The real per-package gate (`pnpm lint`, turbo) passes 37/37, so this is a misconfigured entry point rather than 11,400 genuine defects — most likely missing ignores for generated Next.js output. Needs its own pass; folding it into feature work would bury everything else in noise.
- [x] **(A11, P0)** Investigate and fix the two golden-corpus false positives (one `sql_injection`, one `command_injection`). Installed semgrep locally (`brew install semgrep`) to reproduce for real rather than reason from CI logs — `node scripts/corpus-scan.mjs` then scored identically to CI (16 confirmed, same two flagged). Read the actual flagged source lines against `corpus/javaseccode/ground-truth.manifest.json`: BOTH are genuine, independently-exploitable vulnerabilities, not scanner defects. `SQLI.java:149` (`jdbc_ps_vuln`, the file's own doc comment reads "Incorrect use of prepareStatement") string-concatenates the untrusted `username` into the SQL text before ever calling `PreparedStatement` — the `?` placeholder is never used, so parameterization buys nothing; a second, distinct SQL injection in the same file from the one already labeled at line 67. `CommandInject.java:45` (`codeInjectHost`, doc comment "Host Injection" with a working payload example) concatenates the untrusted `Host` header into a `sh -c curl <host>` command via `ProcessBuilder` — a second, distinct command injection from the one already labeled at line 30. The ground-truth manifest only ever labeled one instance per category per file; both files genuinely contain two. This is not a hunch — it follows an established precedent already documented in `corpus/README.md`'s own sampling-method section (point 4): PyGoat hit the identical situation once before ("an initial real pipeline scan surfaced two apparent false positives... re-reading both confirmed they are genuine, independently-exploitable vulnerabilities... so they were added as true findings"), and `pygoat`'s manifest already carries a `_2`-suffixed second entry for exactly this reason (`gt_pygoat_cmdi`/`gt_pygoat_cmdi2`). Added `gt_javaseccode_sqli2` and `gt_javaseccode_cmdi2` following that exact convention, each hand-verified against the vendored source with a description explaining why it's genuine and distinct from its sibling entry. **Not a baseline loosening** — `corpus/baseline.json`'s numeric thresholds were not touched; the ground truth the scorer compares against was corrected, which is the same fix class the project's own methodology already used once. Verified: rescoring the identical scan output now reports **FP-rate 0.0%, precision 100.0%, recall 34.8% (up from 31.8%, TP 14→16, FP 2→0)** — baseline gate PASS. `packages/qa` (61/61, including the OWASP-category-coverage test that reads this same manifest) and the rest of the full suite (164/164 files, 1827/1829 tests) unaffected. Four unrelated files (`discovery.detectors`, `discovery.pipeline`, `iac.test.ts`, `e2e-scan.test.ts`) now fail locally purely because installing semgrep via brew pulled a different version/ruleset than CI's unpinned `pip install semgrep` — confirmed via `git stash` that they fail identically with or without this fix, so it is pre-existing local-environment drift, not a regression; flagged below as a new, separate finding rather than fixed here (out of scope for A11, needs its own investigation into whether semgrep should be version-pinned).
- [x] **(A12, P1)** Fix real cross-platform path-handling bugs the Windows CI job surfaced. ROOT CAUSE CONFIRMED: ts-morph's `SourceFile.getFilePath()` always returns forward-slash paths, on every OS, while the `dir` these were compared against came from `node:path` (backslashes on Windows). Ten call sites across `packages/appmap/src/**` did raw `abs.startsWith(dir)` before normalizing separators, so on Windows the comparison always failed and every one silently fell through to returning the untouched absolute path instead of a repo-relative one — explaining routes/models/auth-boundaries/telemetry-surfaces all resolving to `undefined`/empty. This directly contradicts the source audit's untested conclusion that App Map path normalization was "fine" — that was never checked against a real Windows run until this task, and it did not hold. Fixed at all ten sites (`build.ts`, `diff.ts`, `routes.ts` ×2, `express.ts`, `callgraph.ts`, `fastify.ts`, `route-models.ts`, `surfaces.ts`, `taint.ts`) by normalizing `dir` to forward slashes before the comparison, not just the final output. Verified against a simulated Windows path pair (mixed separators) confirming the fix produces the correct relative path where the old code returned the full absolute path. Python/Java analyzers use fast-glob (always forward-slash) rather than ts-morph, so they were never affected — checked, not assumed. Two more bugs surfaced by the same investigation, both TEST-only (not product code): `tests/qa.corpus.test.ts` asserted `repo.path.startsWith("/")`, which is false for any Windows absolute path (`C:\...`) — fixed to use `node:path`'s `isAbsolute()`. `tests/stack-agnostic.invariant.test.ts` used `file.split("/").pop()` against a `node:path`-joined path, which no-ops on Windows (no `/` to split on) and silently defeats the file's own exception list — fixed to use `basename()`. All three fixes verified: full suite 168 files / 1918 passed / 2 skipped (stable across repeated runs, unchanged from baseline); typecheck clean; `eslint .` clean; turbo lint 37/37; prettier clean. CONFIRMED on an actual Windows runner (2026-09-11, run 34637132922, commit c1332b6): typecheck passed, and the unit-test step went from roughly two dozen real failures to **1 failed | 1917 passed | 2 skipped (1920)** — every App Map/route/model/auth-boundary/telemetry-surfaces/corpus-loader failure this task set out to fix is gone. The two `generate.agent-loop.test.ts` timeouts also did NOT recur this run (both passed) — encouraging, but one clean run isn't proof a timing-sensitive test can't flake again, so that item stays open below rather than being marked fixed. The single remaining failure was exactly the anticipated one: `fix.container-replay.test.ts`, failing on a Docker networking error (`could not find plugin bridge in v1 plugin registry`) — an environment limitation of the `windows-latest` runner, not a code defect.
- [x] Give `generate.agent-loop.test.ts`'s two timeout-prone tests a longer explicit timeout as a precaution, and exclude `fix.container-replay.test.ts` (real-Docker proof-of-fix) from the `windows` CI job's test step. Both tests now pass `20_000` (20s) as an explicit vitest timeout — generous headroom above the real vitest-subprocess `validatePatch` cost each incurs once per test, which is what crossed the 5000ms default on the slower Windows runner. The exclusion is scoped to the `windows` job's own `vitest run` invocation only (`--exclude "tests/fix.container-replay.test.ts"` added there, NOT in `vitest.config.ts` or the shared `test` script) — verified locally that vitest's CLI `--exclude` merges with, rather than overriding, the config's existing excludes (168 files without the flag, exactly 167 with it, nothing else dropped), so the test still runs for real with real Docker on every other platform's CI job. Verified: full suite 168 files / 1918 passed / 2 skipped, typecheck clean, eslint/prettier clean, and the two edited tests individually re-run green.
- [ ] Flip the new `windows` CI job from `continue-on-error: true` to blocking once a `windows` run is fully green with the fixes above in place — not yet confirmed on an actual Windows runner, only locally.
- [ ] **(A13, P2)** New finding, surfaced while reproducing A11: `tests/discovery.detectors.test.ts`, `tests/discovery.pipeline.test.ts`, `packages/discovery/src/detectors/iac.test.ts` and `apps/worker/src/e2e-scan.test.ts` fail locally the moment a real semgrep binary is on `PATH` (confirmed via `git stash` — they fail identically with or without the A11 fix, so this is pre-existing, not a regression). CI installs semgrep unpinned (`pip install semgrep`, `.github/workflows/ci.yml`); a local `brew install semgrep` pulled a different version (1.176.0) with an apparently different default ruleset/behavior. Investigate whether semgrep should be version-pinned in CI (and documented for local dev) so these tests aren't silently sensitive to whichever semgrep happens to be on a given machine's `PATH`.
- [ ] Decide the default key tier for `moonshot`, `zhipu` and `deepseek`. They currently default to `data_retaining`, so they are blocked under `keyTierGuard: block` until an operator declares an enterprise tier — the cautious reading of their data-retention posture, carried over from the July line. If customers should be able to use them out of the box, this needs revisiting against those providers' actual terms.
- [x] Watch the first CI run on `main` after this work. CORRECTION: this was checked off in the same commit that triggered the run being watched, before it had actually completed — so it was marked done before being done. It has now genuinely been watched (run for `b00a1ff`, 2026-09-09). Results: `build` (install/lint/typecheck/test/build), `docker`, `helm`, `integration-smoke` and `self-scan` all now pass for real in CI, not just locally — confirming A1/A6/A7/A9/A10 and the lint fix all hold. `golden-corpus` fails with the exact same numbers as the local reproduction (TP=14 FP=2 FN=30, unchanged) — a stable, reproducible regression, tracked as A11. `windows` failed — see the new A12 above.
