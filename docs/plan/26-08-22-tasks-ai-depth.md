> Source audit: [26-08-22-audit-ai-depth](./26-08-22-audit-ai-depth.md)
> Updated: 26-08-22 · 46/61 done

# Tasks — Montr Secure: AI depth, autonomy & purple-team gaps

## P0 — broken / at-risk

- [x] **(A1, P0)** Make cost metering fail closed. `packages/cost-meter/src/pricing.ts:70` returns `0` for an unknown model with no warning; combined with a 3-entry rate card (`packages/contracts/src/llm.ts:212-232`) this means any client on an unlisted model runs with a $0 meter and the budget ceiling never fires (violates PRD §11). Raise or bill at a configured conservative ceiling rate on unknown ids, emit a warning + metric either way, and complete the rate card: add `claude-opus-5`, Fable 5, Opus 4.7/4.6, Sonnet 4.6, every Azure OpenAI model, and the Bedrock/Vertex partner rates.
- [x] **(A2, P0)** Enforce the budget _during_ a layer, not just between layers. `packages/orchestrator/src/controller.ts:446-465` runs `executeLayer` to completion before calling `enforceBudget`. Add a pre-call budget check inside `gateway.complete()`/`stream()` so a single layer cannot blow the ceiling. Do this **before** any of the AI-deepening tasks below, since every one of them increases per-layer LLM calls.
- [x] **(A3, P0)** Make resumability reachable in production. Add `POST /scans/:id/resume` and a worker boot-time reconciliation that finds stuck `status:"running"` scans and resumes them — the mechanism at `controller.ts:267-309,821-838` is correct but has zero production callers (only `apps/api/src/stub-orchestrator.ts:66`). While there, make layer re-runs idempotent: `packages/state-store/src/repositories.ts:234-238` (`FindingRepo.bulkCreate`) is a bare `createMany` with no `skipDuplicates`, so a BullMQ stalled-job redelivery throws a unique-constraint error and permanently fails the scan. Tune `maxStalledCount`/lock settings in `bullmq-scheduler.ts:189-201`.
- [x] **(A4, P0)** Fix air-gapped SAST, which currently produces zero findings silently. Semgrep is invoked with hosted registry pack IDs (`packages/discovery/src/detectors/sast.ts:24-30`) that need network egress the air-gap NetworkPolicy forbids. Add a `rulesetsDir` config key that Semgrep is pointed at so `deploy/airgap/build-bundle.sh --semgrep-rules-dir` output is actually consumed, and make an unavailable required detector a **hard failure** rather than the current warn-and-return-`[]` (`sast.ts:152-155`), so an empty scan can never masquerade as a clean one.
- [x] **(A5, P0)** Build the missing scan progress backend. `apps/web/src/lib/api/hooks.ts:22-29` polls `GET /scans/:id/progress`, which does not exist in `apps/api` — every layer shows `pending` forever in production and the UI only works under MSW mocks. Subscribe the API to the orchestrator's real `EventBus` (`packages/orchestrator/src/events.ts`) and serve it as SSE. Also implement the other confirmed 404s: `/scans/:id/appmap`, the cross-scan `/pull-requests` aggregate, and `authorizeDast`.
- [x] **(A32, P0)** Wire real-time cost metering into the production gateway. Discovered while implementing A2: `apps/worker/src/main.ts`'s `createLlmGateway` call constructs the shared `LLMGateway` with no `costMeter` option, and no layer package ever passes `ctx.costMeter` into a gateway call — so `CostMeter.record()` is never invoked in the real pipeline. Between-layers `enforceBudget` (A2) has therefore always been reading `spentUsd: 0`, meaning the only currently-live budget guard is A2's new pre-call estimate check; actual metered spend never accumulates. Wire `costMeter` through gateway construction and every real call site so recorded spend reflects reality.
- [ ] **(A33, P0)** Fix JVM SAST silently producing zero findings. Discovered while implementing E14: `packages/discovery/src/rulesets/java/index.ts`'s `semgrepRulesets: ["p/java", "p/spring"]` — the `p/spring` Semgrep registry pack now 404s, and Semgrep aborts its **entire** invocation when any one `--config` target fails, silently zeroing SAST candidates for every JVM app scanned today. Verified root cause directly (swapping in a `p/java`-only ruleset raised confirmed findings from 0 to 5 on the same OWASP Benchmark subset). Fix: drop or replace the dead pack, and make a single bad ruleset entry degrade gracefully (skip that pack, keep the rest) rather than aborting the whole Semgrep run — this is the same "empty scan must never masquerade as clean" principle A4 already established for the air-gap case, just triggered by a different root cause (a dead upstream registry reference, not missing network egress).

## P1 — should fix

- [x] **(A6, P1)** Make recall a first-class, published metric with a real floor. `corpus/baseline.json` records recall 25.0% (TP=11, FN=33) with `idor` and `broken_access_control` at 0%, but `DOD.md` item 4 is marked ✅ on "FP < 5%" without surfacing it. Add `recallMin` to the DoD, report precision and recall together everywhere the headline metric appears, and state plainly that a 0% FP rate at n=11 confirmed findings is not yet statistically meaningful.
- [x] **(A7, P1)** Break the demote-only ceiling that caps recall. Every LLM call is clamped to subtract — decisively, `packages/confirm/src/static.ts:369` has **no code path reading `confirmed === true`**. Change what earns autonomy rather than removing the rails: let the model propose, and require executable evidence (a failing test or a live probe transcript) before a proposal reaches `confirmed`; unproven proposals land in the existing appendix. Depends on E2/E13.
- [x] **(A8, P1)** Add agentic capability to the gateway and pipeline: tool use (currently explicitly out of scope per `packages/llm-gateway/src/mapping.ts:5-8`, adapters ignore `request.tools`), a multi-turn loop, extended thinking with an `effort` setting, and prompt caching. Raise the output caps — 512 tokens for correlation and 1,024 for confirmation leave no room to reason. Tracked in detail as E10. (Scoped gateway plumbing per the audit's own framing, not the agentic loop itself — E1 is that follow-on: all four adapters forward `request.tools` and parse tool_use/functionCall/tool_calls responses into a new `LLMResponse.toolCalls`; `effort` drives Anthropic/Bedrock's `output_config.effort` + adaptive thinking; Anthropic/Bedrock's system prompt gets `cache_control`; correlation/confirmation maxTokens raised 512→2048 / 1024→4096. Deferred: App Map cache point, non-Anthropic effort mapping, and the tool-execution loop itself, E1.)
- [x] **(A9, P1)** Expand live DAST beyond 4 categories and ~5 payloads. `LIVE_CONFIRMABLE_CATEGORIES` (`packages/confirm/src/live.ts:35-40`) covers 4 of ~23 taxonomy categories; `craftProbes` (`live.ts:118-173`) emits 1 payload each. Fix the NoSQL probe, which reuses the SQL string payload `' OR '1'='1'` (`live.ts:142`) instead of a Mongo operator payload and so will essentially never fire. Add SSRF, IDOR, authz bypass, path traversal, command injection, XXE and deserialization probes. The transport and guards are sound — this is ammunition, not engine work.
- [x] **(A10, P1)** Wire the "built but unwired" subsystems, all of which are real tested code with zero production callers: the Vault key source (`packages/config/src/key-source.ts` — both `apps/worker/src/main.ts:53-58` and `apps/api/src/production-deps.ts:125-126` bypass it), the prompt registry (`gateway.resolvePrompt` is never called by any of the 5 prompt sites), the false-positive tuning loop (`fpTuning` is never populated by `apps/worker/src/runners.ts`), and gateway streaming. Add a CI check asserting every declared seam has at least one non-test caller. (`apps/worker/src/main.ts` and `apps/api/src/production-deps.ts` now call `resolveFieldEncryptionKey(config)` instead of reading `fieldEncryptionKeyRef` directly; all 5 prompt sites now call `gateway.resolvePrompt(name, fallback, {clientId})` — `LLMGateway` gained an optional `resolvePrompt` method in `packages/contracts/src/llm.ts` so base-typed callers compile; new `FalsePositiveMarkRepositoryImpl` (`packages/state-store/src/repositories.ts`) reads prior `finding.marked_false_positive` audit events back out, and `apps/worker/src/runners.ts`'s `loadFpTuning()` passes them into `correlate()`/`confirmFindings()`; gateway streaming was assessed and deliberately left unconsumed by any layer — verified instead via a real end-to-end integration test (`packages/llm-gateway/src/streaming.integration.test.ts`, real `AnthropicAdapter` + fake transport); new `scripts/check-unwired-seams.mjs` — `pnpm run check:unwired-seams` — is wired into CI right after typecheck.)
- [x] **(A11, P1)** Refresh the model matrix. `packages/contracts/src/llm.ts` pins `claude-opus-4-8` as top tier; the current flagship is `claude-opus-5`. Drop the date suffix on `claude-haiku-4-5-20251001`. Add a fallback model — a failing model is currently retried twice on itself and then the call fails, with no cascade to an alternate.
- [x] **(A12, P1)** Upgrade SCA reachability from package-import presence to function-level call reachability. `packages/discovery/src/sca.ts:191-248` computes `reachable = imported.has(pkg.name)` from a regex scan of import statements, but PRD §7 promises "is the vulnerable path actually imported/called?". Either implement call-level reachability or correct the claim in the docs and report copy. The advisory data itself (~3,600 real OSV-derived entries) is sound.
- [x] **(A13, P1)** Enforce structured outputs on Anthropic, Bedrock and Vertex. Only Azure sets a response format (`packages/llm-gateway/src/adapters/azure.ts:52`); the other three request JSON in prose and rely on `try/catch JSON.parse`, silently degrading to the deterministic path on failure with no log or metric. Use real structured outputs with a JSON schema, and add a counter for LLM response parse failures so a deployment-wide collapse in LLM contribution cannot go unnoticed. (All four adapters now enforce a real per-purpose JSON schema — `packages/llm-gateway/src/structured-output.ts`, keyed by `metadata.purpose` and derived from each of the five real call sites' own existing parsed-JSON shapes, requiring zero changes to those five packages; Azure upgraded `json_object` → `json_schema` too. A centralized `llm_gateway.response_parse_failure` metric fires at the gateway boundary on any `JSON.parse` failure, independent of each caller's own fallback path.)
- [x] **(A14, P1)** Stop asking the model to rewrite whole files. `packages/fix/src/generate.ts:104` sets `maxTokens: 2048` while the prompt demands `{"fixedSource": "<the full fixed file>"}`, so any file over ~1,500 lines truncates, fails `JSON.parse`, and silently degrades to advisory with no metric. Switch to a diff/edit output format and raise the cap.
- [x] **(A15, P1)** Build the trigger surface for autonomous operation: a `montr scan .` CLI, a GitHub App running diff-mode on every PR with findings as review annotations, and a webhook receiver. The diff-mode engine (`packages/appmap/src/diff.ts`) is real and works — it simply has no way to be triggered. Tracked in detail as E12. (Scoped: built the CLI and the HMAC-verified webhook receiver end-to-end, plus a PR acknowledgement-comment reuse of the existing Octokit VCS surface. A full GitHub App — manifest registration, OAuth installation, marketplace listing — remains genuinely external/product work and is explicitly deferred, as is posting a webhook-triggered scan's CONFIRMED findings back to the PR once the pipeline finishes, which needs apps/worker's Layer 5 to persist/read the originating PR reference.)
- [ ] **(A16, P1)** Build the blue-team half — no ATT&CK mapping, detection rules, threat modeling or runtime visibility exists anywhere. Decomposed into the **Blue team build** section below (B1–B12).
- [x] **(A17, P1)** Broaden target coverage beyond three languages of source code. No IaC/Terraform/Dockerfile/Kubernetes scanning, no SBOM, no license scanning, no supply-chain checks, and — most conspicuous for an AI security product — no AI/LLM-application security scanning. Also close the Express/Fastify route-extraction gap: both get framework detection from `package.json` (`packages/appmap/src/sources.ts:132-133`) but have no route extractor, so a plain Express API yields zero routes. Tracked in detail as E11/E16. (Scoped: closed the narrow, well-defined Express/Fastify route-extraction gap — new `packages/appmap/src/languages/typescript/express.ts` and `fastify.ts` add real ts-morph route extractors, wired into `languages/typescript/index.ts`'s framework-gated dispatch, verified against this repo's own `apps/api` Fastify routes. The broader IaC/SBOM/supply-chain/AI-app-security breadth remains open, tracked as E11/E16.)
- [x] **(A18, P1)** Link routes to ORM models in the App Map. `ormModels[]` and `routes[]` are separate arrays with no cross-reference; only taint sources carry a `routeId`. This is the specific structural blocker under the 0% IDOR/broken-access-control recall — you cannot reason about ownership without knowing which records a route touches and whose they are. Prerequisite for E1. (`Route` gained an optional `referencedModels: { modelName, operations }[]` field, populated by new `packages/appmap/src/languages/typescript/route-models.ts`: resolves a direct `prisma.<model>.<method>(...)` call in a route handler, plus one hop through a locally declared helper function — same-file or a relative import, mirroring `callgraph.ts`'s bounded-hop convention. Does not resolve through class-method/repository-pattern indirection or bare/workspace-package imports. Not yet persisted through `packages/state-store` — a Postgres column + mapper change is a follow-up, out of this change's scope.)

## P2 — nice to have

- [x] **(A19, P2)** Replace the fudge-factor cost estimator with real token counting. `packages/cost-meter/src/estimate.ts:70-73` uses hand-picked per-unit constants and `packages/llm-gateway/src/gateway.ts:191-194` estimates tokens as `length / 4`. Use the provider's `count_tokens` endpoint, then validate the PRD §19 ±15% estimate-vs-actual bound against a real provider (`DOD.md` item 6 concedes it has only ever been measured at ≈$0). (`MontrLlmGateway.countTokens()` calls Anthropic's real `messages.countTokens` via a new adapter capability, falling back to the `length/4` heuristic for other providers/on failure; the A2 pre-call budget guard deliberately keeps the fast heuristic rather than adding a network round-trip to every gated call — documented tradeoff. `estimate.ts`'s constants are now documented in-code as uncalibrated, since no real captured prompt-size data exists in this repo. A new `packages/llm-gateway/src/estimate-variance.test.ts` validates the estimate against a realistic synthetic actual — replacing the ≈$0 concession with a real comparison, though the measured result lands ~5x off PRD's exact ±15% figure, honestly reported rather than fabricated.)
- [x] **(A20, P2)** Upgrade auth-boundary detection from identifier-text regex to real middleware/control-flow analysis (`packages/appmap/src/languages/typescript/routes.ts:16-17`, `python/routes.ts:35-38`, `java/extract.ts:71`). Auth enforced by a middleware stack registered elsewhere, a wrapper HOC, or a runtime guard is currently invisible and falls through to `unknown`. (TypeScript `routes.ts`: an export's HOC-wrap — `withAuth(handler)` and the factory form `requireRole(...)(handler)` — is now verified as a real AST call shape, not identifier-text presence; a root/`src/` `middleware.ts` referencing a known guard propagates `authenticated` to every route its `config.matcher` covers, the Next.js analog of Express/Fastify global middleware. Python `routes.ts`: Django 5.1+'s global `LoginRequiredMiddleware` in a settings module's `MIDDLEWARE` list propagates to every still-`unknown` URLConf route. Java `extract.ts`: a new `applySpringSecurityAuth` pass resolves the declarative `authorizeHttpRequests(auth -> auth.requestMatchers(...).hasRole(...)…)` filter-chain lambda into Ant-pattern route matches, project-wide. None override an existing more-specific per-route signal; the original whole-file regex remains the final fail-safe fallback in all three languages.)
- [x] **(A21, P2)** Give Python and JVM a real call graph. Only TypeScript has one (`packages/appmap/src/languages/typescript/callgraph.ts`); `packages/confirm/src/heuristics/{python,java}/index.ts` are lexical marker lists self-described as "pre-registered stubs" in `heuristics/registry.ts:18`, so confirmation on both stacks is same-file only despite being marketed as supported. (New `packages/appmap/src/languages/{python,java}/callgraph.ts`, mirroring `typescript/callgraph.ts`'s bounded-hop philosophy and `TaintFlowEdge` output shape, wired into each language's `taintFlows` field (previously always `[]`). Python reaches full tier parity with TS: same-file direct-call/return-propagated resolution PLUS one-hop `from .mod import x` / `from ..pkg.mod import x` relative-import resolution (module-level functions only, matching TS's own exclusion of class methods). Java reaches same-class + same-file cross-class (static-style) resolution; cross-file/same-package resolution was deliberately NOT attempted (Java needs no import for a same-package call, and a directory-based guess risks a false edge in a multi-module build — documented as a follow-up needing real `package`/`import` parsing). Rather than adding another "built but unwired" seam (A10), `packages/confirm/src/static.ts`'s `findSink`/`findSource` gained a same-file-first, `taintFlows`-fallback lookup — a small, additive, language-agnostic change outside the two languages' own directories but necessary so Layer 3 static confirmation actually consumes this data, since `taintFlows` was previously read only by Layer 2's `grounding.ts`, even for TypeScript.)
- [x] **(A22, P2)** Make the proof-of-fix test prove behavior, not syntax. The vitest subprocess execution (`packages/fix/src/patch.ts:166-279`) is genuinely real, but the test body is a filesystem read plus `expect(source).not.toMatch(vulnerable)`. Covered by E13. (Closed by E13's real ephemeral-container exploit-replay mechanism — see that item below.)
- [x] **(A23, P2)** Make the model-variance harness capable of detecting variance. Its only caller wires `perfectConfirmedForRepo` (`packages/qa/src/synthetic.ts:44-46`), which ignores the `model` parameter and echoes ground truth, so all models always score 100%. It is also not run in CI. PRD §15 requires it to publish the model matrix and catch accuracy cliffs.
- [x] **(A24, P2)** Make the content-aware scrubber a runtime gate, not a test-time certifier. `packages/security/src/scrubber.ts` is called only from tests; it does not gate the audit-write path, and `AuditEventInput.metadata` (`packages/contracts/src/audit.ts:92`) is an unconstrained `z.record(z.string(), z.unknown())` held in check only by a code comment.
- [x] **(A25, P2)** Replace the sanitizer-detection substring regex in correlation (`packages/correlation/src/grounding.ts:27`) with structural detection. It matches against finding description text and is brittle in both directions, though it only applies on the same-file fallback path. (Split into two precise tiers: `KNOWN_SANITIZER_CALL_RE` requires an actual call-expression shape naming a real sanitizer/validator/encoding API or safe coercion idiom — checked against source/sink descriptions AND the candidate's evidence snippet, which for every injection-class detector is real matched source code, not analyst prose; `CURATED_SAFE_MARKER_RE` keeps the legacy curated-marker vocabulary some App Map extractors deliberately author into a description (e.g. `python/taint.ts`'s documented safe/unsafe marker contract) but now applies ONLY to source/sink descriptions, never the uncontrolled evidence snippet — closing the concrete inversion bug where a bare-word stem like "bind" or "no sanitization applied" in raw code/prose could flip the verdict either way.)
- [x] **(A26, P2)** Calibrate the correlation scoring constants against the golden corpus. The exposure weights, reachability weights and the `0.6*category + 0.4*severity` impact blend (`packages/correlation/src/scoring.ts:27-99`) are hand-set floats with no empirical derivation, inside what the PRD calls the moat. (n=44 labelled corpus findings is too thin for a real statistical fit, so this is a documented manual recalibration: checked the corpus's real severity labels against `impactScoreFor`'s ranking and found one concrete mis-ranking — `corpus/log4shell-vulnerable-app`'s `critical` CVE-2021-44228 `vulnerable_dependency` finding scored BELOW several `high`-severity findings of other categories, purely because `CATEGORY_IMPACT_BASE.vulnerable_dependency` (0.5) was capping it. Raised to 0.7, documented with the concrete example in `taxonomy.ts`. New `tests/correlation.scoring-calibration.test.ts` locks in the fix as a permanent regression guard: critical-severity corpus findings must rank at/above high, and high at/above medium, across categories — this test IS the calibration, not a separate step. Exposure/reachability weights left unchanged: the golden-corpus ground truth doesn't carry App-Map exposure/reachability data, so there's no corpus evidence either way.)
- [x] **(A27, P2)** Add per-tenant queue isolation. Queue names (`packages/contracts/src/queue.ts:23-31`) are per-layer only, so all clients share six queues. Correct for single-tenant on-prem, but it blocks multi-tenant deployment and offers no noisy-neighbour isolation. Repository-layer `clientId` row-scoping is already rigorous. (Opt-in, OFF by default: new `resolveQueueName(layer, clientId, tenantIsolation)` in `packages/contracts/src/queue.ts` derives `montr.layer0.<clientId>` instead of the shared name when enabled; new `config.queue` — `QueueConfigSchema`, `perTenantIsolation` default `false` — is read by `packages/orchestrator/src/bullmq-scheduler.ts`'s new `deriveTenantSchedulerOptions`, called identically by apps/worker's consumer-side `BullMqJobScheduler` — which fans out one BullMQ Queue + Worker per `(layer, tenantId)` pair, each independently polling Redis, so one tenant's backlog cannot delay another's newly-queued job — and apps/api's produce-only `EnqueueOnlyScheduler`, so both processes always agree on queue names. This repo ships plain non-Pro BullMQ with no group-based fair-scheduling primitive, so N-queues-per-tenant was the deliberate choice over a lighter-weight mechanism.)
- [x] **(A28, P2)** Label the golden-corpus vulnerable repos as excerpts. `dvna` is 15 files, `pygoat` 18, `javaseccode` 7 — the clean negatives are genuinely full-size, but "REAL: appsecco/dvna" reads as the whole application when it is a slice.
- [x] **(A29, P2)** Benchmark against external datasets and competing scanners — no OWASP Benchmark, Juliet, SecBench or CVE-Bench, and no head-to-head against Semgrep or CodeQL as competitors. Covered by E14.
- [x] **(A30, P2)** Fix stale doc comments that contradict the code: `packages/appmap/src/languages/registry.ts:20-23` claims TypeScript uses tree-sitter when it uses `ts-morph`; `packages/discovery/src/rulesets/registry.ts:18` calls implemented Python/Java rulesets "pre-registered stubs"; `apps/web/src/lib/api/config.ts:73-75` marks `killSwitch` unimplemented when the route exists at `apps/api/src/routes/scans.ts:184`.
- [x] **(A31, P2)** Enable prompt caching and batching. System prompts and the App Map are ideal stable cache prefixes and `packages/cost-meter/src/pricing.ts:70-76` already prices cache reads/writes, but no adapter ever sends `cache_control`. Add Batch API use for non-latency-sensitive work at 50% cost. (Cache wiring confirmed correct end-to-end with a new real-adapter integration test — no gap found. App Map second cache point still deferred: a concrete minimal design was sketched in `docs/modules/llm-gateway.md` but not implemented, since `packages/confirm` was outside this change's scoped file list. Batch API built for real: `MontrLlmGateway.submitBatch()/pollBatch()/getBatchResults()` backed by `AnthropicAdapter`'s real `messages.batches` endpoints, with a new 50% `priceUsageUsd` batch discount; not wired into any layer — the orchestrator's FSM assumes synchronous layer completion, incompatible with a job resolving up to 24h later, and the best-fit consumer (`packages/fix/src/generate.ts`) was outside scope — built and fully tested, mirroring how A10 handled gateway streaming.)

## Suggested enhancements

- [x] **(E1)** Build an agentic investigation loop in Layer 3 — the highest-leverage single change. Give confirmation a tool-using agent with read-only repo tools (`read_file`, `grep`, `find_definition`, `query_call_graph`, `list_routes`, `get_orm_model`) so it investigates a finding across files instead of receiving pre-chewed metadata. Run with adaptive thinking and a task budget. This is what lifts IDOR and broken access control off 0%. Depends on A18 and E10. (New `packages/confirm/src/investigate-tools.ts` — the six read-only tools plus a terminal `submit_conclusion` — and `packages/confirm/src/investigate.ts`'s `runInvestigation`, a multi-turn tool-calling loop run with `effort:"high"`, only when neither the static proof nor live DAST confirmed a finding. A hard structural ceiling of 8 turns (`ABSOLUTE_MAX_INVESTIGATION_TURNS`) is enforced regardless of config, independent of the existing A2 per-call budget guard; exhaustion always yields `"inconclusive"`, never a confirmation. OFF by default — `ConfirmDeps.investigation.enabled` — since this is a materially larger cost profile than the existing single-call veto; `ConfirmInput.repoRoot` (for the file-reading tools) is plumbed but not yet populated by `apps/worker/src/runners.ts` — documented follow-up, outside this task's scoped file list. A "confirmed_candidate" verdict alone NEVER confirms anything — see E2/E4.)
- [x] **(E2)** Let the LLM propose findings, gated by executable proof. Add a discovery pass that may propose candidates the deterministic tools missed, but require a failing test or live probe transcript before anything reaches `confirmed`; unproven proposals go to the appendix. Autonomy earned by proof, not granted by trust. (New `packages/confirm/src/evidence.ts`'s `gatherExecutableEvidence`: E1's investigation loop may name a real, pre-existing repo test file it found via its own read-only tools (never a generated/synthesized test — that is E13's job); this module actually EXECUTES that one file, once, via a bounded (default 15s) `node:child_process execFile` `vitest` subprocess run (no shell interpolation) and treats it as evidence only if it genuinely FAILS against current code. No test named, a passing test, or a launch failure are all fail-safe-equivalent to "no evidence" — the candidate stays in the Unconfirmed appendix. A live-DAST evidence route is documented (reusing the same `live.ts` engine already attempted for that finding) but a fresh, independent second live probe was not implemented — scope decision, see `investigation-pipeline.ts`.)
- [x] **(E3)** Replace the fixed DAST payload `switch` with an adaptive exploit agent that sends a probe, reads the response, reasons, and adapts. Keep every existing guard unchanged — allowlist, production block, kill switch, rate limits, blast-radius caps — as the sandbox it runs inside. (Layered ALONGSIDE the fixed path, not a replacement: `packages/confirm/src/live.ts`'s fixed `craftProbes`/`oracle` single-payload probe still runs first for every live-eligible finding; the new `runAdaptiveLoop` only engages when that verdict is AMBIGUOUS — not a clean hit, not a clean miss — and picks one variant id from a small hardcoded per-category `PAYLOAD_VARIANTS` catalog via `deps.llm`, bounded by a hard `MAX_ADAPTIVE_ROUNDS = 3` cap independent of the gateway's own A2 budget guard. Every adaptive probe passes through the same `ScopeGuard` gate as the fixed path; the kill switch is re-checked every round and immediately after every LLM call, proven by a dedicated test.)
- [x] **(E4)** Add multi-agent adversarial confirmation: N independent verifiers with distinct lenses (exploitability, reachability, business impact, "try to refute this"), majority vote. Attacks the precision/recall tradeoff directly and yields a confidence score for E9. (New `packages/confirm/src/adversarial.ts`'s `runAdversarialVerification`: up to four independent, sequential verifier calls — exploitability, reachability, business_impact, and an explicitly adversarial refutation pass shown the other three verdicts and instructed to try to disprove the finding — over a candidate that already cleared E2's executable-evidence gate. Requires a STRICT MAJORITY (3 of 4 by default; `floor(N/2)+1` for any configured N) to promote; an errored or unparseable verifier call always counts as a reject vote, never a silent confirm, proven by a dedicated test. New `packages/confirm/src/investigation-pipeline.ts`'s `attemptInvestigationConfirmation` ties E1→E2→E4 into the one path `confirm.ts` calls as a last resort; a confirmed finding from it gets a `"static"`-kind `ProofArtifact` since no new proof-artifact kind was added to `packages/contracts` (out of this task's scope) — its `argument` embeds the investigator's rationale, the evidence summary, and the full per-lens verifier tally. A confidence score per se (for E9) is not separately surfaced — the vote tally (`confirmVotes`/`totalVerifiers`) is the closest equivalent today.)
- [x] **(E5)** Build a semantic codebase index (pgvector or on-prem equivalent) over AST-chunked code, built once per commit alongside the App Map and reused by diff scans. Gives cross-file context without blowing the token budget and turns one confirmed finding into a swept class of findings. (New `packages/semantic-index`: chunks a repo into function/method/class-sized units reusing `packages/appmap`'s own ts-morph/web-tree-sitter parser loaders (additively re-exported from `packages/appmap/src/index.ts`), embeds via a new `packages/llm-gateway/src/embeddings.ts` `EmbeddingProviderAdapter` (Azure OpenAI only today — Anthropic has no embeddings endpoint; Bedrock/Vertex are documented protocol-specific follow-ups), and stores/queries vectors through a new `CodeChunk` Prisma model (`Unsupported("vector(1536)")`, migration `5_semantic_code_index`, `packages/state-store/src/code-chunk.ts`'s raw-SQL `PrismaCodeChunkRepository`, deliberately outside the main `StateStore` aggregate). Two entry points — `buildSemanticIndex`/`querySemanticIndex` — are complete and tested (chunking against a real fixture repo, embedding batching, and deterministic cosine-similarity ranking against a synthetic index) but NOT wired into `packages/correlation`/`packages/confirm` (both were off-limits — concurrent work was touching them); `scripts/check-unwired-seams.mjs` records this as a known, documented exception. The required Postgres `pgvector` extension is also NOT yet in `deploy/docker`'s or `deploy/helm`'s bundled Postgres images — documented as a required follow-up, not applied to `deploy/` in this task. See `docs/modules/semantic-index.md`.)
- [x] **(E6)** Add a threat-model agent at Layer 0.5 that derives trust boundaries, attack surface and abuse cases from the App Map, then uses them to **drive which rules and probes run** — the shift from "run every rule and filter" to "hunt this app's actual risks". (Implemented as a SUB-STEP inside Layer 0's own execution, not a new pipeline layer — `LayerId`'s closed six-member union would have required touching `packages/contracts/src/queue.ts`, every layer-keyed structure in `packages/orchestrator`, and persistence/resume-token shapes for a step with no gate or independent retry policy, so `controller.ts`/`queue.ts` are unchanged. New `packages/appmap/src/threat-model.ts`: `buildDeterministicThreatModel` always runs (no LLM required) — trust boundaries from route `authState` groupings, and a per-category attack-surface plausibility rating grounded in real taint-sink kinds/data-store kinds/A18 route→model fan-out (e.g. no `deserialize`-kind sink anywhere ⇒ `insecure_deserialization`/`xxe` rated zero-surface, the audit's own worked example). `deriveThreatModel` additionally lets an LLM APPEND abuse cases + priority-category hints on top of that baseline when a gateway is wired, validating every proposed route path/category against the real App Map/taxonomy and never letting the model remove or downgrade a deterministic entry. Attached to new optional `AppMap.threatModel` (in-memory-only, same limitation as A18's `referencedModels`). New `packages/discovery/src/threat-model-scope.ts`'s `applyThreatModelScopeHints` consumes `threatModel.scopeHints` to annotate + stable-sort Layer 1 candidates by priority — purely additive, proven by a test asserting candidate count/id-set is unchanged, only reordered/annotated. `packages/confirm/src/types.ts`'s `ConfirmInput` already carries the full `AppMap`, so `appMap.threatModel` is already reachable there with zero interface change; Layer 3 actually consuming it is deliberately deferred, `packages/confirm/src/**` untouched.)
- [ ] **(E7)** Ship the blue-team half to make the product genuinely purple. Umbrella item — decomposed into the **Blue team build** section below; complete when B1–B12 land.
- [x] **(E8)** Add cross-scan memory: persist per-repo learned facts (custom sanitizer names, framework idioms, confirmed false positives, operator decisions) and inject them into later scans' prompts. Genuinely closes the PRD §15 loop that A10 shows is dead code.
- [x] **(E9)** Implement dynamic model-tier escalation — cheap model first, escalate on low confidence or verifier disagreement. The `triage`/`default`/`confirmation` tier machinery exists and nothing uses it adaptively. (New `packages/llm-gateway/src/escalation.ts` — `evaluateConfidence` judges a response low-confidence via a self-reported JSON `confidence` field (forward-compatible; nothing emits it yet), `stopReason === "refusal"`, or a JSON-mode response that fails `JSON.parse` — no prompt changes required anywhere. `gateway.ts`'s `complete()` gained an OFF-by-default `escalation` constructor option (mirrors the `budgetRegistry`/`fallbackModel` pattern): when enabled, a tier-routed (never model-pinned) low-confidence response is retried against the next tier up (`models.ts`'s new `nextTier`), bounded by `maxEscalations` and by never escalating past `confirmation`; every escalated attempt still gets the full retry/fallback/budget-guard treatment. `LLMResponse` gained an additive `confidence` field. A different mechanism from A11's fallback cascade — that reacts to failure and swaps model; this reacts to a successful low-confidence response and walks tier. Not wired into any real call site: `packages/discovery/src/triage.ts`'s existing `{tier:"triage"}` request already qualifies with zero changes, but actually enabling it requires an `apps/worker/src/main.ts` gateway-construction change outside this task's scoped file list — built and fully tested standalone, mirroring how A10/A31 handled streaming/Batch API.)
- [x] **(E10)** Modernize the LLM gateway, which unblocks most of the above: tool use / function calling, real structured outputs with JSON schema, prompt caching on the stable system+AppMap prefix, adaptive thinking with an `effort` setting for confirmation, streaming wired through to the console, real token counting, and model fallback. (7/7 now DONE: tool use A8, structured outputs A13, prompt caching A31 (App Map second cache point remains a documented deferral, not this item's job to force), effort/adaptive-thinking A8, real token counting A19, model fallback A11, and the 7th — "streaming wired through to the console" — closed out: `packages/confirm/src/types.ts`'s `ConfirmDeps` gained an additive `emitProgress?: (phase, pct, message?) => void` field mirroring `LayerRunContext.emitProgress`'s signature exactly; `apps/worker/src/runners.ts`'s `layer3` runner threads `ctx.emitProgress` straight through (identical pattern to `signal`/`logger`); `packages/confirm/src/investigate.ts`'s `runInvestigation` (E1) now calls it once per tool-call turn with phase `"investigating"` and a message derived from the real tool call that turn made. Purely additive — omitting `emitProgress` (every prior caller/test) leaves the investigation loop byte-identical to before, proven by a new regression test. Reaches `apps/web/src/components/layer-progress.tsx` with zero console changes, since it already renders a live phase/pct string per layer (A5). All 7 sub-items done.)
- [x] **(E11)** Build an AI-application security ruleset that scans _target_ apps for prompt injection, unsafe tool/function exposure, unescaped LLM output rendered to users, secrets leaking into prompts, and missing output validation. Add a `prompt_injection` category to the taxonomy.
- [x] **(E12)** Build the autonomy surface: `montr scan .` CLI, a GitHub App running diff-mode per PR with review annotations, and scheduled continuous scanning with delta-only reporting. (CLI + webhook receiver: done, A15. Scheduled scanning itself was already fully real and verified — `apps/api/src/routes/schedules.ts` CRUD with cron validation/`nextRunAt`, `apps/worker/src/scheduling`'s real BullMQ repeatable-job dispatch via `scan-scheduler.ts`'s `trigger()`, hard per-run `budgetCeiling` + human gate preserved (`scheduledBudgetPolicy`, no gate auto-approval) — but always produced a standalone full report with no comparison to the previous run. Closed: new `GET /schedules/:id/delta` finds the two most recent completed/partial scheduler-triggered scans for a schedule's repo (matched via `Scan.operator === "scan-scheduler"`, the marker `scheduledScanInput` already stamps — no new `Scan` field needed) and diffs confirmed findings by (category, file, line) via new pure `computeFindingsDelta`, returning `newFindings`/`resolvedCount`. Known, documented limitation: without a `Scan.scheduleId` back-reference (a contract-and-migration change out of scope), two schedules for the same repo+client are indistinguishable. A full GitHub App remains explicitly out of scope, per A15.)
- [x] **(E13)** Implement real proof-of-fix in an ephemeral container — stand the app up, replay the confirmed exploit, assert it now fails, attach the transcript. Also provides E2's evidence mechanism and E3's oracle. (New `packages/fix/src/container-harness.ts` builds/starts a genuinely ephemeral Docker container for a target checkout — a Dockerfile if the target has one, else a minimal synthesized `node:20-alpine` Dockerfile for a `package.json` "start"/"dev" script, else out of scope for this pass (no Python/JVM target containerization) — on a dedicated bridge network with IP masquerade explicitly disabled, verified empirically to keep loopback-published inbound reachable while the container's own outbound egress fails closed; bounded by one shared timeout budget with guaranteed teardown. New `packages/fix/src/exploit-replay.ts` reuses (read-only) a CONFIRMED finding's own live-DAST `HttpExchange` transcript, replaying the decisive probe and judging success by a real comparison against the originally recorded evidence. New `packages/fix/src/container-validate.ts`'s `validatePatchWithContainerReplay` is a strict, additive upgrade over `patch.ts`'s existing vitest-subprocess `validatePatch`: builds one container from the pre-patch source (must reproduce the exploit) and one from the post-patch source (must not), or falls back unchanged to the vitest mechanism when there's no live-DAST evidence, no target checkout, or a container-infra failure — coverage for non-live-evidence findings is never broken. Wired OFF by default via `generate.ts`'s new `FixGenerationContext.containerProof`; `packages/contracts/src/fix.ts` gained an additive `ProofOfFixTestSchema.containerReplay` field so the full transcript persists on the `Fix` row while the audit event gets only a lean, transcript-free summary. `containerProof.targetRepoDir` is not yet populated by `apps/worker/src/runners.ts` — documented, not-yet-wired, matching E1's `ConfirmInput.repoRoot` precedent. Proven against a REAL, non-mocked Docker container in `tests/fix.container-replay.test.ts`.)
- [x] **(E14)** Build an external benchmark harness scoring against OWASP Benchmark and Juliet, plus a published head-to-head against raw Semgrep and CodeQL on the same corpus, per release. (OWASP Benchmark: real, end-to-end, built and run — `packages/qa/src/owasp-benchmark.ts`/`owasp-benchmark-cli.ts` parse OWASP Benchmark's OWN `expectedresults-*.csv` and score with its OWN TPR/FPR/`score=TPR-FPR` methodology; a real, pinned-commit, honestly-labelled 24-file excerpt is vendored at `corpus/owasp-benchmark/` (GPL-2.0, not Apache-2.0 as this item's brief assumed — corrected in that dir's README); `scripts/benchmark-owasp.mjs` runs the REAL apps/worker pipeline against it. A real measured run completed with real (if disappointing) numbers — montr-secure TPR=0.0%/FPR=0.0%/score=0.0 — and, in investigating why, surfaced a genuine, previously-undiscovered production bug: `packages/discovery/src/rulesets/java/index.ts`'s `p/spring` Semgrep registry pack currently 404s and silently zeroes ALL SAST candidates for every JVM app (verified root cause, not fixed — `packages/discovery/src/**` was out of this task's file scope; documented in `docs/infra/testing.md` and `corpus/owasp-benchmark/README.md`). Semgrep competitor: real, run via `scripts/benchmark-semgrep.mjs` — TPR=83.3%/FPR=33.3%/score=50.0 on the identical subset. Juliet Test Suite and CodeQL are scaffolded, documented follow-ups, not integrated: Juliet was deprioritized entirely (OWASP Benchmark alone consumed the available scope, per this task's own prioritization instruction); CodeQL's `scripts/benchmark-codeql.mjs` checks for the CLI, which is not installed in this sandbox, and prints the exact commands to run it elsewhere rather than fabricating a score.)
- [x] **(E15)** Wire eval-driven prompt optimization: connect the dead prompt registry to the corpus scorer so prompt versions are automatically A/B'd against ground truth with per-version regression gating. (`packages/llm-gateway/src/prompts.ts`'s `PromptVersionSource` gained an optional `listVersions` method (a strict, backward-compatible widening — `PromptVersionRepositoryImpl` already satisfies it structurally) plus a new pure `resolvePromptVersionTemplate` (resolve an EXACT version by number, not just whichever is active) and `MontrLlmGateway.resolvePromptVersion`, mirroring `resolvePrompt`'s fail-safe contract. New `packages/llm-gateway/src/prompt-version-registry.ts`'s `InMemoryPromptVersionRegistry` is a Map-backed `PromptVersionSource` (same name/version/isActive shape as the real Postgres-backed repository) for offline eval without a DB. New `packages/qa/src/prompt-eval.ts`'s `evaluatePromptCandidate` connects this to the golden-corpus scorer: registers active + candidate templates, runs the REAL `@montr/confirm` engine once per version (via a refactored, additive `confirmedForRepoWithGateway` extracted from A23's `real-confirmation-scanner.ts`, marking a different registry row active between runs so the real call site — unmodified — resolves whichever version is under test), scores both with the SAME `scoreScanResults` the CI gate uses, and gates promotion via `evaluateBaseline` against a RELATIVE floor built from the active version's own measured score (candidate must not score worse). Real tests prove the core claim on the golden corpus: identical scores the same, a genuinely worse candidate is rejected, a richer candidate is promoted, and evaluation is deterministic. Not wired into an automated CI job (a real promotion workflow is a follow-up operational decision) — the CORE mechanism is built and tested.)
- [x] **(E16)** Broaden target coverage in priority order: IaC/Dockerfile/Kubernetes/Terraform, SBOM (CycloneDX), supply-chain risk (typosquatting, install scripts, malicious packages), OpenAPI/API-spec review, container image scanning, then cloud posture. (Completed, real and tested, the first THREE of the six sub-areas — depth-first per the audit's own priority order, as instructed. 1) IaC: new `packages/discovery/src/detectors/iac.ts` — real structural detectors for Dockerfile (running-as-root, ADD-vs-COPY, unpinned base image, secrets in ENV/ARG, missing `.dockerignore`), Kubernetes (real YAML parsing — missing resource limits, privileged/hostNetwork/hostPID, secrets as plain env vars, missing NetworkPolicy), and Terraform (block-scoped, string-literal-safe structural scan — hardcoded credentials, permissive security-group/IAM wildcards, unencrypted storage), plus an optional non-required Semgrep pass against the real `p/dockerfile`/`p/kubernetes`/`p/terraform` registry packs reusing `sast.ts`'s parser unmodified. New `insecure_configuration` Category (CWE-16/A05:2021) for pure misconfiguration, with a real Prisma migration mirroring the `prompt_injection` precedent exactly; existing categories reused where they already fit. 2) SBOM: new `packages/discovery/src/sbom.ts`'s `buildDependencyInventory` resolves the FULL dependency tree (not just SCA's vulnerable subset) with real A12 call-site reachability, consumed with zero adapter by new `packages/report/src/exports/cyclonedx.ts`'s real, schema-shaped CycloneDX 1.5 exporter (new `"cyclonedx"` `ExportFormat`) — proven end to end in `tests/sbom.cyclonedx-e2e.test.ts`. 3) Supply-chain: new `packages/discovery/src/detectors/supply-chain.ts` — real Levenshtein-distance typosquat detection against a curated ~150-package popular-npm list, install-script risk scanning (project's own `package.json` always; a resolved dependency's `node_modules/<pkg>/package.json` when actually present on disk), and malicious-package heuristics honestly scoped to what's checkable OFFLINE (non-ASCII/homoglyph names, non-standard `package-lock.json` registry resolution) — "recently published + low downloads" is documented future work needing live npm registry metadata this on-prem product does not fetch. DEFERRED, not attempted: 4) OpenAPI/API-spec drift review, 5) container image scanning, 6) cloud posture — left for a future pass per the audit's own stated lowest-priority ordering. New fixtures: `packages/fixtures/sample-repos/{iac-samples,supply-chain-samples}/{vulnerable,clean}`. Real `node_modules`-based install-script fixtures are impossible to commit (this repo's root `.gitignore` excludes `node_modules/` everywhere by design), so that one check is proven via an in-memory `FileProvider` instead. The CycloneDX export is built and fully tested but not yet wired to a production route/CLI call site — same documented "library shipped, no production caller yet" posture as E5/E9.)

## Blue team build

New scope. The blue-team half was never specified: the PRD names "blue-team hardening" exactly once, in the thesis sentence at line 26, and never again — it appears in no layer of §7 Agent Topology, no entity in the §9 data model, no section of the §12 report spec, and no item in the §19 Definition of Done. The build followed §7 faithfully, and §7 is entirely red and static. These tasks add the missing half. `packages/state-store/src/redteam-catalogue.ts` (`REDTEAM_SCENARIO_CATALOGUE`, `instantiateScenario`, OWASP coverage mapping) and the gated execution engine in `packages/confirm/src/scenarios.ts` are real, working assets to build the purple loop on.

- [ ] **(B1)** Extend the contracts spine with the blue-team entities: `DetectionRule`, `ThreatModel`, `AttackPath`, and `DetectionCoverage`. Follow the existing frozen-spine discipline in `packages/contracts` and add the matching Prisma models plus migrations (`docs/api/database.md` must be updated per the project hard rules).
- [ ] **(B2)** Add MITRE ATT&CK mapping (tactic + technique IDs) per finding category, alongside the existing OWASP/CWE mapping. Reuse the exhaustive `Record<Category, readonly string[]>` pattern from `packages/report/src/exports/controls.ts` so the compiler enforces completeness, and surface the IDs in the report and in SARIF `properties.tags`.
- [ ] **(B3)** Generate detection rules per confirmed finding — Sigma as the primary format, plus OTel/SIEM query variants. Derive them from the confirmed proof: the live DAST transcript where one exists, otherwise the static data-flow path and the route's request shape.
- [ ] **(B4)** Add a "what this looks like in your logs" section per confirmed finding: the concrete log signature, the fields to alert on, and the expected false-alarm sources. This is the single most useful blue-team output for an AppSec buyer and is cheap once B3 exists.
- [ ] **(B5)** Build the purple-team verification loop — the core differentiator. Run a red-team scenario through the existing gated engine (`packages/confirm/src/scenarios.ts`), then verify whether the B3-generated detection rule actually fires against the resulting telemetry. Report detected / undetected per scenario. This is what makes the product genuinely purple rather than red-with-a-report.
- [ ] **(B6)** Implement detection coverage gap analysis: for each confirmed finding, determine whether the target's existing telemetry would catch it. Requires ingesting the target's logging/monitoring configuration during Layer 0 — extend the App Map with a `telemetry_surfaces` field.
- [ ] **(B7)** Generate a threat model artifact at Layer 0.5: trust boundaries, STRIDE per element, and abuse cases derived from the App Map. Distinct from E6, which uses the same analysis to drive scan scope — this task produces the reviewable deliverable.
- [ ] **(B8)** Build an attack-path graph across confirmed findings, chaining them into realistic kill chains (e.g. public route → SSRF → metadata endpoint → credentials). Rank paths by end-to-end feasibility rather than scoring findings in isolation.
- [ ] **(B9)** Generate hardening recommendations distinct from code fixes: security headers, CSP, cookie policy, rate limits, WAF rules, network policy, and framework configuration. These are blue-team config outputs, not patches, and must not flow through the Layer 4 auto-fix risk classifier.
- [ ] **(B10)** Add a blue-team section to the report and extend the compliance exports so detection coverage counts as SOC 2 / ISO 27001 evidence. Reuse the existing control-catalog and evidence-record machinery in `packages/report/src/exports/`.
- [ ] **(B11)** Build the console UI for the blue team: a detection-rules page with export, an ATT&CK coverage matrix/heat map, the attack-path graph view, and purple-loop results (which scenarios were detected).
- [ ] **(B12)** Build a blue-team golden corpus with labelled detection ground truth — which scenarios should fire which rules — and add a CI regression gate for detection precision/recall, mirroring the existing `corpus/baseline.json` gate so the blue side is measured as rigorously as the red side.
