> Source audit: [26-09-12-audit-red-blue-agentic-posture](./26-09-12-audit-red-blue-agentic-posture.md)
> Updated: 26-09-12 · 24/32 done

- [x] **(A1, P0)** Wire worker-side red-team scenario execution — supply the real `transport` to `runScenario` through the existing `assertScenarioAuthorized` + `ScopeGuard` gates (no new egress path; `packages/confirm/src/purple-loop.ts` already demonstrates the exact call shape), so `POST /scenarios/:id/run` actually probes instead of authorizing and sending nothing. **Depends on A10** — landing this without a request-body field ships an engine structurally blind to ~a quarter of its own catalogue. As an immediate, independent honesty fix, change `RunOutcome` in `apps/web/src/app/scenarios/page.tsx` to stop claiming "probing executes in the worker" until it does.

  RESOLVED. Owner decision (2026-09-12): ship real execution, gated behind explicit PER-TARGET WRITTEN authorization — beyond RBAC + allowlist, an Approver must record an auditable free-text authorization reference before any real probe can ever be sent. New `RedTeamScenario` columns (`liveAuthorizedById`, `liveAuthorizationReference`, `liveAuthorizedAt`, `liveAuthorizedForVersion` — additive Prisma migration `12_scenario_live_authorization`), bound to the scenario's exact version so any edit invalidates a prior grant. New approver-only `POST /scenarios/:id/authorize` records the grant (audited `scenario.authorized`); `POST /scenarios/:id/run` now hard-gates on `hasLiveRunAuthorization` (`packages/contracts/src/phase4.ts`) before doing anything else — missing/stale authorization is a clear, honest 403 (audited `scenario.live_run_rejected`), never a silent no-op. Once authorized, the route keeps its existing no-transport gate-only preview/audit, then enqueues a REAL job onto a new dedicated, non-pipeline BullMQ queue (`apps/api/src/scenario-run-producer.ts` → `apps/worker/src/scenario-runs`), consumed by `processScenarioRunJob`, which re-checks every gate (enabled, production-blocked/kill-switch invariants, written authorization) before building `@montr/confirm`'s existing `defaultTransport` (same transport Layer 3 and A8's purple-team loop already use — no new egress path) and calling the existing `runScenario` for real; audited `scenario.live_run_enqueued`. `apps/web/src/app/scenarios/page.tsx` now shows a live-run-authorization status badge and an approver-only authorization form, and `RunOutcome` reports honestly whether a real worker job was enqueued instead of unconditionally claiming probing "executes in the worker." 32 new tests (`packages/contracts/src/phase4.test.ts`, `apps/api/src/routes/scenarios.test.ts`, `apps/worker/src/scenario-runs/service.test.ts` — including two real end-to-end runs against a live local HTTP server proving authorized execution genuinely probes and is audited, and proof every gate blocks _before a transport is ever constructed_). 3 pre-existing tests in `tests/scenarios.api.test.ts` that predated this change (asserting the old no-authorization-required behavior) were updated to authorize first, matching the new intentional design — verified this is a deliberate behavior change, not a regression, since the owner's decision was to gate ALL real-transport-adjacent runs (including the harmless gate-only preview) behind one clear authorization requirement. Full scoped suite green (contracts/state-store/confirm/api/worker: 610+ passed) plus the 2 pre-existing, unrelated `e2e-scan.test.ts` failures (environmental, documented elsewhere). Updated `docs/modules/confirmation.md`, `docs/api/database.md`, `docs/auth/authorization.md`, `docs/api/route-handlers.md`.

- [x] **(A2, P0)** Make the Layer 5 runner call `resolveAppMap(ctx)` (already used identically in Layers 1–3) and thread it into `buildReport`, so detection coverage, attack paths and the threat model stop rendering as permanently empty panels in the console's Blue Team tab. The threat model is already computed and persisted on the AppMap record at Layer 0 — it is being discarded, not missing.

  RESOLVED (landed together with A7/A8/A13 — see docs/overview.md's 2026-09-12 "A2/A7/A8/A13" entry for the full writeup). `apps/worker/src/runners.ts`'s Layer 5 runner now calls `resolveAppMap(ctx)` exactly like Layers 1-3 and threads the result into `buildReport`'s `appMap` input. Detection coverage, attack paths, and threat-model sections in `packages/report/src/report-builder.ts`'s `buildBlueTeamReport` now compute for real. No changes needed to `buildBlueTeamReport` itself — this was purely a caller-side wiring gap. Verified via new `apps/worker/src/runners.test.ts` cases and 2 pre-existing Layer 5 tests fixed (their fixtures had no App Map, which A2 now requires).

- [x] **(A3, P0)** Add a `confirmation.investigation` config section mirroring `FixAgentLoopConfigSchema`'s shape and caps, surface it via env vars (updating `docs/infra/environment.md`), and populate `ConfirmDeps.investigation` in the Layer 3 runner — making the agentic investigation loop (and the E4 adversarial verifier panel, reachable only through it) reachable without a code change for the first time. Scope it initially to unconfirmed high/critical findings. See the Decisions note below before choosing the default.

  RESOLVED. Owner decision (2026-09-12): default `enabled: true` — a deliberate deviation from every other agentic-loop toggle in this codebase (A5's `fixGeneration.agentLoop`, A9's `semanticIndex`, both OFF by default) — because `idor`/`broken_access_control` have zero static data-flow proof (`taxonomy.ts`'s `DATAFLOW_SINK_KINDS`) and this is the only static-scan path that can ever confirm them. New `ConfirmationInvestigationConfigSchema` (`packages/config/src/schema.ts`): `enabled` (default true), `maxTurns` (default 6, capped at 8 — `investigate.ts`'s own unconditional `ABSOLUTE_MAX_INVESTIGATION_TURNS` ceiling), `verifierCount` (default 4, capped at 4 — `adversarial.ts`'s 4-lens panel), and `severities` (default `["high", "critical"]`) — the scoping field, using `Severity` from `@montr/contracts` (no new field invented). Four env vars: `MONTR_CONFIRMATION_INVESTIGATION_ENABLED`/`_MAX_TURNS`/`_VERIFIER_COUNT`/`_SEVERITIES`. Scoping mechanism: `ProbableFinding` carries no severity field (severity is assigned only at confirmation) — `packages/confirm/src/confirm.ts` gained `isEligibleForInvestigation`, which computes a not-yet-confirmed finding's severity via `deriveSeverity(category, exposure)` (`taxonomy.ts`, the SAME function that assigns a confirmed finding's severity) and gates the call to `attemptInvestigationConfirmation` on it — an ineligible finding never even reaches the investigation loop, so this is a real cost gate, not just an outcome filter. `apps/worker/src/runners.ts`'s Layer 3 runner now populates `ConfirmDeps.investigation` from `ctx.config.confirmation.investigation`, mirroring A5's `fixGeneration.agentLoop` wiring convention exactly. Budget hard-halt: verified, not re-wired — every investigation/adversarial-verifier LLM call already carries the scan's real `metadata.scanId` and flows through the SAME `LLMGateway` instance every other layer uses, so the existing A2 pre-call budget guard (`packages/llm-gateway/src/gateway.ts`'s `assertPreCallBudget`, keyed off `budgetRegistry.get(scanId)`, registered per-scan in `packages/orchestrator/src/controller.ts`) already covers this spend with zero code changes. Traced one real path end to end: Layer 3 runner → `confirmFindings` → `attemptInvestigationConfirmation` (`investigation-pipeline.ts`) → `runAdversarialVerification` (`adversarial.ts`) — confirmed reachable for the first time (previously blocked entirely on this finding, per A17's note). 18 new tests: `packages/config/src/confirmation-investigation-config.test.ts` (15), 3 new cases in `tests/confirm.investigation.test.ts` (severity-scoping is a cost gate, not just an outcome gate), 3 new cases in `apps/worker/src/runners.test.ts` (production-default reachability, explicit-disable no-op, severity-scoping end to end via the real runner) — 21 new tests total. Full `packages/confirm`/`packages/config`/`apps/worker` scoped suites green except two PRE-EXISTING, unrelated `apps/worker/src/e2e-scan.test.ts` failures in `live-scanners` discovery mode (a real SQL-injection candidate never reaches Layer 2's `probable` output at all — entirely a Layer 1/2 discovery/correlation-time issue, traced and confirmed to occur before Layer 3 (and therefore this change) is ever reached; most plausibly a sandboxed-environment/network dependency on the hosted Semgrep registry, not caused by this or the concurrent A11 change). Real behavior/cost change flagged prominently in `docs/overview.md`'s Recent Changes entry (every operator upgrading gets this ON by default) and `docs/infra/environment.md`. Updated `docs/modules/confirmation.md`, `docs/infra/environment.md`, `docs/overview.md`, `tests/config.test.ts`.

  **Same-day correction (coordinator pass, post-landing):** the original `isEligibleForInvestigation` used the exposure-discounted `deriveSeverity(category, exposure)` for the eligibility gate, which downgrades a non-public-exposure finding one tier below its category's base. Since `idor`/`broken_access_control` are both base "high" and the overwhelmingly common real-world instance of either is authenticated-only (not anonymous-public), that derived them to "medium" — outside the default `["high", "critical"]` scope, meaning the feature would have silently missed most real-world IDOR/broken-access-control findings, the exact categories it exists to catch. Fixed by adding `baseSeverityForCategory` (`packages/confirm/src/taxonomy.ts`) and using it for eligibility instead of `deriveSeverity` — idor/broken_access_control are now always in scope under the default regardless of exposure. A finding's final reported severity is unaffected (still correctly exposure-discounted at confirmation time via `deriveSeverity`); only the investigation-eligibility cost gate changed. Updated the 3 severity-scoping tests in `tests/confirm.investigation.test.ts`, `packages/confirm/src/types.ts`'s doc comment, `docs/modules/confirmation.md`, `docs/infra/environment.md`, and `docs/overview.md`'s A3 entry to match.

- [x] **(A4, P0)** Close the golden-corpus precision regression: FP-rate 12.5% / precision 87.5% against the committed 5% / 90% thresholds, from two false positives (sql_injection, command_injection) present since the 2026-08-19 A17 recalibration. Do not loosen the baseline — its own header says tighten, never loosen. Carried over from `26-09-09-tasks-july-line-divergence.md`; this gates the AI-autonomy work above.

  RESOLVED — not a detector/scoring defect. Re-ran the real pipeline (`node scripts/corpus-scan.mjs`, real semgrep + gitleaks installed locally) and re-scored (`pnpm --filter @montr/qa qa:corpus`). Current measured result: **FP-rate 0.0%, precision 100.0%, recall 34.8% (TP=16 FP=0 FN=30), baseline gate PASS** — the two flagged cases from this finding's own description are gone. Investigating why (root-cause, not just re-measuring) found this item had already been closed concurrently, as `26-09-09-tasks-july-line-divergence.md`'s **A11**, in commit `4ff1c73` (same working tree, landed earlier the same day): both "false positives" were real bugs in `corpus/javaseccode/ground-truth.manifest.json`, not the scanner. `SQLI.java:149` (`jdbc_ps_vuln`) string-concatenates the untrusted `username` parameter into the SQL text _before_ calling `PreparedStatement` — the `?` placeholder is never used, so parameterization buys nothing; verified by reading the vendored source directly. `CommandInject.java:45` (`codeInjectHost`, doc comment "Host Injection") concatenates the untrusted `Host` header into a `sh -c curl <host>` command via `ProcessBuilder`; also verified against source. Both are second, distinct vulnerability instances in files whose manifest had previously labelled only one instance per category — the scanner was correctly flagging real vulnerabilities the ground truth had never catalogued, the same failure mode `corpus/README.md`'s sampling-method section already documents happening once before with PyGoat (`gt_pygoat_cmdi2`). The fix was adding `gt_javaseccode_sqli2`/`gt_javaseccode_cmdi2` to the manifest, per that established precedent — `corpus/baseline.json`'s thresholds were never touched, and no corpus fixture or detector code was special-cased. No further code change was needed here; this entry independently reproduces and confirms that closure rather than re-doing it. `packages/qa`'s test suite re-run clean (136/136 across `tests/qa.*.test.ts` + `packages/qa/src/*.test.ts`) — no packages/discovery or packages/correlation code was touched by either fix, so no further test suites were in scope.

- [x] **(A5, P1)** Add a top-level Blue Team nav section to `NAV_SECTIONS` with cross-scan aggregation — org-wide ATT&CK coverage over time, a detection-rule inventory with export-all, and a detection-coverage trend — so blue-team output is not reachable only by opening an individual scan and finding a tab.

  RESOLVED. New viewer-visible `NAV_SECTIONS` entry (`apps/web/src/lib/rbac.ts`) links to a new org-wide aggregate page, `apps/web/src/app/blue-team/page.tsx`, following the same real-API-only, self-contained-hooks pattern as `app/dashboards`. Backed by a new `GET /analytics/blue-team` route (`apps/api/src/routes/analytics.ts`) that reads only real, already-persisted data — no new persistence: each scan's already-built `Report.blueTeam.mitreAttack.coverage` (B2/B10) for org-wide ATT&CK coverage merged across scans, and the real, persisted (A7) `DetectionRule`/`DetectionCoverage` repositories (newly exposed read-only on `ApiStore`) for a cross-scan detection-rule inventory (deduped by format+content) and a per-scan detected/undetected/unknown coverage trend. The page reuses B11's own `AttackHeatMap` component directly (same `MitreTechniqueCoverageShape[]` input) and `lib/exports.ts`'s `downloadDetectionRule`/`downloadDetectionRuleBundle` for per-rule/export-all downloads — no new export mechanism invented. 6 new `apps/api/src/routes/analytics.test.ts` tests (dedup, cross-scan merge, chronological trend, client isolation, viewer-visibility) plus manual verification against a real seeded in-memory API (multi-scan/multi-repo fixture) via curl and a real browser render of the page shell/nav/error-state; full in-browser live-data rendering wasn't captured because the console's session cookie is `SameSite=Strict` by design, which blocks any cross-origin fetch without a same-origin reverse proxy — a pre-existing constraint shared by every other real-API page (dashboards/rules/scenarios), not introduced here. `tsc -b`/`next build` clean on contracts/api/web; full `apps/api` vitest suite green (79 tests). See `docs/overview.md`'s 2026-09-12 "A5" entry for the full writeup.

- [x] **(A6, P1)** Add at least one real end-to-end assertion (natural home: the existing `pnpm e2e` scan path) that a genuine pipeline run produces non-empty blue-team report sections, so mock-backed console verification can never again pass against fields production leaves empty.

  RESOLVED. New dedicated describe block in `apps/worker/src/e2e-scan.test.ts` (separate scan/store from the shared, environment-dependent `run`, since the existing `vulnerable-nextjs` fixture's two confirmed categories can never satisfy an attack-path chain condition regardless of whether A2's wiring works). Six new tests assert real, non-empty `detectionEngineering.coverage` (B6), `attackPaths` (B8, a genuine 2-hop critical-severity chain), `threatModel.present` (B7) with real content, a well-shaped `hardening` advisory result (B9), and `mitreAttack` mapping (B2). New minimal fixture `packages/fixtures/sample-repos/attack-chain-nextjs` (SQLi + command-injection routes, pairing to unconditionally form an RCE-class attack-path chain) — not part of the golden corpus, doesn't affect `corpus/baseline.json` grading. Purple-team (B5) deliberately left uncovered here (needs a real approver-gated live-DAST run, already covered in `apps/worker/src/runners.test.ts`). **Verified as a genuine regression guard, not just claimed**: temporarily reverted A2's `resolveAppMap(ctx)` call in the Layer 5 handler, confirmed 3 of 6 new tests correctly went red (coverage/attackPaths/threatModel — the A2-dependent ones) while the 2 A2-independent tests stayed green, then restored the original code and confirmed all 6 pass again; verified no trace of the temporary bypass remained. Final suite: 19/21 `e2e-scan.test.ts` tests pass — the 2 failures are exactly the pre-existing, documented, unrelated live-scanners-mode failures. Updated `docs/infra/testing.md`.

- [x] **(A7, P1)** Call `persistDetectionCoverageForScan` from the Layer 3 runner so the tri-state detection-coverage verdict is actually written to the B1 repositories instead of being computed by nothing.

  RESOLVED (landed together with A2/A8/A13). Layer 3's runner calls `packages/appmap/src/coverage-analysis.ts`'s `persistDetectionCoverageForScan` immediately after `confirmFindings` resolves the confirmed set — best-effort, wrapped in try/catch so a store hiccup logs a warning and never fails confirmation. Verified via a new end-to-end `apps/worker/src/runners.test.ts` case running an actual scenario and persisting a genuine live verification onto the resulting `DetectionCoverage` row.

- [x] **(A8, P1)** Wire the purple-team verification loop into the pipeline and feed `purpleTeamEntries` into `buildReport`, so Sigma-rule-would-have-fired verification runs for real customers rather than only inside `packages/qa`'s corpus harness.

  RESOLVED (landed together with A2/A7/A13). Wired into Layer 3 (not Layer 5 — Layer 5's job data has no `allowLive`/`stagingUrl`, so Layer 3 is the only place with legitimate live-DAST authorization). Gated on the same approver-authorized live-DAST toggle (`job.allowLive`/`stagingUrl`) Layer 3's own live confirmation already uses. Scenario selection is category-based, mirroring `packages/qa/src/blue-team-corpus.ts`'s hand-traced pattern via the newly-exported `REDTEAM_CATEGORY_TO_FINDING_CATEGORIES`, matched against the client's real enabled `RedTeamScenario` catalogue rows. Reuses `packages/confirm/src/live.ts`'s own `defaultTransport` (newly exported) — no new egress path. Results reach Layer 5 through the orchestrator's in-process `priorOutputs` cache (`Layer3OutputSchema` gained an additive, non-persisted `purpleTeamEntries` field, mirroring `Layer2Output.demoted`'s precedent) and feed `BuildReportInput.purpleTeamEntries`. Verified via new tests including a real scenario run against a real local HTTP server, and a safety-gating test proving refusal when `dast.enabled` is false.

- [x] **(A9, P1)** Implement an OpenAI embeddings adapter (same wire shape the existing Azure adapter already speaks) so the semantic index is usable on more than 1 of 10 providers, then wire `buildSemanticIndex`/`querySemanticIndex` into correlation/confirmation and expose semantic retrieval to the A3 investigator alongside `grep`.

  RESOLVED — `packages/llm-gateway/src/embeddings.ts` gained a real `OpenAiEmbeddingAdapter` (same `openai` SDK `embeddings.create` wire shape the Azure adapter already speaks), wired into `createEmbeddingAdapter`'s provider switch for `openai`; the other five OpenAI-compatible providers remain honest `NotImplementedError` stubs (out of this item's explicit scope). `packages/semantic-index` is now a real pipeline consumer: `packages/appmap/src/build.ts` gained a best-effort, structural `semanticIndex` hook called at Layer 0 (any failure caught and logged, never fails the scan); a new OFF-by-default `SemanticIndexConfigSchema` (`MONTR_SEMANTIC_INDEX_ENABLED`/`MONTR_SEMANTIC_INDEX_EMBEDDING_MODEL`) gates real construction in `apps/worker/src/main.ts`, requiring `llm.provider` to be `azure` or `openai`. `apps/worker/src/runners.ts` wires the result into Layer 0 (index build), Layer 2 correlation (`CorrelateInput.semanticSearch`, purely informational — never touches the golden-corpus-calibrated scoring formula), and Layer 3 confirmation, which gained a new seventh read-only investigation tool, `semantic_search`. Every capability degrades to a logged skip or an honest "not available" tool result when unavailable. `scripts/check-unwired-seams.mjs` moved `createEmbeddingAdapter`/`buildSemanticIndex`/`querySemanticIndex` from the informational `KNOWN_UNWIRED` list into the enforced `SEAMS` list, so CI now fails if any of the three ever loses its real caller. 44 new/updated tests, full scoped suite green, `tsc -b` clean on every touched package. Updated `docs/modules/semantic-index.md`, `docs/modules/llm-gateway.md`, `docs/modules/confirmation.md`, `docs/modules/correlation.md`, `docs/modules/appmap.md`, `docs/infra/environment.md`.

- [x] **(A10, P1)** Add a request-body field to `RedTeamStep` and send it from `runScenario`, so the three catalogue scenarios whose confirming payload lives in a POST body (command_injection, insecure_deserialization, ssrf) can actually prove anything, and so generated Sigma rules keyed on `cs-body|contains` can structurally fire. Prerequisite for A1 being worth doing.

  RESOLVED — `RedTeamStepSchema` (`packages/contracts/src/phase4.ts`) gained an additive, optional `body: z.string().optional()` field. `runScenario` (`packages/confirm/src/scenarios.ts`) now forwards `step.body` to `deps.transport.send` when a step defines one, and `toExchange` records it on the transcript exchange as `request.bodySnippet` — both changes are purely additive; a step without a `body` behaves byte-identical to before. The three catalogue scenarios whose confirming payload is a POST body (`owasp-a03-command-injection`, `owasp-a08-software-data-integrity`, `owasp-a10-ssrf` in `packages/state-store/src/redteam-catalogue.ts`) now carry their real confirming payload on the relevant step — a `$(id)` command-injection marker, a Jackson-style `"@type"` polymorphic-deserialization type marker, and a loopback/metadata SSRF target URL respectively, each a literal `CATEGORY_MARKERS` entry (`packages/report/src/detection-rules/context.ts`) so the generated Sigma rule's `cs-body|contains` condition genuinely matches. `packages/confirm/src/purple-loop.ts`'s `evaluateSigmaRule`/`evaluateDetectionRuleAgainstScenario` needed no logic change (they already read `request.bodySnippet` and evaluate `cs-body|contains` against it) — only the module header and the per-request "no body" gap message were corrected, since the gap is now per-step (a step that defines no body) rather than universal. `packages/qa/src/blue-team-corpus.ts`'s `BLUE_TEAM_GROUND_TRUTH` relabelled all three previously `expectedFired: false` cases to `true`, with updated per-case traces. Real measured result via `node scripts/blue-team-corpus-scan.mjs` + `pnpm --filter @montr/qa qa:blue-team-corpus`: **TP=9 FP=0 FN=0 TN=0 across all 9 labelled scenarios (up from TP=6/TN=3), detectionPrecision=100.0%, detectionRecall=100.0%, blue-team baseline gate PASS** — confirmed against the real B3 rule generator + real B5 evaluator, not hand-computed. Consequence documented rather than hidden: with zero labelled negative cases left, `detectionPrecisionMin` (`corpus/blue-team-baseline.json`) is now a dormant floor for this specific corpus (no case can ever produce a false positive); `detectionRecallMin` remains live, and negative/near-miss coverage of the evaluator itself continues via `tests/confirm.purple-loop.test.ts`'s hand-built cases, independent of the corpus. Two pre-existing test assertions were updated because they encoded the old, now-fixed bug as an invariant (both were about the labelled corpus/measurement containing a false case, not about product behavior): `tests/qa.blue-team-corpus.test.ts`'s "not a trivially one-sided corpus" check and `scripts/blue-team-corpus-scan.run.test.ts`'s "not a tautological echo" check, both now assert real per-case evidence variety instead. Two new tests added: `tests/confirm.scenarios.test.ts` proves `runScenario` forwards a step's body to the transport and records it on the transcript (and that a body-less step sends no `body` key at all), and `tests/confirm.purple-loop.test.ts` proves `runPurpleTeamScenario` fires `true` when the confirming marker lives only in a step's body. Full scoped suite (`packages/contracts`, `packages/confirm`, `packages/state-store`, `packages/qa`, plus the `tests/confirm.*`/`tests/qa.blue-team-*`/`tests/report.blue-team`/`tests/scenarios.api`/`scripts/blue-team-corpus-scan.run` integration tests) passes clean: 526 passed, 2 pre-existing skips. `pnpm turbo run typecheck` clean across `@montr/contracts`, `@montr/state-store`, `@montr/confirm`, `@montr/report`, `@montr/qa`, and `@montr/web` (consumes `RedTeamStep` in `apps/web/src/app/scenarios/`). No Prisma/DB schema change — `RedTeamScenario.steps` was already an encrypted Json blob; `body` is a new optional field inside the existing TS shape stored in it, not a new column. Updated `docs/modules/confirmation.md`, `docs/infra/testing.md`, `corpus/blue-team-baseline.json`. A1 (worker-side scenario execution wiring) remains genuinely blocked on being worth doing until it lands — this closes the specific prerequisite A1 names.

- [x] **(A11, P1)** Add adaptive scoping and effort allocation to the orchestrator so it spends more turns on findings with high reachability × exposure × impact and skips expensive confirmation on findings the App Map already rules out — the first change that lets the model influence control flow rather than only content, and a cost reduction as well as a capability add.

  RESOLVED — landed inside `packages/confirm`, not `packages/orchestrator`'s `fsm.ts`/`controller.ts` (those stay a fixed, predictable layer sequence by design, per this finding's own framing; the scoping decision is bounded and lives entirely within the existing Layer 3 confirmation step, not the layer sequence itself). New `packages/confirm/src/structural-gate.ts`'s `structuralConfirmationGate`, called from `confirm.ts`'s per-finding loop right after the existing §15 FP check and before any static/live/investigation work: a bounded, deterministic **existence check**, never a score threshold — when a `ProbableFinding` carries a `routeId`, it verifies that route still appears in the exact App Map Layer 3 is confirming against, and routes it straight to the Unconfirmed appendix (`toUnconfirmed` + a `finding.demoted` audit event, `reason: "structurally_ruled_out"`) when it does not. Deliberately narrow: a finding with no `routeId` (secrets, vulnerable dependencies, non-route entrypoints — corroborated by Layer 2 through a different structural surface) is never touched, to avoid the false-positive-suppression risk a broader "file not present anywhere in the App Map" rule would carry for those categories. Within one continuous scan this can never fire (Layer 2 already demotes anything it can't corroborate before promotion, and the same App Map instance grounds and confirms), so it is a real but narrow safety net for a resumed/distributed scan whose App Map was rebuilt between correlation and confirmation — not expected to change golden-corpus numbers, and it did not (see below). Effort allocation: `packages/confirm/src/investigation-pipeline.ts`'s `attemptInvestigationConfirmation` now scales the E1 investigation loop's turn budget (`ConfirmDeps.investigation.maxTurns`) per finding by the SAME `reachabilityScore × exposureScore × impactScore` product Layer 2 already persists and ranks by — no new scoring signal invented. Score ≥0.5 gets the caller's full configured budget, <0.15 gets roughly half, everything else three-quarters, floor 2 turns; the scaled value can only reduce the configured/default budget, never exceed it, and stays subject to `investigate.ts`'s unconditional `ABSOLUTE_MAX_INVESTIGATION_TURNS` ceiling regardless. Live DAST's adaptive-round cap (`live.ts`'s `MAX_ADAPTIVE_ROUNDS`) was deliberately left unscaled — a module-level constant threaded through substantially more call surface, judged disproportionate risk for this item's scope; a documented follow-up, not silently dropped. `packages/confirm` typecheck clean; full scoped suites green (`tests/confirm.*.test.ts` 111/111, `tests/fp-loop.tuning.test.ts` 7/7, `tests/correlation.*.test.ts` 52/52 unaffected as expected, `packages/qa`'s offline golden-corpus-equivalent gates — `tests/qa.corpus.test.ts`, `qa.baseline.test.ts`, `qa.blue-team-corpus.test.ts`, `qa.blue-team-baseline.test.ts`, `qa.scorer.test.ts`, `qa.metrics.test.ts`, `fp-loop.corpus.test.ts` — 69/69, identical pass count before and after). Did not run the expensive real-repo `pnpm corpus:scan` in this session: `apps/worker/src/e2e-scan.test.ts`'s golden-corpus-style grading currently fails (1 of 2 ground-truth findings missing) due to **A3's concurrent, still-in-progress work** in this same shared tree — verified by disabling this change's own gate locally and reproducing the identical failure unchanged, so it is not attributable to this item; left for A3's own closure to resolve rather than touched here. Updated `docs/modules/confirmation.md` (new "Structural Scoping Gate and Proportional Effort Allocation (A11)" entry + Update Triggers) and `docs/modules/correlation.md` (AGENT SEE cross-reference on the Scoring Engine entry — no scoring formula changed).

- [x] **(A12, P1)** Make enabling the fix agent loop actually enable multi-file reading: `maxToolCalls` defaulting to 0 means `MONTR_FIX_AGENT_LOOP_ENABLED=true` delivers a retry loop with no `read_file` tool at all, which is not what the env var's name or the deployment docs imply. Either raise the default or document the second knob prominently.

  RESOLVED — chose to raise the default. `FixAgentLoopConfigSchema.maxToolCalls` (`packages/config/src/schema.ts`) now defaults to `3` instead of `0` (still capped at 20), so `MONTR_FIX_AGENT_LOOP_ENABLED=true` alone now delivers the sandboxed `read_file` tool by default, matching the env var's own name and the feature's stated purpose. Belt-and-suspenders for the remaining edge case (an operator explicitly sets `MONTR_FIX_AGENT_LOOP_MAX_TOOL_CALLS=0`): `apps/worker/src/runners.ts`'s Layer 4 runner now logs a `worker.fix_agent_loop.no_tool_calls` warning whenever the loop is enabled with `maxToolCalls` at 0, and `docs/infra/environment.md`'s `MONTR_FIX_AGENT_LOOP_ENABLED`/`MONTR_FIX_AGENT_LOOP_MAX_TOOL_CALLS` entries now cross-reference each other explicitly. Updated the default-value assertions in `packages/config/src/fix-generation-config.test.ts` and added a new `apps/worker/src/runners.test.ts` case proving the warning fires; both suites pass (14/14, 20/20).

- [x] **(A13, P1)** Call `generateHardeningRecommendations` with a real `FileProvider` over the repo checkout and pass the result into `buildReport` as a precomputed input, so the seven hardening detectors produce output for real scans.

  RESOLVED (landed together with A2/A7/A8). Layer 5's runner now calls `packages/hardening/src/generate.ts`'s `generateHardeningRecommendations` using a real `@montr/discovery` `fsFileProvider` over the same `repoRoot` Layer 1/4 already resolve, threading the result into `BuildReportInput.hardeningRecommendations`. Degrades to an honest empty list (never a guess) when no local checkout exists (remote repo). Added `@montr/hardening` as a worker dependency. Verified via new `apps/worker/src/runners.test.ts` cases for both the real-generation and empty-checkout-degrade paths.

- [ ] **(A14, P2)** Give `gateway.stream()` its first consumer by streaming the A3 investigation narrative to the console — the progress-event sink is already threaded into the investigation loop, so this turns a multi-minute opaque wait into a visible agent trace.
- [x] **(A15, P2)** Reconcile the stale "not wired / later wave" header comments in a single pass (`packages/report/src/detection-rules/`, `packages/report/src/exports/mitre-attack.ts` both claim standalone status for code `report-builder.ts` now calls unconditionally), and adopt the rule that a "not wired yet" comment must name the task that will wire it.

  RESOLVED (landed in Wave 1; this checkbox and its `docs/overview.md` Recent Changes entry were both lost to a later concurrent-agent edit to these shared files and are being restored here — the actual code/comment changes are safely committed, unaffected). Corrected the stale "standalone/not wired yet" claims in `packages/report/src/detection-rules/generate.ts`, `packages/report/src/exports/mitre-attack.ts`, `packages/report/src/index.ts`, and `packages/report/src/detection-rules/index.ts` against the real call graph (`report-builder.ts`'s `buildBlueTeamReport` calls all of them unconditionally). Also corrected `packages/report/src/exports/index.ts`'s note about MITRE not being in the export-format registry to state plainly that no task is currently scoped to add it, rather than a vague "later wave." Comment-only changes, no logic touched.

- [ ] **(A16, P2)** Let the model author red-team payloads within guardrails — composed payloads validated against a deterministic safety predicate (non-destructive, within blast-radius caps, allowlisted target) before send — rather than selecting an id from a pre-written `PAYLOAD_VARIANTS` map. The two-layer guardrail architecture to permit this safely already exists.
- [x] **(A17, P2)** Adopt schema-constrained `responseSchema` decoding at the six JSON-returning call sites that currently ask for `responseFormat: "json"` and parse defensively, removing a class of retry/parse-failure handling. It is implemented and tested across all ten providers and used at exactly one site today.

  RESOLVED, with a correction to the finding's premise. Audited all seven real `responseFormat: "json"` call sites (six unaccounted for by the finding's "one site" count, plus `confirm/src/live.ts`'s already-`responseSchema`'d `chooseNextVariant`) against `packages/llm-gateway/src/structured-output.ts`. Six of the seven — `appmap/src/llm.ts` (appmap_labeling), `appmap/src/threat-model.ts` (threat_model), `discovery/src/triage.ts` (triage), `correlation/src/llm.ts` (correlation), `confirm/src/static.ts` (confirmation), `fix/src/generate.ts` (fix_generation) — were **already** receiving real, provider-side schema-constrained decoding automatically: `resolveStructuredOutputSchema` resolves a schema keyed by `metadata.purpose` alone (landed under A13, before this audit), and all ten provider adapters (verified directly: `mapping.ts`'s `buildAnthropicStyleFields` for Anthropic/Bedrock, `adapters/vertex.ts`, and `adapters/azure.ts`'s `buildBody` shared by Azure + all six OpenAI-compatible providers) apply it with zero call-site changes needed. Adding `request.responseSchema` explicitly at these six would only duplicate an already-live schema with no functional effect, and would reintroduce the schema-drift risk `structured-output.ts`'s own design deliberately avoids — so it was NOT done. Their JSON-parse try/catch fallbacks were also deliberately left in place: this codebase's explicit fail-safe posture (truncation/off-spec responses are a genuinely different failure mode from JSON shape, per this task's own instructions to preserve those), and several sites' flexible-shape branches are exercised by `packages/fixtures/src/llm.ts`'s non-schema-conformant canned test responses (confirmed concretely for `discovery/src/triage.ts`'s bare-object canned fixture) — stripping them would require rewriting that shared fixture, a much larger, riskier change with no production benefit for a P2 item. The seventh site, `confirm/src/adversarial.ts`'s `runOneVerifier` (E4 adversarial panel), was a **genuine bug**, not a redundancy: it shares `metadata.purpose: "confirmation"` with `static.ts` but its own lenses ask for a DIFFERENT shape (`{confirm, rationale}` vs. `static.ts`'s `{confirmed, argument}`). Under real schema enforcement the mismatched purpose-keyed default's `additionalProperties: false` would silently strip `confirm`/`rationale` from every real provider response, so every verifier would read as an undefined vote and always reject — invisible to every existing test, since all mock `LLMGateway.complete()` directly and never exercise real schema enforcement. Fixed by giving that one call site its own explicit `request.responseSchema` (the one site where `responseSchema` genuinely was the right fix, mirroring `live.ts`'s pre-existing `chooseNextVariant` precedent). No production caller reaches this path yet (blocked on A3 above), so no live customer impact today, but it would have silently neutered E4 the moment A3 lands. New `packages/confirm/src/adversarial.test.ts` (1 test, asserts the correct schema is sent and that a real `{confirm:true}` reply now counts as a confirm vote); full `packages/confirm` suite (110 tests) and `pnpm --filter @montr/confirm typecheck` both clean. Updated `docs/modules/llm-gateway.md`, `docs/modules/confirmation.md`, `docs/overview.md`. Did not touch `packages/appmap/src/threat-model.ts`, `discovery/src/triage.ts`, `correlation/src/llm.ts`, `confirm/src/static.ts`, or `fix/src/generate.ts` beyond reading them (see above for why); also did not touch `packages/confirm/src/investigate.ts`, `scenarios.ts`, or `purple-loop.ts` — explicitly out of scope this wave (concurrent work).

- [x] **(A18, P2)** Add an optional, clearly-labelled generated executive summary on top of the deterministic report — keeping the structured findings as the artifact of record — so the one part of the pipeline the customer actually reads is not the only part with no AI in it.

  RESOLVED. New optional `Report.generatedExecutiveSummary` field (`GeneratedExecutiveSummarySchema`, `packages/contracts/src/report.ts`), structurally separate from the existing deterministic `ExecutiveSummarySchema` so it can never be confused with the audited artifact of record — carries a fixed disclaimer, narrative, top priorities, and model/provider/timestamp provenance, and is absent (not an empty object) unless generation actually ran and succeeded. New `packages/report/src/executive-summary.ts`'s `generateExecutiveSummary` is `@montr/report`'s first real LLM gateway consumer — the gateway is dependency-injected (never imported directly), mirroring `packages/discovery/src/triage.ts`'s "skip when no gateway is provided" convention, so `buildReport`/`buildBlueTeamReport` themselves stay pure/offline. Uses a new `metadata.purpose: "executive_summary"` with a dedicated schema-constrained `responseSchema` entry (per the A17 standard, not defensive parsing). New `reporting.executiveSummary.{enabled, maxTokens}` config, env vars `MONTR_REPORTING_EXECUTIVE_SUMMARY_ENABLED`/`_MAX_TOKENS` — **`enabled` defaults to `false`**, a deliberate reversal of A3's default-ON exception, since this session already shipped one real cost-increasing default-ON change (A3) and stacking a second was unnecessary risk. Wired into `apps/worker/src/runners.ts`'s Layer 5 handler as a small surgical addition after `buildReport` completes; budget hard-halt covered automatically via the scan's shared `LLMGateway` instance (same mechanism A3 verified). New `apps/web/src/components/generated-executive-summary.tsx` renders a visually distinct, "AI-GENERATED"-badged card on the Report tab, only when the field is present. All scoped suites green (contracts 251/251, llm-gateway 219/219, config 61/61, report-related root tests 76/76, worker green after the severity-scoping test fix noted under A3's correction). Updated `docs/modules/reporting-vcs.md`, `docs/modules/llm-gateway.md`, `docs/infra/environment.md`, `docs/modules/web-console.md`.

## Suggested enhancements

- [x] Make the investigator reachable and scope it initially to unconfirmed high/critical findings — the single highest-value change available, targeting the 0%-recall IDOR and broken-access-control categories directly. (Closed via A3.)
- [x] Fix the Layer 5 plumbing: one argument unlocks four report sections, two more unlock the rest, and everything downstream is already tested. (Closed via A2/A7/A8/A13.)
- [x] Wire worker-side scenario execution (with the request-body field added first), closing the purple-team loop for real and removing a false statement from the console. (Closed via A10 + A1.)
- [x] Wire the semantic index and add an OpenAI embeddings adapter — the adapter is near-free, and semantic retrieval materially strengthens the investigator over `grep` alone on a large repository. (Closed via A9.)
- [x] Add a top-level Blue Team section with cross-scan aggregation — likely the most compelling screen in the product for the security-lead buyer, and every input for it exists once the Layer 5 plumbing lands. (Closed via A5.)
- [ ] Ship detection rules as a real push integration (Splunk, Elastic, Sentinel) rather than only a download, converting an advisory artifact into a surface a SOC team touches weekly.
- [ ] Add detection-coverage regression gating in CI, mirroring the existing golden-corpus gate — fail a build when a newly confirmed finding lands on a route with no telemetry.
- [x] Add adaptive scoping and effort allocation to the orchestrator — the first change that makes the model influence control flow rather than content, and a cost reduction rather than only a capability add. (Closed via A11.)
- [ ] Allow payload authorship within guardrails, validated against a deterministic safety predicate before send.
- [ ] Extend cross-scan learning beyond false positives — carry confirmed exploit shapes and per-repo sanitizer conventions in the learned-facts mechanism so recall improves scan over scan rather than staying flat.
- [ ] Stream the investigation narrative to the console — both a UX win and the most effective possible demonstration of the agentic capability.
- [ ] Extend `scripts/check-unwired-seams.mjs` to cover every unwired seam rather than the six currently hand-declared. It passes green today while at least seven further seams have no caller; a checker structurally incapable of catching the dominant defect class is worse than none, because it is trusted.
- [x] Add one real end-to-end blue-team assertion to the existing e2e scan path so mock-backed console verification can never again mask an empty production payload. (Closed via A6.)
- [x] Reconcile the stale header comments in a single pass and require every "not wired yet" comment to name the task that will wire it. (Closed via A15.)

---

Four items in this list turned on decisions that are the owner's, not engineering calls — sequencing against the red precision gate (A4 before A3, resolved), the cost/latency default for the investigator (A3, resolved 2026-09-12: ON by default, scoped to unconfirmed high/critical findings), whether autonomous attack execution is a product to ship at all (A1, still open), and whether Layer 5 should use an LLM (A18, still open). Each is stated with a recommendation in the source audit's "Decisions that need an owner" section; settle the remaining two before starting A1.
