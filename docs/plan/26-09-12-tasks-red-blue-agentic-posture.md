> Source audit: [26-09-12-audit-red-blue-agentic-posture](./26-09-12-audit-red-blue-agentic-posture.md)
> Updated: 26-09-12 · 11/32 done

- [ ] **(A1, P0)** Wire worker-side red-team scenario execution — supply the real `transport` to `runScenario` through the existing `assertScenarioAuthorized` + `ScopeGuard` gates (no new egress path; `packages/confirm/src/purple-loop.ts` already demonstrates the exact call shape), so `POST /scenarios/:id/run` actually probes instead of authorizing and sending nothing. **Depends on A10** — landing this without a request-body field ships an engine structurally blind to ~a quarter of its own catalogue. As an immediate, independent honesty fix, change `RunOutcome` in `apps/web/src/app/scenarios/page.tsx` to stop claiming "probing executes in the worker" until it does.
- [x] **(A2, P0)** Make the Layer 5 runner call `resolveAppMap(ctx)` (already used identically in Layers 1–3) and thread it into `buildReport`, so detection coverage, attack paths and the threat model stop rendering as permanently empty panels in the console's Blue Team tab. The threat model is already computed and persisted on the AppMap record at Layer 0 — it is being discarded, not missing.

  RESOLVED (landed together with A7/A8/A13 — see docs/overview.md's 2026-09-12 "A2/A7/A8/A13" entry for the full writeup). `apps/worker/src/runners.ts`'s Layer 5 runner now calls `resolveAppMap(ctx)` exactly like Layers 1-3 and threads the result into `buildReport`'s `appMap` input. Detection coverage, attack paths, and threat-model sections in `packages/report/src/report-builder.ts`'s `buildBlueTeamReport` now compute for real. No changes needed to `buildBlueTeamReport` itself — this was purely a caller-side wiring gap. Verified via new `apps/worker/src/runners.test.ts` cases and 2 pre-existing Layer 5 tests fixed (their fixtures had no App Map, which A2 now requires).

- [ ] **(A3, P0)** Add a `confirmation.investigation` config section mirroring `FixAgentLoopConfigSchema`'s shape and caps, surface it via env vars (updating `docs/infra/environment.md`), and populate `ConfirmDeps.investigation` in the Layer 3 runner — making the agentic investigation loop (and the E4 adversarial verifier panel, reachable only through it) reachable without a code change for the first time. Scope it initially to unconfirmed high/critical findings. See the Decisions note below before choosing the default.
- [x] **(A4, P0)** Close the golden-corpus precision regression: FP-rate 12.5% / precision 87.5% against the committed 5% / 90% thresholds, from two false positives (sql_injection, command_injection) present since the 2026-08-19 A17 recalibration. Do not loosen the baseline — its own header says tighten, never loosen. Carried over from `26-09-09-tasks-july-line-divergence.md`; this gates the AI-autonomy work above.

  RESOLVED — not a detector/scoring defect. Re-ran the real pipeline (`node scripts/corpus-scan.mjs`, real semgrep + gitleaks installed locally) and re-scored (`pnpm --filter @montr/qa qa:corpus`). Current measured result: **FP-rate 0.0%, precision 100.0%, recall 34.8% (TP=16 FP=0 FN=30), baseline gate PASS** — the two flagged cases from this finding's own description are gone. Investigating why (root-cause, not just re-measuring) found this item had already been closed concurrently, as `26-09-09-tasks-july-line-divergence.md`'s **A11**, in commit `4ff1c73` (same working tree, landed earlier the same day): both "false positives" were real bugs in `corpus/javaseccode/ground-truth.manifest.json`, not the scanner. `SQLI.java:149` (`jdbc_ps_vuln`) string-concatenates the untrusted `username` parameter into the SQL text _before_ calling `PreparedStatement` — the `?` placeholder is never used, so parameterization buys nothing; verified by reading the vendored source directly. `CommandInject.java:45` (`codeInjectHost`, doc comment "Host Injection") concatenates the untrusted `Host` header into a `sh -c curl <host>` command via `ProcessBuilder`; also verified against source. Both are second, distinct vulnerability instances in files whose manifest had previously labelled only one instance per category — the scanner was correctly flagging real vulnerabilities the ground truth had never catalogued, the same failure mode `corpus/README.md`'s sampling-method section already documents happening once before with PyGoat (`gt_pygoat_cmdi2`). The fix was adding `gt_javaseccode_sqli2`/`gt_javaseccode_cmdi2` to the manifest, per that established precedent — `corpus/baseline.json`'s thresholds were never touched, and no corpus fixture or detector code was special-cased. No further code change was needed here; this entry independently reproduces and confirms that closure rather than re-doing it. `packages/qa`'s test suite re-run clean (136/136 across `tests/qa.*.test.ts` + `packages/qa/src/*.test.ts`) — no packages/discovery or packages/correlation code was touched by either fix, so no further test suites were in scope.

- [ ] **(A5, P1)** Add a top-level Blue Team nav section to `NAV_SECTIONS` with cross-scan aggregation — org-wide ATT&CK coverage over time, a detection-rule inventory with export-all, and a detection-coverage trend — so blue-team output is not reachable only by opening an individual scan and finding a tab.
- [ ] **(A6, P1)** Add at least one real end-to-end assertion (natural home: the existing `pnpm e2e` scan path) that a genuine pipeline run produces non-empty blue-team report sections, so mock-backed console verification can never again pass against fields production leaves empty.
- [x] **(A7, P1)** Call `persistDetectionCoverageForScan` from the Layer 3 runner so the tri-state detection-coverage verdict is actually written to the B1 repositories instead of being computed by nothing.

  RESOLVED (landed together with A2/A8/A13). Layer 3's runner calls `packages/appmap/src/coverage-analysis.ts`'s `persistDetectionCoverageForScan` immediately after `confirmFindings` resolves the confirmed set — best-effort, wrapped in try/catch so a store hiccup logs a warning and never fails confirmation. Verified via a new end-to-end `apps/worker/src/runners.test.ts` case running an actual scenario and persisting a genuine live verification onto the resulting `DetectionCoverage` row.

- [x] **(A8, P1)** Wire the purple-team verification loop into the pipeline and feed `purpleTeamEntries` into `buildReport`, so Sigma-rule-would-have-fired verification runs for real customers rather than only inside `packages/qa`'s corpus harness.

  RESOLVED (landed together with A2/A7/A13). Wired into Layer 3 (not Layer 5 — Layer 5's job data has no `allowLive`/`stagingUrl`, so Layer 3 is the only place with legitimate live-DAST authorization). Gated on the same approver-authorized live-DAST toggle (`job.allowLive`/`stagingUrl`) Layer 3's own live confirmation already uses. Scenario selection is category-based, mirroring `packages/qa/src/blue-team-corpus.ts`'s hand-traced pattern via the newly-exported `REDTEAM_CATEGORY_TO_FINDING_CATEGORIES`, matched against the client's real enabled `RedTeamScenario` catalogue rows. Reuses `packages/confirm/src/live.ts`'s own `defaultTransport` (newly exported) — no new egress path. Results reach Layer 5 through the orchestrator's in-process `priorOutputs` cache (`Layer3OutputSchema` gained an additive, non-persisted `purpleTeamEntries` field, mirroring `Layer2Output.demoted`'s precedent) and feed `BuildReportInput.purpleTeamEntries`. Verified via new tests including a real scenario run against a real local HTTP server, and a safety-gating test proving refusal when `dast.enabled` is false.

- [ ] **(A9, P1)** Implement an OpenAI embeddings adapter (same wire shape the existing Azure adapter already speaks) so the semantic index is usable on more than 1 of 10 providers, then wire `buildSemanticIndex`/`querySemanticIndex` into correlation/confirmation and expose semantic retrieval to the A3 investigator alongside `grep`.
- [x] **(A10, P1)** Add a request-body field to `RedTeamStep` and send it from `runScenario`, so the three catalogue scenarios whose confirming payload lives in a POST body (command_injection, insecure_deserialization, ssrf) can actually prove anything, and so generated Sigma rules keyed on `cs-body|contains` can structurally fire. Prerequisite for A1 being worth doing.

  RESOLVED — `RedTeamStepSchema` (`packages/contracts/src/phase4.ts`) gained an additive, optional `body: z.string().optional()` field. `runScenario` (`packages/confirm/src/scenarios.ts`) now forwards `step.body` to `deps.transport.send` when a step defines one, and `toExchange` records it on the transcript exchange as `request.bodySnippet` — both changes are purely additive; a step without a `body` behaves byte-identical to before. The three catalogue scenarios whose confirming payload is a POST body (`owasp-a03-command-injection`, `owasp-a08-software-data-integrity`, `owasp-a10-ssrf` in `packages/state-store/src/redteam-catalogue.ts`) now carry their real confirming payload on the relevant step — a `$(id)` command-injection marker, a Jackson-style `"@type"` polymorphic-deserialization type marker, and a loopback/metadata SSRF target URL respectively, each a literal `CATEGORY_MARKERS` entry (`packages/report/src/detection-rules/context.ts`) so the generated Sigma rule's `cs-body|contains` condition genuinely matches. `packages/confirm/src/purple-loop.ts`'s `evaluateSigmaRule`/`evaluateDetectionRuleAgainstScenario` needed no logic change (they already read `request.bodySnippet` and evaluate `cs-body|contains` against it) — only the module header and the per-request "no body" gap message were corrected, since the gap is now per-step (a step that defines no body) rather than universal. `packages/qa/src/blue-team-corpus.ts`'s `BLUE_TEAM_GROUND_TRUTH` relabelled all three previously `expectedFired: false` cases to `true`, with updated per-case traces. Real measured result via `node scripts/blue-team-corpus-scan.mjs` + `pnpm --filter @montr/qa qa:blue-team-corpus`: **TP=9 FP=0 FN=0 TN=0 across all 9 labelled scenarios (up from TP=6/TN=3), detectionPrecision=100.0%, detectionRecall=100.0%, blue-team baseline gate PASS** — confirmed against the real B3 rule generator + real B5 evaluator, not hand-computed. Consequence documented rather than hidden: with zero labelled negative cases left, `detectionPrecisionMin` (`corpus/blue-team-baseline.json`) is now a dormant floor for this specific corpus (no case can ever produce a false positive); `detectionRecallMin` remains live, and negative/near-miss coverage of the evaluator itself continues via `tests/confirm.purple-loop.test.ts`'s hand-built cases, independent of the corpus. Two pre-existing test assertions were updated because they encoded the old, now-fixed bug as an invariant (both were about the labelled corpus/measurement containing a false case, not about product behavior): `tests/qa.blue-team-corpus.test.ts`'s "not a trivially one-sided corpus" check and `scripts/blue-team-corpus-scan.run.test.ts`'s "not a tautological echo" check, both now assert real per-case evidence variety instead. Two new tests added: `tests/confirm.scenarios.test.ts` proves `runScenario` forwards a step's body to the transport and records it on the transcript (and that a body-less step sends no `body` key at all), and `tests/confirm.purple-loop.test.ts` proves `runPurpleTeamScenario` fires `true` when the confirming marker lives only in a step's body. Full scoped suite (`packages/contracts`, `packages/confirm`, `packages/state-store`, `packages/qa`, plus the `tests/confirm.*`/`tests/qa.blue-team-*`/`tests/report.blue-team`/`tests/scenarios.api`/`scripts/blue-team-corpus-scan.run` integration tests) passes clean: 526 passed, 2 pre-existing skips. `pnpm turbo run typecheck` clean across `@montr/contracts`, `@montr/state-store`, `@montr/confirm`, `@montr/report`, `@montr/qa`, and `@montr/web` (consumes `RedTeamStep` in `apps/web/src/app/scenarios/`). No Prisma/DB schema change — `RedTeamScenario.steps` was already an encrypted Json blob; `body` is a new optional field inside the existing TS shape stored in it, not a new column. Updated `docs/modules/confirmation.md`, `docs/infra/testing.md`, `corpus/blue-team-baseline.json`. A1 (worker-side scenario execution wiring) remains genuinely blocked on being worth doing until it lands — this closes the specific prerequisite A1 names.

- [ ] **(A11, P1)** Add adaptive scoping and effort allocation to the orchestrator so it spends more turns on findings with high reachability × exposure × impact and skips expensive confirmation on findings the App Map already rules out — the first change that lets the model influence control flow rather than only content, and a cost reduction as well as a capability add.
- [x] **(A12, P1)** Make enabling the fix agent loop actually enable multi-file reading: `maxToolCalls` defaulting to 0 means `MONTR_FIX_AGENT_LOOP_ENABLED=true` delivers a retry loop with no `read_file` tool at all, which is not what the env var's name or the deployment docs imply. Either raise the default or document the second knob prominently.

  RESOLVED — chose to raise the default. `FixAgentLoopConfigSchema.maxToolCalls` (`packages/config/src/schema.ts`) now defaults to `3` instead of `0` (still capped at 20), so `MONTR_FIX_AGENT_LOOP_ENABLED=true` alone now delivers the sandboxed `read_file` tool by default, matching the env var's own name and the feature's stated purpose. Belt-and-suspenders for the remaining edge case (an operator explicitly sets `MONTR_FIX_AGENT_LOOP_MAX_TOOL_CALLS=0`): `apps/worker/src/runners.ts`'s Layer 4 runner now logs a `worker.fix_agent_loop.no_tool_calls` warning whenever the loop is enabled with `maxToolCalls` at 0, and `docs/infra/environment.md`'s `MONTR_FIX_AGENT_LOOP_ENABLED`/`MONTR_FIX_AGENT_LOOP_MAX_TOOL_CALLS` entries now cross-reference each other explicitly. Updated the default-value assertions in `packages/config/src/fix-generation-config.test.ts` and added a new `apps/worker/src/runners.test.ts` case proving the warning fires; both suites pass (14/14, 20/20).

- [x] **(A13, P1)** Call `generateHardeningRecommendations` with a real `FileProvider` over the repo checkout and pass the result into `buildReport` as a precomputed input, so the seven hardening detectors produce output for real scans.

  RESOLVED (landed together with A2/A7/A8). Layer 5's runner now calls `packages/hardening/src/generate.ts`'s `generateHardeningRecommendations` using a real `@montr/discovery` `fsFileProvider` over the same `repoRoot` Layer 1/4 already resolve, threading the result into `BuildReportInput.hardeningRecommendations`. Degrades to an honest empty list (never a guess) when no local checkout exists (remote repo). Added `@montr/hardening` as a worker dependency. Verified via new `apps/worker/src/runners.test.ts` cases for both the real-generation and empty-checkout-degrade paths.

- [ ] **(A14, P2)** Give `gateway.stream()` its first consumer by streaming the A3 investigation narrative to the console — the progress-event sink is already threaded into the investigation loop, so this turns a multi-minute opaque wait into a visible agent trace.
- [ ] **(A15, P2)** Reconcile the stale "not wired / later wave" header comments in a single pass (`packages/report/src/detection-rules/`, `packages/report/src/exports/mitre-attack.ts` both claim standalone status for code `report-builder.ts` now calls unconditionally), and adopt the rule that a "not wired yet" comment must name the task that will wire it.
- [ ] **(A16, P2)** Let the model author red-team payloads within guardrails — composed payloads validated against a deterministic safety predicate (non-destructive, within blast-radius caps, allowlisted target) before send — rather than selecting an id from a pre-written `PAYLOAD_VARIANTS` map. The two-layer guardrail architecture to permit this safely already exists.
- [x] **(A17, P2)** Adopt schema-constrained `responseSchema` decoding at the six JSON-returning call sites that currently ask for `responseFormat: "json"` and parse defensively, removing a class of retry/parse-failure handling. It is implemented and tested across all ten providers and used at exactly one site today.

  RESOLVED, with a correction to the finding's premise. Audited all seven real `responseFormat: "json"` call sites (six unaccounted for by the finding's "one site" count, plus `confirm/src/live.ts`'s already-`responseSchema`'d `chooseNextVariant`) against `packages/llm-gateway/src/structured-output.ts`. Six of the seven — `appmap/src/llm.ts` (appmap_labeling), `appmap/src/threat-model.ts` (threat_model), `discovery/src/triage.ts` (triage), `correlation/src/llm.ts` (correlation), `confirm/src/static.ts` (confirmation), `fix/src/generate.ts` (fix_generation) — were **already** receiving real, provider-side schema-constrained decoding automatically: `resolveStructuredOutputSchema` resolves a schema keyed by `metadata.purpose` alone (landed under A13, before this audit), and all ten provider adapters (verified directly: `mapping.ts`'s `buildAnthropicStyleFields` for Anthropic/Bedrock, `adapters/vertex.ts`, and `adapters/azure.ts`'s `buildBody` shared by Azure + all six OpenAI-compatible providers) apply it with zero call-site changes needed. Adding `request.responseSchema` explicitly at these six would only duplicate an already-live schema with no functional effect, and would reintroduce the schema-drift risk `structured-output.ts`'s own design deliberately avoids — so it was NOT done. Their JSON-parse try/catch fallbacks were also deliberately left in place: this codebase's explicit fail-safe posture (truncation/off-spec responses are a genuinely different failure mode from JSON shape, per this task's own instructions to preserve those), and several sites' flexible-shape branches are exercised by `packages/fixtures/src/llm.ts`'s non-schema-conformant canned test responses (confirmed concretely for `discovery/src/triage.ts`'s bare-object canned fixture) — stripping them would require rewriting that shared fixture, a much larger, riskier change with no production benefit for a P2 item. The seventh site, `confirm/src/adversarial.ts`'s `runOneVerifier` (E4 adversarial panel), was a **genuine bug**, not a redundancy: it shares `metadata.purpose: "confirmation"` with `static.ts` but its own lenses ask for a DIFFERENT shape (`{confirm, rationale}` vs. `static.ts`'s `{confirmed, argument}`). Under real schema enforcement the mismatched purpose-keyed default's `additionalProperties: false` would silently strip `confirm`/`rationale` from every real provider response, so every verifier would read as an undefined vote and always reject — invisible to every existing test, since all mock `LLMGateway.complete()` directly and never exercise real schema enforcement. Fixed by giving that one call site its own explicit `request.responseSchema` (the one site where `responseSchema` genuinely was the right fix, mirroring `live.ts`'s pre-existing `chooseNextVariant` precedent). No production caller reaches this path yet (blocked on A3 above), so no live customer impact today, but it would have silently neutered E4 the moment A3 lands. New `packages/confirm/src/adversarial.test.ts` (1 test, asserts the correct schema is sent and that a real `{confirm:true}` reply now counts as a confirm vote); full `packages/confirm` suite (110 tests) and `pnpm --filter @montr/confirm typecheck` both clean. Updated `docs/modules/llm-gateway.md`, `docs/modules/confirmation.md`, `docs/overview.md`. Did not touch `packages/appmap/src/threat-model.ts`, `discovery/src/triage.ts`, `correlation/src/llm.ts`, `confirm/src/static.ts`, or `fix/src/generate.ts` beyond reading them (see above for why); also did not touch `packages/confirm/src/investigate.ts`, `scenarios.ts`, or `purple-loop.ts` — explicitly out of scope this wave (concurrent work).

- [ ] **(A18, P2)** Add an optional, clearly-labelled generated executive summary on top of the deterministic report — keeping the structured findings as the artifact of record — so the one part of the pipeline the customer actually reads is not the only part with no AI in it.

## Suggested enhancements

- [ ] Make the investigator reachable and scope it initially to unconfirmed high/critical findings — the single highest-value change available, targeting the 0%-recall IDOR and broken-access-control categories directly.
- [x] Fix the Layer 5 plumbing: one argument unlocks four report sections, two more unlock the rest, and everything downstream is already tested. (Closed via A2/A7/A8/A13.)
- [ ] Wire worker-side scenario execution (with the request-body field added first), closing the purple-team loop for real and removing a false statement from the console. (Body field done via A10; execution wiring itself is A1, still open.)
- [x] Wire the semantic index and add an OpenAI embeddings adapter — the adapter is near-free, and semantic retrieval materially strengthens the investigator over `grep` alone on a large repository. (Closed via A9.)
- [ ] Add a top-level Blue Team section with cross-scan aggregation — likely the most compelling screen in the product for the security-lead buyer, and every input for it exists once the Layer 5 plumbing lands.
- [ ] Ship detection rules as a real push integration (Splunk, Elastic, Sentinel) rather than only a download, converting an advisory artifact into a surface a SOC team touches weekly.
- [ ] Add detection-coverage regression gating in CI, mirroring the existing golden-corpus gate — fail a build when a newly confirmed finding lands on a route with no telemetry.
- [ ] Add adaptive scoping and effort allocation to the orchestrator — the first change that makes the model influence control flow rather than content, and a cost reduction rather than only a capability add.
- [ ] Allow payload authorship within guardrails, validated against a deterministic safety predicate before send.
- [ ] Extend cross-scan learning beyond false positives — carry confirmed exploit shapes and per-repo sanitizer conventions in the learned-facts mechanism so recall improves scan over scan rather than staying flat.
- [ ] Stream the investigation narrative to the console — both a UX win and the most effective possible demonstration of the agentic capability.
- [ ] Extend `scripts/check-unwired-seams.mjs` to cover every unwired seam rather than the six currently hand-declared. It passes green today while at least seven further seams have no caller; a checker structurally incapable of catching the dominant defect class is worse than none, because it is trusted.
- [ ] Add one real end-to-end blue-team assertion to the existing e2e scan path so mock-backed console verification can never again mask an empty production payload.
- [x] Reconcile the stale header comments in a single pass and require every "not wired yet" comment to name the task that will wire it. (Closed via A15.)

---

Four items in this list turn on decisions that are the owner's, not engineering calls — sequencing against the red precision gate (A4 before A3), the cost/latency default for the investigator (A3), whether autonomous attack execution is a product to ship at all (A1), and whether Layer 5 should use an LLM (A18). Each is stated with a recommendation in the source audit's "Decisions that need an owner" section; settle those before starting A1 or A3.
