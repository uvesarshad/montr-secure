/**
 * Tailwind CSS v4 pipeline. v4 moved the PostCSS plugin into
 * `@tailwindcss/postcss`; content is auto-detected (no tailwind.config needed).
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
